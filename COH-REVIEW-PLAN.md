# CLASS OF HAPPINESS — ARCHITECTURAL REVIEW FINDINGS & FIX PLAN
**Date: 18 August 2026 · Read-only review per COH-HANDOVER.md Section 10 · Verified against real source files, live Supabase data, and the live website**

Every finding below was checked against actual code (file:line), live Supabase queries (via the service key in `backend/.env`), and — for the website — a live `curl` of classofhappiness.com, not against COH-HANDOVER.md's claims or stale local file copies. Several handover claims turned out to be stale, incomplete, or backwards; those are flagged explicitly.

Items are grouped **LAUNCH-BLOCKING** (must be true before real money/real school data is on the line) → **LAUNCH-ACCEPTABLE** (real, evidenced problems that don't block launch — ship and fix soon) → **POST-LAUNCH** (explicitly deferred by Jono). Within each group, items still map back to the original review's Section 6 (Admin Sync) / Section 7 (Fundamental Issues) framing, noted in each heading. Execute one item at a time with Sonnet.

---

## VERIFICATION GAP — READ THIS FIRST

`portal.html` and the legacy `admin.html` are **not in the git repo** — hand-uploaded to Namecheap/cPanel, and no local copy could be confirmed as exactly what's live. This review used the most-recently-modified local scratch copies as the best available proxy:
- Portal: `/Users/jono/Desktop/portal100.html` (Aug 18 08:38 — same day as this review, plausibly current)
- Legacy admin: `/Users/jono/Downloads/coh_admin-2.html` (Aug 4 08:32 — 2 weeks stale relative to portal)

The marketing site **was** checked live via `curl https://classofhappiness.com`, and that check proved the risk exactly: two findings from the stale local copy (Jul 30) turned out to already be fixed live. **Before executing any portal.html/admin.html fix in this plan, pull the actual live file from cPanel first** — the same gap almost certainly applies there too.

---

# LAUNCH-BLOCKING

### L1. [SECURITY] Hardcoded superadmin bypass code shipped in the app bundle *(Section 6 #1)* — ✅ DONE 2026-08-18
**File:** `frontend/app/admin/dashboard.tsx:1123-1141`

The in-app admin unlock screen calls `POST /admin/verify` to check the real session's role (correct, fixed Aug 14 — `backend/server.py:7031`). But the `catch` block still contains a **client-side fallback**:
```
} catch {
  if (adminCode === 'COH_SUPER_2026') { setUnlocked(true); setIsSuperAdmin(true); }
  else if (adminCode.length === 6) { setUnlocked(true); setIsSuperAdmin(false); }
  else { Alert.alert('Invalid code'); }
}
```
This fires on ANY network failure (Railway cold start, transient timeout, offline device) — not just a missing endpoint. The hardcoded string `COH_SUPER_2026` is sitting in the shipped JS bundle and grants full superadmin unlock with zero server validation; **any 6-character string** grants school_admin-level unlock. Once unlocked, `isSuperAdmin` is a plain local `useState` boolean (`:1100`) never re-verified for the component's lifetime. Entry to this screen is role-gated upstream (`frontend/app/settings.tsx:530-533` only shows "Admin Dashboard" to `admin`/`superadmin`/`school_admin`), so it isn't reachable by a random teacher/parent — but it's reachable by exactly the population (school admins) for whom this wrongly grants a higher privilege tier than they should ever get client-side.

**Fix:** delete the entire `catch` fallback block. If `/admin/verify` fails, show a retry/error state — never grant access client-side.

**What was actually done:**
1. **Verified the real path first**, as required before touching this: logged in live against production (`https://class-of-happiness-production.up.railway.app`) as `jono@classofhappiness.com` with PIN `COH2026JONO` — confirmed `role: superadmin` and a valid session token. Called `POST /admin/verify` with that real session token and got `{valid: true, is_super_admin: true}` back — the primary, server-checked path works correctly and doesn't depend on the fallback in any way.
2. **Removed the hardcoded bypass.** The `catch` block in `unlock()` (`frontend/app/admin/dashboard.tsx:1132-1141`) no longer contains the `COH_SUPER_2026` string or the "any 6-character code" school_admin fallback — a network/server failure now shows an error alert and denies access, full stop. Diff confirmed isolated to exactly this block (7 lines removed, replaced with one `Alert.alert` call) — no other part of the file touched.
3. `npx tsc --noEmit` shows 3 pre-existing type errors in this file (lines 32, 1230, 1234) — all unrelated to the edited lines (1123-1135), confirmed present regardless of this change.

**Not yet done — flag before considering this fully live:** this fix is committed to the repo but has **not been deployed** (no `git push`, no Expo/EAS rebuild). The hardcoded bypass remains live in any already-shipped app bundle and in the current Railway/Expo deployment until a new build goes out. Deploying is a separate, more consequential action (affects real users' installed app) — confirm with Jono before pushing/rebuilding.

### L2. School subscription pricing — backend and Stripe must be updated to match the website *(Section 7 #2)* — ✅ DONE & VERIFIED 2026-08-18
**Files:** `backend/server.py:67-79` (`SUBSCRIPTION_PLANS`), `backend/server.py:10568-10572` (`_get_plan_from_price`), `backend/server.py:6029-6031` (`GET /subscription/plans`, confirmed live-consumed by the app at `frontend/src/utils/api.ts:199`), Stripe dashboard products/prices, `portal100.html` (verified below).

**Ground truth from Jono: the website's school pricing is correct.** Live-confirmed via `curl https://classofhappiness.com` (`calcSavings()`, lines 1239-1241): **Starter €499/yr (≤5 teachers), Standard €999/yr (≤15 teachers), Plus €1,999/yr (>15 teachers)** — three tiers, differing by included teacher count.

The backend today defines only **two** school tiers at the **wrong** prices: `school_small` (`server.py:67-73`, €399/year, "5 teacher accounts, 150 students") and `school_large` (`server.py:74-80`, €1,499/year, "unlimited teachers"). This is a missing middle tier, not just two wrong numbers — there is no backend plan at all for a school with 6-15 teachers, and neither existing price matches any of the three real prices.

Confirmed **portal.html does not display school-plan pricing anywhere** (grepped for 399/499/999/1499/1999/"School Plan"/"Starter plan" — zero matches). The portal's "Subscriptions & Revenue" dashboard aggregates real subscription data rather than hardcoding plan prices, so it needs no direct price edit — but should be spot-checked after the fix to confirm its revenue totals reconcile against the corrected tiers.

Why this is launch-blocking, not cosmetic:
1. **The app itself shows the wrong prices.** `/subscription/plans` (`server.py:6029`) returns `SUBSCRIPTION_PLANS` directly and is fetched by the live app — any school admin viewing pricing in-app today sees €399/€1,499, not €499/€999/€1,999.
2. **Stripe products/prices must exist for the real three tiers.** No Stripe price IDs are hardcoded anywhere in the codebase (`grep` for `price_1`/`payment-link`/`buy.stripe.com` returns nothing) — checkout appears to rely on Stripe-hosted products/prices set up directly in the dashboard (acct `1TBwO3GVgsNSHYyt`), which must be created/verified for Starter/Standard/Plus before any school can be charged the advertised amount.
3. **The Stripe webhook can't currently tell school tiers apart at all.** `_get_plan_from_price` (`server.py:10568-10572`) only branches on whether the Stripe price id/nickname contains `"parent"` or `"teacher"` — every school price falls through to a generic `"subscriber"` label. Live-verified: `subscription_plan` is `NULL` for every user today regardless of `subscription_status` — no evidence school tier is tracked anywhere post-payment.
4. The closest thing to seat-based enforcement is informational only: `school_profiles.subscription_seats` (default 5, `server.py:8814`) and a `seats_used` count (`server.py:8595`) shown on the school admin's billing view — no code path blocks inviting more teachers than the seat count. Confirm whether that should become real enforcement now that tiers are correctly seat-differentiated.

**Fix (in order):**
1. Update `SUBSCRIPTION_PLANS` in `server.py:67-79` to three tiers matching the website exactly: Starter €499/yr (≤5 teachers), Standard €999/yr (≤15 teachers), Plus €1,999/yr (>15 teachers).
2. Create/verify the three corresponding Stripe products+prices (acct `1TBwO3GVgsNSHYyt`); confirm whatever generates the checkout link/session points at the correct price IDs (see L3 below — this checkout path is itself broken today and needs fixing in the same pass).
3. Extend `_get_plan_from_price` (`server.py:10568-10572`) with real branches for the three school price ids/nicknames so `subscription_plan` gets set correctly on webhook events, instead of falling through to `"subscriber"`.
4. Decide whether `subscription_seats`/`seats_used` should become real enforcement now that tiers are seat-differentiated, or stay informational by design.
5. Re-test school signup → Stripe checkout → webhook → `/subscription/plans` display end-to-end once (1)-(3) land — this touches real billing.

**Teacher pricing (€7.99/month) is unchanged** — independently verified to already match the live site exactly.

**What was actually done:**
1. `SUBSCRIPTION_PLANS` (`server.py:52-84`) restructured to three tiers exactly matching the website: `school_starter` €499/yr (≤5 teachers), `school_standard` €999/yr (≤15 teachers, previously didn't exist at all), `school_plus` €1,999/yr (>15 teachers). `price_aud` values are explicitly commented as rough placeholders, not real currency conversion — real multi-currency work is P1 (post-launch).
2. `_get_plan_from_price` (`server.py:10745-10755`) extended with `school_starter`/`school_standard`/`school_plus` branches.
3. **Real bug found and fixed while wiring this up:** the Stripe webhook (`server.py:~10800`) was passing only `price.id` (Stripe's auto-generated random string, e.g. `price_1Abc...`) into the plan-matching logic — which would **never** contain "teacher"/"parent"/"school_starter" no matter what Jono named the product, since Dashboard-created prices can't have a custom `id`. The actual identifying name lives in `price.nickname` or `price.lookup_key`. Fixed to check nickname → lookup_key → id, in that order, so it works regardless of which field ends up populated. Without this fix, none of the plan-matching work above would have actually worked in production.
4. **Confirmed with Jono:** Teacher Monthly (€7.99) and Parent Monthly Stripe prices already existed with no nickname set; Jono has since set their nicknames to `teacher_monthly`/`parent_monthly` — matches this code's expectations exactly.

**Deployed and verified live** (2026-08-18, against Stripe test mode — see L3 for the full test walkthrough): `GET /subscription/plans` confirmed returning all 5 correct plans in production. A real Stripe test subscription on the teacher price was created via the API, and the webhook correctly classified and persisted `subscription_plan: "teacher"` — proving the nickname-matching fix (#3 above) actually works end-to-end, not just in theory.

**Two more real bugs found and fixed only while live-testing** (not from static review — these only surface when the code actually runs against Stripe):
1. **The installed `stripe-python` version doesn't implement `.get()` on its response objects at all** (raises `AttributeError`, confirmed via Railway logs) — only bracket/index access works. This means the **entire webhook handler had been broken since it was first written**, for every event type, regardless of the nickname fix — `subscription_status`/`subscription_plan` updates from Stripe had very likely never once succeeded in production. Fixed with a small `_sget(obj, key, default)` helper (`server.py`, defined near the webhook) using proven-working bracket access, applied everywhere the webhook and the new status endpoint touch a Stripe SDK object. Separate commit: `89fc0d6`.
2. **Test-mode setup gap** (test-environment only, confirmed the live webhook endpoint was already correctly configured): the freshly-created test webhook endpoint was subscribed to `checkout.session.completed`/`customer.subscription.updated` but not `customer.subscription.created` — meaning Stripe never even attempted delivery for the event our test relied on. Fixed by updating the test endpoint's subscribed events to match what the code actually handles (`customer.subscription.created/updated/deleted`, `invoice.payment_succeeded/failed`). Live webhook (`we_1TpWTXGVgsNSHYytdK2b3RQQ`) checked read-only and already had the correct event list — no live change needed.

School Stripe products (Starter/Standard/Plus) and their Payment Links are still Jono's to create for the manual sales flow — not required for anything currently wired into the app, per the L3 scope decision (self-serve checkout is teacher/parent only).

### L3. Parent pricing model — free with school package, €4.99/mo self-serve otherwise; the self-serve payment path doesn't exist yet *(Section 7 #2, extended)* — ✅ DONE & VERIFIED 2026-08-18
**Files:** `backend/server.py:3546-3567` (`/parent/link-child`), `:3425-3441` (`/family/members`), `:9913-9921` (`/family/custom-strategies`), `:3311-3336` (`/resources`), `frontend/src/utils/api.ts:203-207` (`subscriptionApi.createCheckout`/`getPaymentStatus`), `frontend/app/subscription/index.tsx`, live website pricing copy.

**Ground truth from Jono: parents are free when their school has a package (linked via school invite code); parents at schools without a package subscribe €4.99/mo/family in-app, paid via web/Stripe — never Apple IAP.** (Confirmed: no `react-native-iap`/`expo-in-app-purchases`/StoreKit code exists anywhere in `frontend/` — nothing to remove there, good.)

This requires two things to actually work, and **neither does today**:

**(a) "Free with school package" is only partially wired, and has real gaps.** Four backend gates touch parent-facing limits, but only one of them checks school-package coverage at all:
- `POST /parent/link-child` (`:3546-3567`, the invite-code redemption itself) **does** check school coverage — `parent_covered` includes `bool(user.get("school_admin_id"))`, and it also checks the linked teacher's own `school_admin_id`/subscription via the classroom lookup. This one is close to correct already, modulo the pricing/tier fix in L2 not affecting its logic.
- `POST /family/members` (`:3425-3441`, "2 children" cap) — checks **only** `user.get("subscription_status")`, with **no school-coverage check at all**. A parent linked to a package school who hasn't personally been marked "active" would incorrectly hit this cap.
- `POST /family/custom-strategies` (`:9913-9921`, "6 strategies" cap) — same gap: subscription_status only, no school-coverage exemption.
- `GET /resources` (`:3311-3336`, `is_locked` flag) — same gap: subscription_status only, no school-coverage exemption.

None of these three carry a persistent "this parent's child attends a package school" flag set at link time — they'd need to look up the parent's linked children (`parent_links` → `students` → `classrooms` → teacher's `school_admin_id`) the same way `/parent/link-child` already does, or a `school_covered` flag should be written onto the parent's own user row at link time so subsequent checks are cheap. Either approach works; today none of the three do it at all, so **a parent at a fully-paid package school can still be told to upgrade** for adding a 3rd child, a 7th family strategy, or unlocking a resource.

**(b) The €4.99/mo self-serve payment flow for unlinked parents is completely non-functional today** — this is the launch-blocker inside the launch-blocker. The app's "Subscribe" button (`frontend/app/subscription/index.tsx:80-91`, `handleSubscribe`) calls `subscriptionApi.createCheckout` → `POST /subscription/checkout`, and payment confirmation calls `subscriptionApi.getPaymentStatus` → `GET /subscription/status/{session_id}`. **Neither endpoint exists anywhere in `backend/server.py`** — confirmed by direct grep, zero matches for `checkout` (case-insensitive) in the entire file. There is no `GET /subscription/status` collision either (the one existing route at `server.py:6445` is `/subscription/status`, a different no-path-param endpoint that just returns the caller's own current status — not the session-lookup one the frontend calls). **Right now, tapping "Subscribe" as either a parent or a teacher fails outright.** Live Supabase confirms this isn't theoretical: of 17 parent-role users, 0 have a `stripe_customer_id` set — nobody has ever actually completed a real Stripe payment as a parent. (4 parent rows show `subscription_status: active` with no Stripe customer behind them — almost certainly manually-set demo/test data, not real payments; no refund/cancellation cleanup needed.)

**Fix (in order):**
1. Build `POST /subscription/checkout` (create a Stripe Checkout Session for the given plan, return `{url, session_id}`) and `GET /subscription/status/{session_id}` (look up session status) — these are referenced by working frontend code today and simply don't exist server-side. This is required for the parent €4.99 flow to function at all, and also fixes teacher self-serve checkout, which is equally broken today.
2. Add the missing school-coverage check to `/family/members` (`:3425-3441`), `/family/custom-strategies` (`:9913-9921`), and `/resources` (`:3311-3336`) so a parent linked to a package school is treated as covered consistently everywhere, not just at link time.
3. Update `frontend/app/subscription/index.tsx` copy: the parent plan currently reads unconditionally "from €4.99/mo" (`:174`) with a flat `PARENT_PLANS`/`PARENT_FEATURES` list (`:10-13, 20-29`) that doesn't mention the free-with-school-package condition at all. Update to state the real model (e.g. "Free if your child's school has a Class of Happiness package — otherwise €4.99/mo per family") and skip straight to a "you're covered" state for parents who are already school-linked, rather than showing them a price at all.
4. Update the live website's parent pricing card (currently "EUR 2.99/month… Get started", unconditional, no mention of the free path — confirmed live at the pricing section) to clearly state the same conditional model.
5. **Bonus find, fix alongside this:** `frontend/app/subscription/index.tsx:37` (`TEACHER_FEATURES`) lists "SMS & push alerts" as a teacher plan feature — SMS is not implemented anywhere in the backend (same false claim pattern as the now-fixed website one). Correct to "Push alerts" while this file is being edited.
6. Re-test end-to-end once (1)-(2) land: invite-code redemption → parent sees no paywall anywhere in the app; a genuinely unlinked parent → sees the €4.99/mo prompt → completes real Stripe checkout → `subscription_status` flips to `active` correctly.

**Decided by Jono (2026-08-18):** `/reports/pdf/family/{id}` stays ungated — free for all parents regardless of subscription status, deliberately. It's a caring wellbeing report about their own child, not a premium feature, so it's intentionally excluded from the free/paid parent split described above. No change needed here as part of L3; this also settles A8's open question about this specific route (the school-overview and classroom-overview PDF gating question in A8 is unaffected and still open).

**Pricing correction while executing this:** Jono's actual considered parent price is **€4.99/mo**, not €2.99 as originally stated — confirmed in Stripe (existing "Parent Plan €4.99/mo" price, nickname now set to `parent_monthly`). All code, copy, and this plan doc updated to €4.99 throughout; a repo-wide grep for `2.99`/`2,99` confirms no leftover references anywhere (backend, frontend, or the website upload file) except one intentional historical comment noting the correction.

**What was actually done:**
1. **`_parent_is_school_covered(user)` helper added** (`server.py:3547-3592`), walking `parent_links → students → classrooms → teacher` and requiring the resolved school_admin's own `subscription_status == "active"` — the **stricter** definition Jono chose, closing a real loophole: the old logic only checked `bool(school_admin_id)` (link presence), so any school_admin on a lapsed/`none` plan would have made all their teachers' parents free forever, whether or not the school ever actually paid. Includes the same dual-match fallback (school_name lookup) as L4, since a teacher's own `school_admin_id` can still be unbackfilled.
2. **Applied the stricter coverage check** at all 4 sites: `/parent/link-child`'s existing gate (`server.py:3621-3654`, both the parent-side and the teacher-side checks rewritten to require `active` status, not just presence), `/family/members`'s 2-children cap (`:3446-3452`), `/family/custom-strategies`'s 6-strategies cap (`:10077-10084`), and `/resources`'s `is_locked` flag (`:3336-3343`, scoped to `role == "parent"` only — teacher gating is untouched since teacher pricing didn't change).
3. **Built the two missing endpoints**: `POST /subscription/checkout` (`server.py:6548-6577`, scoped to `teacher_monthly`/`parent_monthly` only per Jono's decision — school stays manual/sales-assisted) and `GET /subscription/status/{session_id}` (`:6579-6598`, returns the Stripe session's own `payment_status` directly rather than waiting on the async webhook, matching exactly what `frontend/app/subscription/success.tsx`'s `result.status === 'paid'` check expects).
4. **Stripe price IDs wired as env vars** (`STRIPE_PRICE_TEACHER_MONTHLY`, `STRIPE_PRICE_PARENT_MONTHLY`, `server.py:10738-10743`) rather than hardcoded, so Jono can set/rotate them without a code deploy — **not yet set on Railway, checkout will 500 with a clear "not configured" error until they are.**
5. **New `GET /parent/coverage-status`** (`server.py:3595-3603`) added — wasn't in the original plan text but is required to support #6 below; returns `{covered: bool}` for the currently logged-in parent.
6. **`frontend/app/subscription/index.tsx` rewritten**: annual billing removed entirely per Jono's decision (was already broken — `parent_annual`/`teacher_annual` had no backend or Stripe support, and the $39.99/$59.99 figures were placeholder USD, not real prices); a real bug fixed where `origin_url` was silently sent as `''` on every native build (`window.location.origin` doesn't exist outside a browser) — now uses `ExpoLinking.createURL('/')`, producing a real `classofhappiness://` deep link so Stripe's redirect can actually reopen the app; parent screen now fetches coverage status on load and shows a "✅ Free — your school has a package" card instead of the paywall when covered; the stray "SMS & push alerts" feature bullet corrected to "Push alerts"; School Package CTA and legal text corrected (was quoting a non-existent "$299/year" school price and "Prices in USD" despite showing EUR figures).
7. **`frontend/src/utils/api.ts`**: added `subscriptionApi.getParentCoverage()`.
8. **Website parent pricing card** (local upload-ready copy at `/Users/jono/Desktop/index-UPLOAD-2026-08-18.html`, freshly fetched live and edited, **not yet uploaded to cPanel**) rewritten to lead with "Free with your school's package" and state the €4.99/mo fallback, across all 6 languages (translations reviewed but not natively verified — recommend a native-speaker check before upload, especially PT/ES/IT/DE/FR). Also fixed a real, separate bug in the school ROI calculator (`calcSavings()`, line 1235) that was still computing the "what you'd pay individually" comparison using the old €2.99 figure — now €4.99, which directly affects the savings number shown to prospective school buyers.

**Deployed and verified live** (2026-08-18): Railway env vars set (`STRIPE_PRICE_TEACHER_MONTHLY`, `STRIPE_PRICE_PARENT_MONTHLY`, plus a temporary `STRIPE_TEST_MODE` toggle + `STRIPE_SECRET_KEY_TEST`/`STRIPE_WEBHOOK_SECRET_TEST` used only for this test pass, since Railway had only ever had a **live** Stripe key configured — testing against real money wasn't an option). Full loop tested against Stripe test mode:
- `POST /subscription/checkout` (teacher_monthly) → real Stripe Checkout Session created successfully.
- `GET /subscription/status/{session_id}` on the unpaid session → correctly returned `{"status":"unpaid","plan":"teacher_monthly"}` (this call originally 500'd — see the `.get()` bug below, found and fixed during this same test pass).
- A real test-mode subscription was created via the Stripe API (bypassing the hosted Checkout page, which can't be driven headlessly) using Stripe's built-in test payment method — this exercises the exact same webhook path a real completed checkout would. Webhook fired, `_find_user` matched the demo account by email and persisted `stripe_customer_id`, and `subscription_status`/`subscription_plan` updated correctly to `active`/`teacher` in Supabase — confirmed both directly in Supabase and via the app-facing `GET /subscription/status`.
- `GET /parent/coverage-status` tested against a real linked parent account — returned `covered: true`, traced end-to-end through `parent_links → students → classrooms → teacher → school_admin` to confirm the logic fired for the right (if slightly tangled, pre-existing demo-data) reason rather than a false positive.
- Test artifacts cleaned up afterward: Stripe test subscription cancelled, the demo teacher account (`jono+teacher@gmail.com`) reverted to its pre-test `subscription_status: trial` baseline, `STRIPE_TEST_MODE` flipped back to `false` (confirmed via a follow-up deploy) so the server is back on the live Stripe key.

**Real bug found only by live-testing, not fixable from static review — see L2 above for full detail**: this stripe-python version doesn't support `.get()` on its response objects; the webhook (and the new status endpoint) had used it throughout. Fixed via a `_sget()` helper (commit `89fc0d6`) — without this, none of the webhook-driven `subscription_status`/`subscription_plan` updates in this whole plan would have worked, in test or live.

**Still pending, not part of this fix:**
- `/Users/jono/Desktop/index-UPLOAD-2026-08-18.html` needs manual upload to cPanel — not automated, no FTP access from here.
- School Stripe products/Payment Links — Jono's to create for the manual sales flow (see L2).

### L4. School identity (school_admin_id vs school_name) — real data loss happening today *(Section 7 #1)* — ✅ DONE 2026-08-18
**File:** `backend/server.py` (every `.eq("school_admin_id"` / `.eq("school_name"` query site)

Queried live Supabase `users` table directly:
- All 4 `school_admin` rows have `school_admin_id = NULL` on their own row (matched to their school purely by `school_name`).
- Of 23 `teacher` rows: **0 have both fields set.** 3 real teachers at "Sunshine International School" have `school_name` set but `school_admin_id = NULL`. 1 has only `school_admin_id`. 19 have neither.
- A **third** school-identity concept exists beyond the two the handover names: a separate `school_profiles` table with its own `school_admin_user_id` column, currently 1 row ("St Lucy's ", trailing space in the stored name), `school_admin_user_id` also NULL. Not resolved by this fix — flagged as-is for a future canonical-school-entity decision.

Any endpoint filtering by `school_admin_id` alone silently drops the 3 real, active Sunshine teachers' data — not theoretical, today's actual data. This is launch-blocking rather than a nice-to-have because it directly undermines L2/L3's school-package coverage logic too: if a school's own teachers/parents aren't reliably matched to that school, "free because your school has a package" can't be trusted to work correctly for exactly the accounts most likely to be affected (newly-linked ones).

**What was actually fixed:**
1. **Root cause re-investigated, turned out different from the original hypothesis.** `/school/join` (`server.py:8280-8323`, the real invite-code redemption flow) already correctly sets *both* `school_admin_id` and `school_name` — it was never broken. The 3 affected teachers (`demo_teacher_001/002/003`, sarah.mitchell/james.okonkwo/maria.santos @sunshine-school.com) are pre-existing seed data created May 30–Jun 9, 2026, *before* any "Sunshine" school_admin account existed (created Jul 28) — they were seeded directly with only a `school_name` string, never run through the join flow. No ongoing linking-flow bug to fix.
2. **Ambiguity found and resolved:** two separate school_admin rows both use `school_name = "Sunshine International School"` — `demo_school_001` (demo@schoolportal.app) and `44bf76b1-c0ee-49d5-bc68-3e47bb5cf8b3` (schooladmindemo@classofhappiness.com, the account documented in COH-HANDOVER.md Section 2 as the real demo login). Backfilled against the documented account per Jono's decision; `demo_school_001` is an unused duplicate seed row, tracked separately in A13 below rather than fixed here.
3. **Backfilled** `school_admin_id = 44bf76b1-c0ee-49d5-bc68-3e47bb5cf8b3` onto all 3 teacher rows via direct Supabase PATCH — confirmed in the update response for each.
4. **Dual-match code fix applied** at the 3 single-match risk sites identified in the original review, using the same pattern already correct elsewhere in the file (merge-and-dedupe by `user_id` across an `school_admin_id` lookup and a `school_name`+`role=="teacher"` lookup):
   - `GET /school-admin/stats` (`server.py:8180-8186`)
   - `GET /school-admin/subscription` (`server.py:8592-8598`)
   - Superadmin "Schools Breakdown" loop (`server.py:6887-6892`)
   - `python3 -m py_compile backend/server.py` confirmed valid syntax after the edit.

**Verified live**, logged in as the real `schooladmindemo@classofhappiness.com` account against production (`https://class-of-happiness-production.up.railway.app`):
- `GET /school-admin/stats` → `total_teachers: 3, total_students: 24` (was silently 0 before the backfill)
- `GET /school-admin/subscription` → `seats_used: 3`, all 3 teachers (Maria Santos, Sarah Mitchell, James Okonkwo) listed by name/email
- `GET /school-admin/analytics` → `total_teachers: 3, total_students: 24`

All three checks ran against the **already-deployed** (pre-dual-match-fix) backend and passed purely from the data backfill — confirming the data-layer fix alone resolves the user-visible symptom immediately. The dual-match code change is deployed as defense-in-depth for any future similarly-orphaned row (e.g. from manual Table Editor edits, which the handover notes has happened before) — re-verify these same 3 endpoints once Railway redeploys, to confirm the new code path doesn't change the (already-correct) result.

**Deferred, not part of this fix:** part (d) of the original fix list — whether `school_profiles` becomes the canonical school entity long-term — remains open. `demo_school_001` cleanup is tracked in A13.

---

# LAUNCH-ACCEPTABLE
*(Real, evidenced problems. Don't block launch, but should be worked through soon after — roughly in the order listed.)*

### A1. Duplicate route registrations — dead code, no live impact *(Section 6 #2)* — ✅ DONE 2026-08-18
**File:** `backend/server.py`

Two paths were registered twice under `api_router`; FastAPI matches in registration order, so the first definition won and the second was unreachable — same bug class as commit `0792701`:
- `POST /auth/promote-admin` — line 2469 (live) vs 7307 (dead).
- `DELETE /admin/teacher-strategies/{strategy_id}` — line 8110 (live) vs 9456 (dead).

**Fix:** delete the dead (second) definition of each pair or merge intentionally; grep for other duplicates after any large edit.

**What was actually done:** re-ran the full-file duplicate-route scan first (`grep` every `@api_router.*` decorator, sorted, counted) to confirm these were still the only two duplicates after all the L1-L4/L2-L3 edits — they were, no new ones introduced.

For each pair, read both full definitions before touching anything, since "merge intentionally" turned out to be the right call in both cases, not a plain delete:
1. **`POST /auth/promote-admin`**: the live version hardcoded `["ADMINCLASS2026", "HAPPYADMIN2026"]` directly; the dead version read the same two codes from the shared `PROMO_CODES` dict (single source of truth, plus `.upper().strip()` input normalization) — confirmed `PROMO_CODES` currently holds exactly those two `"type":"admin"` entries, so accepted codes are unchanged today. Merged the dead version's logic into the live slot (better engineered, no duplication), kept the live version's original response shape (`{"message":..., "role":...}`), then deleted the now-fully-duplicate dead definition.
2. **`DELETE /admin/teacher-strategies/{strategy_id}`**: the live version only allowed `admin`/`superadmin`; the dead version was a genuine capability superset — added `school_admin` with an ownership check (school_admin can only delete their own) and proper try/except error handling. Grepped every frontend/app and portal.html caller of this path first — confirmed `school_admin` never actually calls this endpoint today from either surface (the app's `StrategyManager` and portal both route school_admin to `/school-admin/school-strategies` instead) — so promoting the richer version changes nothing for existing admin/superadmin behavior and simply enables a capability that was clearly intended but permanently unreachable. Merged it into the live slot, kept the original response shape (`{"message": "Strategy deleted"}`), deleted the dead definition.

**Verified:** `python3 -m py_compile server.py` passes; re-ran the duplicate-route scan — zero duplicates remain anywhere in the file; confirmed via grep that no frontend/portal caller inspects the response body of either endpoint (both call sites `await` the request without reading the result), so the response-shape choices above were low-risk either way. Not deployed as part of this change — bundle with the next push.

### A2. Full admin-capability parity map — retire legacy admin.html, close the school_admin app/portal gap *(Section 6 #3)*

**App — `frontend/app/admin/dashboard.tsx`, one shared component for both roles, no `frontend/app/school-admin/` directory exists:**
Superadmin: 6 tabs (Analytics, Strategies, Resources, Schools, Users, Settings). School_admin: strict subset, 4 tabs (Analytics, Strategies, Resources, "School"=Settings). Sharpest asymmetry: superadmin's Settings (`:1246-1253`) is one static "Platform Version" card; school_admin's Settings (`SchoolSettings`, `:843-956`) is a real functional form (school profile, wellbeing-alert email, teacher invite-code generator).

**Portal (`portal100.html`) — superadmin (`buildSuperAdminPortal`, line 415): 6 tabs** (Dashboard, Creatures, Schools, Users, Strategies, Resources).

**Portal — school_admin (`buildSchoolAdminPortal`, line 1423) — 9 tabs, the biggest gap the handover missed:** Dashboard, Resources, Strategies, Users, **Wellbeing Tracker, Our Team, Services** (third-party provider directory), **Creatures, My Wellbeing** — the last four exist *only* in the school_admin portal, zero app equivalent. This is the largest concrete SYNC PRINCIPLE violation found: not a lighter app version, just absent from the app.

**Legacy admin.html (`coh_admin-2.html`, 1,844 lines) — superadmin-only, targets Railway directly:** Login, Dashboard, Resources, Strategies, Users, Alerts, Schools, School Mgmt, Reports. Every capability maps 1:1 to something the portal already covers against the identical backend — **zero unique capabilities** — and it's demonstrably broken: `loadSubscriptionIntents()` (`:876`) is called but never defined (dashboard subscription card permanently stuck loading); School Analytics panel is fully broken (`:1223`, `:643` — references to a DOM element and handler that don't exist in this version); its "Grant Trial" button calls `POST /auth/apply-promo-trial` (`:1184`) — **this endpoint doesn't exist in `server.py`** (only `/auth/promo-code` does).

**Fix:**
1. **Retire legacy admin.html entirely** — zero unique capabilities, three already-broken features, unmaintained since Aug 4. Confirm portal's own Grant Trial / School Analytics equivalents work first, then remove from cPanel. (Also closes A5's XSS finding for this file.) — ✅ DONE 2026-08-18, see below.
2. Decide whether Wellbeing Tracker / Our Team / Services / My Wellbeing stay portal-only by design or need an app equivalent — product call. **Still open.**
3. Build an app-side school_admin "School Profile" settings screen matching the portal; decide what superadmin's app Settings should actually contain. **Still open.**
4. Port `/admin/wellbeing-alerts` into the portal's superadmin Dashboard if still wanted — live endpoint, no surface points at it today. **Resolved as not-needed** — see below, portal deliberately replaced this with the Wellbeing Tracker system instead.

**Item 1 — what was actually done (2026-08-18):** before touching anything, re-verified the live file directly rather than trusting the local scratch copy at face value. The real live URL wasn't literally `admin.html` (that path 404s) — it was `https://classofhappiness.com/coh_admin.html`, confirmed byte-identical to the `coh_admin-2.html` copy used throughout this review, so every finding above (broken Subscription Intents, broken School Analytics, dead Grant Trial endpoint) applied to the live file with no staleness gap. Confirmed zero dependencies before removal: no reference anywhere in the frontend app or marketing site; not served by the backend at all (a static file in cPanel's web root, independent of the FastAPI app — no code change needed); no `.htaccess` or server config reference; COH-HANDOVER.md only lists it as a surface to reconcile, no operational workflow depends on it. Found portal.html's own comments confirming deliberate, already-completed supersession — `// SCHOOL MANAGEMENT (ported from admin.html...)` at `portal100.html:1160`, and critically `// Replaces the old wrong /admin/wellbeing-alerts calls...` at `:1777`, which resolves item 4 above (the one capability the original review flagged as possibly-orphaned turns out to have already been deliberately replaced by the richer Wellbeing Tracker system, with the old endpoint explicitly recognized as wrong for the job). Jono deleted `coh_admin.html` from cPanel; confirmed 404 on the live URL immediately after. Items 2 and 3 remain open product/build decisions, unrelated to the retirement itself.

### A3. Creatures global-approve — ✅ DONE for backend/portal 2026-08-18, live-tested; mobile-parity question flagged separately below *(Section 6 #4)*
**Files:** `frontend/src/components/CreatureManagement.tsx:6-11`, `backend/server.py:10928-10971`

`CreatureManagement.tsx`'s own comment describes a two-step approval (teacher/parent approve locally — works; superadmin global-approve — the real gate before anything goes public). Grepping `frontend/` for `global-approve` returns exactly one hit, that same comment — **no React Native app code calls this endpoint.** Portal's Aug 18 Creatures tab renders an approval queue, but this review didn't confirm its Approve button actually posts to `/creatures/global-approve` with `visibility_scope`.

**Fix:** confirm live whether portal's Approve button calls this endpoint correctly; if not, it's a fully orphaned backend capability needing a UI built.

**What was actually found:** the premise didn't hold — portal already does this correctly, no building needed. Confirmed against the live portal file: `loadSACreatures()` → `GET /creatures/awaiting-global-approval` populates the queue; `renderSACreatureQueue()` renders three distinct approve buttons per item ("Classroom only" / "Whole school" / "Everyone (global)") plus reject; `saGlobalApprove(id, scope)` → `POST /creatures/global-approve/{id}` with `{action:'approve', visibility_scope: scope}`, exactly matching the backend contract; `saGlobalReject(id)` → same endpoint with `{action:'reject', reason}`. Live-tested read-only as real superadmin against production: `GET /creatures/awaiting-global-approval` → `200 OK`, `[]` (nothing currently queued — a normal empty state, not an error). Backend and portal sides are complete, correct, and confirmed working end-to-end.

**Separate open item, not urgent, deliberately not decided now:** the mobile app still has zero equivalent UI for this (confirmed no React Native code anywhere calls `global-approve`). Whether that's a real gap or an intentional portal-only capability (similar to how superadmin's app Settings tab is already thin by design, per the SYNC PRINCIPLE's "app matched at phone-appropriate level") is a product call for Jono, not something to build reflexively. If it does get built, it's a real UI task (queue screen, scope picker) — not a quick follow-up.

### A4. Superadmin period-pill bug — appears ALREADY FIXED, handover is stale *(Section 6 #5)* — ✅ VERIFIED RESOLVED 2026-08-18, no code change
**File:** `portal100.html:544-559`

Live code has an Aug 18-dated comment describing the exact cross-call fix the handover still lists as open. `changeSAStatsPeriod` now calls only `loadSuperAdminData()`.

**Fix:** no code change — live-test to confirm, then strike from the priority list.

**Re-verified against the actual live file** (fetched `https://classofhappiness.com/portal.html` directly and diffed byte-for-byte against the local `portal100.html` scratch copy used throughout this review — identical, so no staleness gap here unlike the marketing site earlier). Confirmed the fix genuinely holds: `changeSAStatsPeriod`/`saStatsPeriod` (superadmin) calls only `loadSuperAdminData()`; `changeSAPeriod`/`currentSAPeriod` (school_admin) calls only `renderOverview()`. No cross-call exists. The duplicate `#saPeriodTabs` DOM id (lines 430, 1582) is confirmed still present but dormant — the two builder functions are mutually exclusive per page load, so it never collides in a live DOM. Nothing to fix; struck from the priority list as the handover's own claim was simply stale.

### A5. [SECURITY] Stored-XSS risk in legacy admin.html; audit portal.html for the same pattern *(Section 6 #7)* — ✅ DONE 2026-08-19, both instances fixed
**Files:** `coh_admin-2.html` (retired, see below); `portal100.html` — `renderTeacherAlerts()` (`:3033-3095`), `renderSchoolUsersList()` (`:1522-1541`), `loadSASchools()` (`:1352-1381`), `displayTracker()` (`:1870-1891`)

Lower-privilege-originated data (school names, task notes, alert messages, teacher/user names) is inserted into a higher-privilege user's DOM via unescaped hand-built-HTML-string `innerHTML` assignment, with no escaping helper defined anywhere in either file.

**Resolved for `coh_admin-2.html`**: moot as of A2 item 1 — the file was deleted from cPanel 2026-08-18 (confirmed 404 on the live URL), so this exact vulnerable code no longer exists anywhere.

**`portal100.html` audit (2026-08-19):** confirmed the identical pattern, live — fetched the hosted file directly (`https://www.classofhappiness.com/portal.html`) and confirmed it's byte-identical to the local working copy before auditing, so findings reflect what's actually live, not a stale draft. No `esc()`/`escapeHtml()`/sanitize helper exists anywhere in the file's ~5,770 lines. Confirmed vulnerable in all 4 areas originally asked about:
- **`renderTeacherAlerts`** (most severe) — `a.message`, a **parent's free-text alert message**, interpolated completely raw; any parent could inject arbitrary HTML/JS into a teacher's alert view. Also `student_name`, `classroom_name`, and `resolveSN(a.strategy_name)` (can resolve to a user-typed custom strategy name) — all unescaped.
- **`renderSchoolUsersList`** ("Users") — `u.name||u.email` and `u.email` (self-set signup fields) raw-interpolated into a school_admin's DOM.
- **`loadSASchools`** ("Schools") — `school_name` and `principal_email` raw-interpolated into a **superadmin's** DOM. While fixing, found the same function also raw-interpolates `city`/`country`/`subscription_package` — all confirmed free-text inputs via `saSaveSchool` (not fixed dropdowns as assumed) — included these in the fix too, beyond the original 2-field scope.
- **`displayTracker`** (Wellbeing Tracker) — `student_ref`, `year_group`, `action`, `delegate`, and free-text `areas` (including a user-typed "Other" field) — lower severity (mostly a school_admin viewing their own input back) but same systemic gap.

Also found and fixed: every `onclick`/`title` attribute built from these same fields only escaped single-quotes for JS-string safety (`name.replace(/'/g,"\\'")`) — never escaped for the surrounding HTML-attribute context, so a `"` or `>` could still break attribute parsing.

**Fix applied:** added one shared `esc()` helper (HTML-entity escape: `& < > " '`) near the top of the file, and wrapped every field listed above with it — including the attribute-context ones, applied *around* the existing JS-quote-escape so both layers are covered. Scope stayed limited to exactly these fields; the file's other 100+ `innerHTML` assignments (static strings, emoji, counts, fixed-dictionary values) were left untouched.

**Verified:** diff reviewed and approved before applying; extracted and validated the full inline script with `node --check` — clean syntax, no errors. Final grep confirmed zero leftover unescaped instances of any flagged field in the 4 functions (one intentionally-untouched `+name+` remains inside a `confirm()` dialog call, which renders plain text only — not an innerHTML/XSS context, correctly out of scope). **Not deployed by this session** — `portal100.html` isn't tracked in git and has no established upload path available here (per A11's standing risk note); written to `/Users/jono/Desktop/portal100-UPLOAD-2026-08-19.html` for Jono to upload to cPanel manually, same process as prior website fixes. Not visually tested in a browser — no environment available here to do so; verified via source-level review, live byte-for-byte diff against the hosted file, and JS syntax validation instead.

### A6. The 'admin' role — was vestigial-but-live-exploitable, plus two more discovered self-serve promotion bugs — ✅ DONE 2026-08-20, live-verified *(Section 6 #6)*
Live Supabase had zero `role='admin'` rows, but `POST /auth/promote-admin` (`server.py:2469`) could still create one. Once set, `admin` = `school_admin`-level in the portal (`portal100.html:408`) but `admin` = `superadmin`-level in legacy admin.html (`coh_admin-2.html:797`) — same role string, two different privilege levels depending which surface reads it.

**2026-08-18 partial fix:** the specific admin.html-vs-portal conflict was resolved as a side effect of A2's admin.html retirement — no longer two surfaces disagreeing. Deliberately left open at the time: whether `admin` was a real stepping-stone tier worth keeping, or dead code to remove.

**2026-08-20 investigation — the deeper problem, requested as "show me what it is, what removing it would mean, quick decision":** the exact same cross-surface inconsistency never actually went away — it just relocated *inside* `server.py` itself. `/admin/verify` treated `admin` as school_admin-tier (`is_super_admin: false`), but ~20 real resource endpoints gated on `role in ("admin", "superadmin")`, treating it as fully equal to superadmin — their own docstrings said things like *"Superadmin can cancel any creature globally"* and *"Returns all users for superadmin"*, yet granted that to `admin` too (`GET /admin/users`, `POST /creatures/cancel/{id}`, `POST /admin/unlink-user`, `PUT /admin/global-strategies/{id}`, and ~16 more). Worse: this role was live self-serve reachable, not just dead code — 4 separate endpoints could grant it: `/auth/promote-admin` and `/auth/promo-code` (via hardcoded promo codes `ADMINCLASS2026`/`HAPPYADMIN2026`, no other auth check), `/subscription/redeem-trial-code` (same codes), and `/auth/fix-role` (a hardcoded-secret endpoint that could set **any** `user_id`'s role, not just the caller's own). Decision: **remove entirely** (Jono) — small, mechanical, zero data migration (zero live rows), nothing in the frontend branched on it.

**What was actually done (round 1, `a44ab78`):** deleted both `PROMO_CODES` admin entries, `/auth/promote-admin`, `/auth/fix-role`, and the admin branches inside `/auth/promo-code` and `/subscription/redeem-trial-code`. Collapsed all ~20 `role in ("admin","superadmin")` checks (plus `/admin/verify`'s own admin branch, added in A19 the same day) to superadmin-only. Left untouched, confirmed harmless: ~38 endpoints where `admin` appeared only alongside `school_admin`/`teacher`/`parent` in a broader allow-list (can no longer match since nothing grants it — stale but inert, not a behavior change), and 2 unrelated `created_by_role` default-value fallbacks (data tagging, not access control).

**Round 2 (`6115562`) — a bigger, adjacent bug found while wiring up the frontend side:** removing the now-dead `authApiExtended.promoteToAdmin` wrapper, found it had a real live caller — `settings.tsx`'s "Admin Access" section, visible to **every logged-in user** regardless of role. That UI's `handlePromoteAdmin` had a client-side hardcoded check for `['JONO_SUPERADMIN_2026', 'CLASS_CREATOR_2026']` — matching either would call `POST /auth/promote-superadmin`, a previously-unaudited endpoint (missed by the original grep, which searched for lowercase `"admin"` — these use different, uppercase code strings and weren't in `PROMO_CODES` at all) granting **full `role="superadmin"`** to any authenticated account, no other check. Same class of bug as L1 (hardcoded superadmin bypass shipped in the app bundle), just not caught in that pass, and more severe than the admin-role issue just fixed. A sibling endpoint, `/auth/promote-school-admin`, had the identical pattern for `role="school_admin"` via 3 more hardcoded codes (`ADMINCLASS2026`/`HAPPYADMIN2026`/`SCHOOLADMIN2026`) with a user-supplied `school_name` and no per-school binding or expiry.

**What was actually done (round 2):** deleted `/auth/promote-superadmin` and `/auth/promote-school-admin` entirely, and the whole "Admin Access" UI block in `settings.tsx` (the expandable code-entry section, both handlers, and the now-dead state: `showAdminCode`, `adminCode`, `promotingAdmin`, `showAdminCodeText`), plus the now-fully-dead `authApiExtended.promoteToAdmin` wrapper in `api.ts`. Confirmed via grep first: no other caller anywhere (frontend, backend, or the local `portal100.html` copy) referenced either endpoint or any of the code strings. Confirmed real school_admin onboarding is unaffected — `POST /admin/create-school-admin` already exists as the legitimate, superadmin-gated path and wasn't touched.

**Verified — both rounds, live against production after each deploy:** real superadmin login + `/admin/verify` + a real superadmin-gated resource endpoint (`GET /admin/stats`) all still work correctly; real school_admin (demo account) login + `/admin/verify` still work correctly; all 6 removed grant paths now correctly return 404 with the exact real codes that used to work (`/auth/promote-admin`, `/auth/fix-role`, `/auth/promote-superadmin`, `/auth/promote-school-admin`) or 400 "invalid code" (`/auth/promo-code`, `/subscription/redeem-trial-code` with the old admin codes); sibling legitimate functionality (real trial promo codes) unaffected; confirmed no account's role was altered by any of the failed probe attempts. `python3 -m py_compile` and `tsc --noEmit` both clean on every touched file — not even pre-existing errors in the two frontend files. Committed and pushed both rounds (`a44ab78`, `6115562`).

### A7. Colour drift — systemic, worse than "app vs. portal," no central fix point *(Section 7 #3/#4)* — ✅ DONE 2026-08-19 (frontend TSX/TS sweep complete)
- 42 files use the old palette (`#4CAF50`/`#F44336`/`#FFC107`) vs. 5 using brand (`#4CAF73`/`#E05252`/`#FFD93D`) across `frontend/src`+`frontend/app`.
- `ZoneButton.tsx:29-58` (`ZONE_CONFIG`) is the nominal shared source but bypassed by 10+ local redeclarations (`admin/dashboard.tsx:14`, `parent/alerts.tsx:11`, `teacher/dashboard.tsx:42`, `kiosk/index.tsx:14`, and more).
- **Three, not two, mutually-inconsistent palettes exist**: old, brand, and a third variant inside `teacher/dashboard.tsx:199,213` (`#43A047`/`#F9A825`/`#E53935`) — one file disagreeing with itself.
- Backend (`server.py` CREATURES model + every PDF generator) and portal (several functions, e.g. `renderTeacherAlerts`, the strategy zone picker) both lean old-palette too, alongside brand-correct sections in the same files.
- Brand yellow `#FFD93D` barely appears live anywhere — app, backend, and portal all default to `#FFC107` instead. This is the design doc being out of sync with reality, not surface drift.
- No central colour-constants file exists anywhere.

**Fix:** one shared colour-constants module (`frontend/src/constants/emotionColours.ts` + matching Python dict), single pass across all drifted files. Recommend canon `#4CAF73`/`#E05252`/`#4A90D9`, and update the design doc's yellow to the real `#FFC107` rather than repainting 45+ files.

**What was actually done (scoped pass, ~30-45 min budget — deliberately not the full sweep):**
1. Created `frontend/src/constants/emotionColours.ts` — `EMOTION_COLOURS` + `EMOTION_LIGHT_COLOURS`, canonising green/red/blue to brand (`#4CAF73`/`#E05252`/`#4A90D9`) and yellow to `#FFC107` (not the design doc's `#FFD93D`, per the reasoning already in this section — matches what real "correct-looking" screens already used).
2. `ZoneButton.tsx` — the nominal shared source — now actually imports from it instead of hardcoding its own copy. `teacher/strategies.tsx` already imported `ZONE_CONFIG` from `ZoneButton`, so it's fixed for free, no edit needed there.
3. `app/index.tsx` (the app's landing screen, seen by every user every session) — fixed directly. This one only imported `ZONE_FACES` (the emoji) from `ZoneButton`, not `ZONE_CONFIG`, so it had its own separate hardcoded colour array that fixing `ZoneButton` alone would **not** have covered — worth knowing for whoever does the remaining files, since "imports from ZoneButton" doesn't always mean "gets the colour fix for free."
4. `teacher/dashboard.tsx` — fixed all three local declarations, including the one the original review flagged as disagreeing with itself (`ZONE_COLORS_MAP`/`ZONE_COLORS_MAP2` used a third palette, `#43A047`/`#F9A825`/`#E53935`, different from `ZONE_COLORS` higher in the same file). Note: this file was being actively edited by Jono concurrently while this fix was applied — confirmed via git diff after the fact that the two sets of changes landed in different parts of the file with no collision, but flagging in case anything looks unexpected on review.
5. `student/rewards.tsx` — fixed the inline zone-colour ternary on the tip banner. Left one unrelated old-palette hex in the same file alone (`pointsEarned` text colour, `#4CAF50`) — that's a generic "success" accent colour, not zone logic, out of scope for this fix.

**Verified:** all 4 diffs reviewed in full before applying, isolated and clean; `tsc --noEmit` shows only pre-existing errors, none within dozens of lines of any edit made. Committed and pushed (`730f500`).

**Second pass (2026-08-19) — the remaining files, completed:**
Went through every file with a genuine `ZONE_COLORS`/`ZONE_COLOR`/`ZONE_CONFIG`/`ZONE_COLORS_MAP` zone-to-hex mapping (re-confirmed via a `ZONE_COLOR|zoneColors|ZONE_CONFIG` construct-name grep, not the earlier raw-hex grep, since raw hex matches were mostly incidental UI accents unrelated to zone logic — delete buttons, success ticks, alert badges, etc. — and correctly left untouched). Same two-part pattern applied to each: import `EMOTION_COLOURS` (and `EMOTION_LIGHT_COLOURS` where a file also had a matching light-tint dict) from the appropriate relative path, then replace the local hardcoded declaration with a reference to the shared constant. Files fixed: `parent/linked-child/[id].tsx` (`ZONE_COLORS`, `ZONE_CONFIG`, an inline ternary, a filter-chip array, and a fallback literal), `admin/dashboard.tsx` (`ZONE_COLORS` plus a separately-inlined zone check-in-count block that was using a **third**, previously-uncatalogued yellow variant, `#E0A800` — corrected to the canon `#FFC107`, the one real visual micro-change in this pass), `parent/alerts.tsx`, `parent/family-member-stats/[id].tsx`, `parent/widget.tsx`, `kiosk/index.tsx`, `teacher/alerts.tsx`, `teacher/classrooms.tsx`, `teacher/bulk-checkin.tsx` (keyed `b`/`g`/`y`/`r` rather than full zone names, so mapped per-key instead of a direct assignment), `teacher/checkin.tsx` (`ZONE_COLORS`, a `ZONES` array, and a separate inline dict further down the file), `teacher/student-detail.tsx` (`ZONE_COLORS`, two duplicate `ZONE_COLORS_MAP` inline declarations, and an inline zone-from-strategy-id ternary), `teacher/widget.tsx`, `src/components/CreatureManagement.tsx`, `src/components/FeelingsButton.tsx` (both `color` and `lightColor` fields — `lightColor` matched `EMOTION_LIGHT_COLOURS` exactly).

**Deliberately still not touched — flagged as separate findings, not silently changed:**
- `student/strategies.tsx` — uses a wholly different pastel palette (`#5DADE2`/`#58D68D`/`#F4D03F`/`#EC7063`), not part of the old/brand drift this fix targets.
- `src/components/CreatureCollection.tsx` and `src/components/CreatureShowcase.tsx` — both use a matching but distinct pastel palette (`#4FC3F7`/`#81C784`/`#FFD54F`/`#FF7043`, with its own light-tint set), same reasoning.

Changing either of these would be an unreviewed visual change to screens not part of the app/portal drift this item was scoped to fix — worth a deliberate design decision later, not a mechanical sweep.

**Still out of scope for this item** (frontend-only per the original scoping): the backend Python dict (CREATURES model + PDF generators in `server.py` still hardcode the old palette), and `portal100.html`'s own drifted functions (`renderTeacherAlerts`, the strategy zone picker, the school-sharing button — see Section 6 #8 in the original review).

**Verified:** every genuine `ZONE_COLOR*`/`zoneColors`/`ZONE_CONFIG` construct across `frontend/app` and `frontend/src` traced back to `EMOTION_COLOURS` via a final grep pass (excluding the 3 intentionally-skipped files above). `tsc --noEmit` run in 2 batches covering all edited files — all reported errors are pre-existing and unrelated to the colour changes (confirmed none reference `ZONE_COLORS`/`EMOTION_COLOURS` or fall on edited lines).

### A8. Freemium/gating consistency — PDF endpoints *(Section 7 #5)*
5 PDF routes exist; only student (`:4240`) and teacher-wellbeing (`:4917`) enforce download caps. Family, school-overview, and classroom-overview PDFs have zero cap gating (verified with a 120-line post-route grep window).

**Fix:** extend the existing cap pattern to the 3 ungated routes, or confirm deliberately that they're meant to stay uncapped. Revisit alongside L3's open question about whether family PDFs should gate on the parent's new coverage status.

### A9. Three strategy systems — ✅ DONE 2026-08-18 for teacher/parent content, live-tested *(Section 7 #2 old / now #6)*
Live Supabase `admin_teacher_strategies`: 39 rows, 22 student/9 teacher/8 parent. `GET /strategies` (the real check-in consumer) only reads `helpers`+`custom_helpers`, never this table. **The 17 teacher/parent rows are real, editable superadmin content, structurally unreachable by any teacher or parent today.**

**Fix:** build the missing teacher/parent-facing consumer screens, or explicitly stop presenting this content as if it reaches users.

**Correction before fixing:** the original framing ("structurally unreachable") turned out to be half-true. Deeper investigation found the real student flow doesn't even use `GET /strategies` — the app calls `GET /helpers` directly, whose defaults are hardcoded Python dicts (`DEFAULT_HELPERS` + per-language translations), not a Supabase table at all; `helpers`/`custom_helpers` only feed the less-used `/strategies` alias. More importantly: **teacher/checkin.tsx already had a half-finished integration** — it fetched `GET /admin/teacher-strategies` into an `adminStrategies` state that genuinely was merged into the rendered picker (`strategiesForZone`, merged with a hardcoded `TEACHER_STRATEGIES` list by zone, deduped by name) — so teacher-tagged content *was* already reaching teachers. The real bug: the fetch never passed `strategy_type`, so it silently pulled back **all 39 rows** — student- and parent-tagged content could appear in a teacher's own wellbeing check-in alongside the real teacher content, whenever it shared a zone and didn't collide by name. **Parent side (`parent/checkin.tsx`) had no integration attempt at all** — a separate hardcoded `PARENT_STRATEGIES` list existed, but nothing ever fetched from `admin_teacher_strategies`.

**What was actually done:**
1. `frontend/app/teacher/checkin.tsx` — added `?strategy_type=teacher` to the existing `loadAdminStrategies()` fetch. One-line fix; no backend change needed, the endpoint already supported the filter (`server.py:8237-8247`), nothing in the app was passing it.
2. `frontend/app/parent/checkin.tsx` — built the missing fetch from scratch, mirroring teacher's now-correct pattern: fetches `GET /admin/teacher-strategies?strategy_type=parent`, filters by zone client-side (the endpoint doesn't filter by zone), dedupes by name against both the hardcoded `PARENT_STRATEGIES` and the existing `/family/custom-strategies` fetch, merges all three sources.
3. Both diffs confirmed isolated and clean (`git diff` reviewed in full before applying); `tsc --noEmit` shows only pre-existing, unrelated errors in both files.
4. **Live-tested end-to-end** against production using real teacher and parent account logins (not just superadmin): `GET /admin/teacher-strategies?strategy_type=teacher` → exactly the 9 real teacher rows, no student/parent bleed-through; `?strategy_type=parent` → exactly the 8 real parent rows. Confirmed with both a superadmin token and the actual teacher/parent account types that call this in production.
5. **Not verified:** the actual on-screen render in the Expo app — no simulator/device environment available in this session to visually confirm. Verified the data layer (backend contract) and the code (clean diff, type-safe, mirrors an already-proven merge pattern) instead.

**Student-tagged rows (22) and the third-tier `school_strategies`/fork-on-touch system remain out of scope for this fix** — those were already reaching users via other paths per the original review; only the confirmed-orphaned teacher/parent content was addressed here.

### A10. Website claims — mostly already fixed live *(Section 7 #6)*
Checked live via `curl`: the stale local copy's "Now on iOS & Android" claim is already corrected live to "Now in Beta — iOS & Android" (honest); the stale copy's raw "SMS alerts" fallback text is already corrected live to "Wellbeing alerts" (only a harmless invisible HTML comment remains at line 550). No fix needed for either. The school pricing mismatch originally flagged here is now tracked as L2/L3 above (and reclassified: backend was wrong, not the site).

**Fix:** optional — remove the stale `<!-- SMS alerts -->` comment for cleanliness. Nothing else required.

### A11. Portal.html fragility — standing risk, no quick fix *(Section 7 #7)*
5,770-line hand-edited file, string-concatenated HTML, inconsistent escaping styles (cosmetic, not currently broken). The file's own version comment documents a prior incident where one missing brace broke the *entire* portal (60+ nested functions, blank/unclickable tabs) — real evidence of fragility. No build step, no linting, no local diff against cPanel.

**Fix:** not urgent, but plan a longer-term move to versioned `.js` files so syntax errors are catchable pre-upload.

### A12. PIN/auth model — handover's description was wrong; code is fine *(Section 7 #8)*
Verified `server.py:7321-7353`: PIN required for the 4 `ALWAYS_OPEN_PINS` demo accounts and for `admin`/`superadmin` roles via `ADMIN_PIN`; everyone else uses password/OAuth, no PIN. Opposite of what the handover claimed, but the actual behavior is coherent and needs no code change.

**Fix:** correct the handover doc only.

### A13. Cleanup, no dependencies — ✅ DONE 2026-08-20 (the two safe-to-delete items); analytics query-quality item still open
- `backend/server_backup.py`/`server_old.py` — ✅ DELETED. Re-verified fresh before touching, per Jono's "don't assume the old context is still accurate": both still exactly 6,548 lines, `git show HEAD:...` confirmed they were genuinely tracked and unchanged since the first commit (a mid-investigation `git ls-files` call briefly suggested otherwise due to a cwd mixup — corrected before acting, not before verifying), zero references anywhere across `.py`/`.json`/`.toml`/`Procfile`/`.yml` in the whole repo, and `Procfile`/`railway.toml` both explicitly start `server:app`, not either backup file. Removed via `git rm`; `server.py` confirmed still compiles clean afterward.
- `/creatures/analytics` (`server.py:10891-10933`) — correctly superadmin-gated; N+1 query pattern building `top_users`/`top_schools`, and groups schools by raw `school_name` string (fragile to spelling/casing variants, e.g. the "St Lucy's " trailing-space example already in `school_profiles`) rather than a canonical school ID. Code-quality, not correctness. **Still open** — not part of this cleanup pass.
- **`demo_school_001`** (email `demo@schoolportal.app`) — ✅ DELETED. Re-verified fresh: still existed, still "Sunshine International School," still `school_admin_id: null`. One correction to the original note — it was created **21 minutes *after*** the real demo account (`16:10:13` vs `15:49:06`), not before as originally recorded; doesn't change the conclusion, just the direction. Confirmed exhaustively orphaned before deleting — zero references across `users.school_admin_id`, `admin_settings`, `invite_codes`, `classrooms.user_id`, `school_wellbeing_tracker`, `school_team_roles`, `school_services_directory` (and `school_profiles` has no `school_admin_id` column at all, checked). Re-confirmed the 3 real Sunshine teachers (`demo_teacher_001/002/003`) are correctly linked to the real account, not this one. Deleted via direct REST call; confirmed gone with a follow-up read.

### A14. Crash on parent "My Wellbeing" screen — `.map(resolveName)` argument leak — ✅ DONE 2026-08-19, live-tested via real device
**File:** `frontend/app/parent/my-wellbeing.tsx:390,597`

Found by Jono real-device-testing tonight's A9 fix (not part of the original review — surfaced during verification). `resolveName(id, customNames?, t?)` was passed directly as an `Array.prototype.map` callback in two places. `map` always calls its callback as `(element, index, array)` — three arguments — so the array itself landed in `resolveName`'s third parameter (`t`). A non-empty array is truthy, so `if (t)` passed, then `t('strat_' + id)` tried to call the array as a function, throwing `"t is not a function (it is an object)"` — exactly the crash reported, at exactly the reported line. Confirmed unrelated to tonight's A9 fix itself (this file wasn't touched by it) — the other `resolveName` definitions elsewhere (`parent/alerts.tsx`, `parent/linked-child/[id].tsx`) only take a single `id` param, so the same `.map(resolveName)` pattern is harmless there.

**Fix:** wrapped both call sites in an arrow so only `id` gets passed (`.map((id: string) => resolveName(id))`), matching how every other caller already uses it.

**Verified:** diff reviewed in full before applying (isolated to exactly the two call sites); `tsc --noEmit` shows only a pre-existing, unrelated error in this file. No RN simulator/device available in this session to visually run the app (Playwright's bundled Chromium doesn't support this host's macOS version) — instead reproduced the exact error and confirmed the fix with a plain Node.js repro of the isolated `.map()` logic: buggy version throws `"t is not a function"` verbatim, fixed version resolves cleanly. Confirmed by Jono on a real device afterward.

### A15. "Most used strategies" showing raw UUIDs instead of names — ✅ DONE 2026-08-19, live-tested
**File:** `frontend/app/parent/my-wellbeing.tsx:73-96` (`fetchStrategyNames`)

Also found by Jono real-device-testing tonight. Traced via real production data (`family_zone_logs`, the actual table parent self-check-ins write to): the account's most recent entry (2026-08-18, i.e. that same testing session) had `strategies_selected: ["410a7a20-...", "074eee48-...", "1617e2c0-..."]` — real Supabase UUIDs. `074eee48...`/`1617e2c0...` resolved to `admin_teacher_strategies` rows "Routine check"/"Calm the environment" — parent-tagged content that only became selectable *tonight*, via the A9 fix. `resolveName`'s name-lookup chain (translation key → `strategyNames` state → hardcoded `STRATEGY_NAMES` dict → raw-id cleanup fallback) had no path to these: `strategyNames` is built from `GET /strategies`, which only ever queries the `helpers`/`custom_helpers` tables — it has never queried `admin_teacher_strategies` at all. So every parent strategy surfaced by tonight's A9 fix was guaranteed to display as a raw UUID here, by construction, until this fix.

(The third UUID, `410a7a20...`, resolved to a personal custom strategy ("Water Garden") that — confirmed live — `GET /strategies?zone=yellow` already returns correctly; that case was most likely just a casualty of the A14 crash preventing the section from ever finishing its render, not a separate name-resolution gap.)

**Fix:** extended `fetchStrategyNames()` to also fetch `GET /admin/teacher-strategies?strategy_type=parent` (same endpoint/pattern as the A9 fix) and merge those `id → name` pairs into `strategyNames` alongside the existing ones.

**Verified:** diff reviewed before applying (isolated addition, no changes to existing fetch logic); `tsc --noEmit` shows only the same pre-existing unrelated error as A14. Live-tested directly against production: called the new endpoint with a real parent token and confirmed both real problem UUIDs from tonight's actual check-in history (`074eee48...`, `1617e2c0...`) now resolve to their correct names ("Routine check", "Calm the environment") instead of the raw id.

### A16. Creature submission code accepted no real validation until after photo upload — ✅ DONE & LIVE-VERIFIED 2026-08-20
**Files:** `backend/server.py` (new `GET /creatures/validate-code/{code}`), `frontend/app/student/submit-creature.tsx`

Found and reported by Jono, unrelated to the original review. The code-entry screen's "Continue" button was gated only by a client-side `code.length < 6` check — no backend call at all — and even that threshold was wrong (real codes are 8 characters, generated by `generate_submission_code(length=8)`). The actual code check (existence, `used_count >= max_uses`, expiry, all against the `submission_codes` table) only ran at the very last step, inside `POST /creatures/submit` — meaning a student could complete the tutorial, fill in name/description/emotion, and upload all 4 stage photos (real network/bandwidth cost) before ever finding out the code was bad, with a fairly technical error message.

**Fix:** added `GET /creatures/validate-code/{code}`, reusing the exact same validation `/creatures/submit` already does, without creating a submission. The code-entry screen now calls this before advancing past the code step, showing the real reason ("Invalid code" / "Code has expired" / "Code has been used too many times") immediately. Also corrected the length gate to 8 characters to match real codes.

**Verified:** diff reviewed before applying (isolated to the code-entry step and the new endpoint); `python3 -m py_compile` and `tsc --noEmit` both pass clean, no errors at all in either changed file (not even pre-existing ones). Committed and pushed (`cab54ea`). Live verification was blocked the night this was written by a Railway platform-wide incident (deploys stuck in "Sleeping · Queued") — **re-verified live 2026-08-20** once Railway recovered: confirmed the deploy picked up the change (fresh deployment ID), then tested both cases directly against production — `GET /creatures/validate-code/ZZZZZZZZ` (garbage) → `{"valid":false,"reason":"Invalid code"}`; generated a real code via `POST /creatures/generate-code` as a real teacher account (`GAKAT22J`) and validated it → `{"valid":true}`. Both paths behave exactly as designed.

### A17. Admin PIN entry fails after a fast password→unlock sequence — race condition, exposed (not caused) by today's L1 fix — ✅ DONE 2026-08-20, verified against the real backend contract
**File:** `frontend/app/admin/dashboard.tsx`

Found and reported by Jono, unrelated to the original review — surfaced while testing today's L1 security fix. `AdminDashboard`'s `authToken` starts `null` and is only populated asynchronously:
```js
const [authToken, setAuthToken] = useState<string|null>(null);
useEffect(() => {
  AsyncStorage.getItem('session_token').then(t => setAuthToken(t));
}, []);
```
Neither the PIN input nor the "Unlock" button is gated on this load finishing. If `unlock()` fires before the `AsyncStorage` read resolves, `apiCall('/admin/verify', null, ...)` sends the request with **no Authorization header at all** (`apiCall` only sets it `if (token)` — a falsy token means the header is simply omitted, not sent as a literal "Bearer null"). The backend's `get_current_user` then finds nothing, `/admin/verify` returns `401`, and `unlock()`'s `catch` block fires.

**This is why it's connected to L1, without L1 having caused it:** before today, that `catch` block contained the hardcoded bypass L1 removed — so this exact race condition was already happening, just silently papered over by insecure auto-grant. Today's fix (correctly) replaced that bypass with a real error message and a real denial — which means this pre-existing timing bug is now visible as a hard failure for the first time, instead of a silent (if insecure) success. `settings.tsx` has a "Set Password" flow immediately followed by the "Admin Dashboard" entry point further down the same screen — setting a password then quickly tapping into Admin Dashboard and typing a PIN is exactly the kind of fast sequence that could beat the async token load, matching "PIN entry not working after password entry" precisely.

**Proposed fix:** don't let `unlock()` proceed (or show a brief loading state) until `authToken` has actually finished loading — e.g. gate the Unlock button on an `authTokenLoaded` flag set once the `AsyncStorage` read resolves, or have `unlock()` read the token synchronously/fresh right before calling `/admin/verify` instead of relying on component state that might not have settled. Since this touches the auth-unlock path L1 just secured, re-verify the full L1 fix (real login → real PIN → unlock) alongside this fix, not just the race condition in isolation, before considering it done.

**What was actually done (2026-08-20):** took the second option — `unlock()` now reads the session token fresh via `AsyncStorage.getItem('session_token')` at the exact moment of the call, instead of trusting the `authToken` component state, which removes the race at its source rather than adding a loading gate around it. `authToken` state is left untouched everywhere else in the file (its other uses only run after `unlocked` is true, well past any race window). Diff is a single isolated change to `unlock()`; `tsc --noEmit` shows the same 3 pre-existing errors as always in this file, none near the edit.

**Verified against the real backend contract** (no RN simulator/device available in this environment to literally reproduce the fast-tap race in a running app, so verified what each code path actually depends on instead) — all 4 distinct outcomes confirmed live against production:
- Real superadmin token → `{"valid":true,"is_super_admin":true}` → unlocks as superadmin.
- **No token at all** (the exact race-condition scenario) → `401 Not authenticated` → frontend's `catch` block → "Could not verify" shown, not a false unlock. This is what re-verifying L1 alongside this fix was for: confirms the security fix and this race-condition fix are both correctly in effect together.
- Real school_admin token → `{"valid":true,"is_super_admin":false}` → unlocks at school_admin level.
- Real non-admin-role token (tested with a teacher account) → `{"valid":false}` → "Invalid code" shown — correctly distinct from the network-error message, not conflated with it.

**Side-finding, not part of this fix:** `/admin/verify` never actually inspects the `adminCode` value the user types — access is purely role-gated server-side (`role == "superadmin"` / `role in ("school_admin","admin")`). The PIN field is effectively decorative today as long as the logged-in account already has the correct role. Not a new vulnerability (still gated by real authentication + role), just an unused input — flagged for awareness, not treated as a bug to fix here.

---

### A19. [PRIORITY — ADMIN ACCESS] Admin unlock skipped PIN entry entirely — root-caused, fixed, and live-verified — ✅ DONE 2026-08-20
**File:** `backend/server.py` (`/admin/verify`)

**Reported by Jono, real device:** logged in as `jono@classofhappiness.com` (the superadmin demo account) and pressed "Unlock" on the admin PIN screen — it went straight through to the admin dashboard with **no PIN code typed at all**.

**Root cause, fully traced — A17 is not broken; theory 2 confirmed, theory 1 ruled out.**
1. **`ALWAYS_OPEN_PINS` (theory 1) is a dead end for this account.** Live Supabase check confirmed `jono@classofhappiness.com`'s `role` is `superadmin`. `/auth/email-login`'s PIN check (`server.py:7511-7519`) is an `if role in ("admin","superadmin") ... elif email in ALWAYS_OPEN_PINS`, so the role branch matches first and `ALWAYS_OPEN_PINS` never fires for this account — it's only reachable for non-admin roles (e.g. the demo `school_admin`/parent/teacher accounts). Confirmed live: `ADMIN_PIN` (checked via `railway variables`) is `COH2026JONO` — the actual PIN required and typed at login was real.
2. **A17's own side-finding (theory 2) is the exact and complete cause.** `/admin/verify` (added `7627c0d`, "Real fix Aug 14") never read `code` from the request body at all — it granted `valid:true` purely from `get_current_user(request).role`, by deliberate design ("since the caller already has a real session token... no separate PIN needed"), predating A17 by a week. Frontend's `unlock()` sends `{code: adminCode}` with no non-empty check before calling it. Session tokens live 30 days in `AsyncStorage` (`server.py:7548`). Net effect: once logged in (with a real PIN, at login), the in-app "Unlock" screen — which visually implies a second PIN checkpoint — accepted anything, including empty, for up to 30 days per session, because the code was never inspected server-side.

**A17 confirmed NOT at fault** — its token-freshness fix is unrelated and still correct; re-verified alongside this fix (scenario 5 below).

**Decision (Jono): Option A — make the PIN a real second factor again, additive on top of the existing session+role check, not a replacement for it.**

**What was actually done:**
`/admin/verify` now requires the role check to pass (unchanged) **and** a matching PIN:
- `superadmin`/`admin` roles → compared against `ADMIN_PIN` env var (same value already required at login — no new secret).
- Accounts whose email is in `ALWAYS_OPEN_PINS` (covers the demo `school_admin`, `schooladmindemo@classofhappiness.com`) → compared against that account's own fixed value.
- **Real (non-demo) `school_admin` accounts intentionally left on the role-only check — Path 1, deliberate, not an oversight.** Live-checked who this actually affects: `kairos@classofhappiness.com` and `send@kairosmontessori.com` (a real school, Kairos Montessori) plus the orphaned `demo_school_001` (A13) have zero PIN infrastructure anywhere today — neither at login nor here. Inventing a shared/per-school PIN for them now was explicitly scoped out by Jono as a separate future feature (how a school admin would learn the PIN, reset flow, etc. — real design questions, not bundled into this additive fix).
- Frontend required **zero changes** — `unlock()` already had `if (d.valid) {...} else Alert.alert('Invalid code')`; the gap was entirely that the backend always returned `true`.

**Verified — full matrix run live against production, after deploy:**
1. Superadmin, correct PIN (`COH2026JONO`) → `{"valid":true,"is_super_admin":true}` ✅
2. Superadmin, **empty PIN — the exact original repro** → `{"valid":false,"is_super_admin":false}` ✅ (previously this returned `true`)
3. Superadmin, wrong PIN → `{"valid":false}` ✅
4. No token at all (A17's own race-condition scenario, re-confirmed still correctly denied) → `401 Not authenticated` ✅
5. Demo school_admin, correct PIN (`COH2026DEMO`) → `{"valid":true,"is_super_admin":false}` ✅
6. Demo school_admin, empty PIN → `{"valid":false}` ✅
7. Real (non-demo) school_admin (`kairos@classofhappiness.com`), empty PIN → `{"valid":true,"is_super_admin":false}` ✅ — confirms Path 1 genuinely unchanged, no regression for real customers.
8. Real school_admin, arbitrary random code → still `{"valid":true}` ✅ — same reasoning, role-only as designed.
9. Unrelated role (teacher) → `{"valid":false}` ✅ — unaffected by this change, as before.

All 9 scenarios matched expectations exactly. Diff reviewed and approved before applying (shown in full, no changes to session handling, A17's fix, or the general auth flow — purely additive). `python3 -m py_compile` clean. Deployed via `git push` (`a3a2539`), confirmed live via a fresh Railway deployment ID before testing. **Not yet visually confirmed on-device** — no simulator available in this environment; verified against the real backend contract instead, same methodology as A17. Frontend code required no changes, so no new client-side risk was introduced.

---

### A18. "Today" range pill on parent "My Wellbeing" showed a mislabeled 7-day count — root-caused, fixed, and live-verified — ✅ DONE 2026-08-19
**File:** `frontend/app/parent/my-wellbeing.tsx` (`loadData`)

**Reported by Jono, real device:** family/parent screen showed "3 check-ins today" for the family member Madalena when only 1 real check-in actually happened. Turned out not to be the family dashboard (`parent/dashboard.tsx`) at all — traced to the per-member "My Wellbeing" screen (`my-wellbeing.tsx`), reached via each adult family member's "Wellbeing" button.

**Scope check done first:** verified `parent/dashboard.tsx`'s own multi-source check-in fetch (`fetchMemberData`, covers `relationship==='child'` family members + school-linked children) separately — its 3 backend calls (`GET /zone-logs/student/{id}`, `GET /family/zone-logs/{id}`, `GET /parent/linked-child/{id}/all-checkins`) all correctly declare and honor a `days: int` query param matching what the frontend sends. **That path is unaffected** — this bug is isolated to the one `my-wellbeing.tsx` screen. (Also ruled out: Madalena is `relationship: 'partner'`, not `'child'`, so she was never in `fetchMemberData`'s scope in the first place — this initially misdirected the investigation toward the wrong file before the real screen was found.)

**Root cause: a query-parameter contract mismatch, not a fan-out or duplicate-data bug.** `loadData()` sent `?start=<ISO>&end=<ISO>` to `GET /family/members/{member_id}/checkins`, computed from whichever range pill (Today/7/14/30 days) was selected. The backend endpoint (`server.py:9690-9691`) only ever declared `days: int = 7` — it never read `start`/`end` at all, and FastAPI silently drops query params that don't match a declared parameter. Every range pill therefore always returned the same hardcoded last-7-days window, mislabeled with whatever pill happened to be selected. Confirmed live before fixing: hit the endpoint with the app's exact "Today" request (a real 24-hour window) and it still returned 3 rows — 1 from today plus 2 from exactly 7 days earlier, which is precisely what Jono saw.

**Fix:** `loadData()` already computed `const days = parseInt(range)` locally one line above the fetch — it just wasn't being sent. Changed the request to `?days=${days}`, matching the `days: int` convention every other check-in endpoint in this codebase already uses. Confirmed via grep this is the *only* caller of this endpoint sending `start`/`end` — the endpoint's other two callers (`linked-child/[id].tsx`, `family-member-stats/[id].tsx`) already correctly sent `days=`, so this was an isolated outlier, not a wider pattern. No backend change needed — the endpoint already correctly implemented `days`, it just never received it.

**Verified:** diff reviewed before applying (4-line change, isolated to the one fetch call, no other logic touched); `tsc --noEmit` shows only the same pre-existing, unrelated `useWindowDimensions` error already present in this file all session. This is a frontend-only change — no backend deploy needed. Live-tested all 4 range pills against production with the real fixed request shape: `days=1` → 1 (correct, was 3), `days=7` → 3, `days=14` → 4, `days=30` → 4 — each now genuinely distinct and matching the real underlying data, where before every pill silently returned the same number. Committed and pushed (`b78d20d`).

---

# APP STORE / PLAY STORE READINESS
*(Audit only, 2026-08-19 — TestFlight targeted this weekend, App Store/Play Store after TestFlight feedback. Nothing fixed yet, per Jono's instruction — this is the findings + decision list.)*

### S1. Account deletion — ✅ DONE 2026-08-20, built and safety-tested; blocked on one manual migration
Originally found missing (no backend endpoint, no UI, thorough grep across `server.py`/app/portal — zero real hits), then **re-verified from scratch a second time** after Jono flagged a contradictory claim from earlier the same night that it "fully exists" — re-confirmed exhaustively (every one of the 18 real `DELETE`-verb routes checked individually, none touch `users`; broad name/text sweeps across backend, frontend, and portal — zero matches anywhere). The earlier "fully exists" claim was wrong; likely confused with the real-but-different "unlink" feature (severs a relationship, not an account).

**Why this blocks submission, not just "nice to have":** Apple's App Store Review Guideline 5.1.1(v) (enforced since June 2022) requires any app that supports account creation to also offer in-app account deletion — an email-to-support path does not satisfy it. Google Play has an equivalent Account Deletion requirement.

**Decisions (Jono):**
1. **Teacher:** personal data only (login/name/email/push_token). Classrooms/students reassigned to their linked school_admin immediately at request time. Blocked with a contact-support message if no school_admin is linked.
2. **School_admin:** blocked entirely for now — real support-assisted process only, no cascade logic.
3. **Soft-delete, 30-day grace period**, matching the Privacy Policy's own retention language. Immediate session revocation + login block on request; a scheduled purge job does the actual hard-delete after 30 days. Requires re-authentication (password or fresh Google re-auth) before the request is accepted.

**What was actually built:** `POST /account/delete-request` (role gate → re-auth check → **writes `deletion_requested_at` first**, before any other mutation — deliberate ordering so a missing-column failure, see below, can't leave a partial state where classrooms got reassigned or a subscription got cancelled but the account was never actually marked → teacher classroom/student reassignment → Stripe subscription cancellation, found and closed a real adjacent gap since nothing in this app previously cancelled a subscription on account closure at all → session revocation). `POST /admin/process-account-deletions` (superadmin-gated purge job, same "call this via a scheduled job" pattern as the existing `/creatures/notify-expiry` endpoint — needs an external cron/trigger, this app has no internal scheduler). Login blocks added to both `/auth/email-login` and `/auth/google` for accounts with a pending deletion. Frontend: a real "Danger Zone" section in `settings.tsx`, teacher/parent only (school_admin/superadmin see a contact-support message instead of a non-functional button), reusing the same Google Sign-In client IDs already used for login.

**Real infrastructure blocker found and worked around, not hidden:** this environment has REST API access only (`SUPABASE_URL`/`SUPABASE_SERVICE_KEY`) — no direct Postgres connection, no SQL-exec RPC function (checked — none exist), so **no way to run schema changes (DDL) from code**. The feature needs one new column:
```sql
ALTER TABLE users ADD COLUMN deletion_requested_at timestamptz;
```
**Jono needs to run this once in the Supabase SQL Editor** — small, purely additive, doesn't touch existing data. Until then, the endpoint fails safely (see ordering above) rather than partially.

**Verified live, deliberately limited to zero-side-effect paths given how destructive this feature is** — did not trigger a real deletion against any account without explicit go-ahead: school_admin correctly blocked (403, no write attempted — role check happens before any mutation), superadmin correctly blocked (403), no-auth correctly rejected (401), and a real parent account given a deliberately wrong password correctly rejected re-authentication (403) with **zero side effects confirmed** — the same account logged in normally immediately afterward. The actual success path (correct re-auth → real soft-delete) has not been tested end-to-end against any account — that's a deliberate choice, not an oversight, and needs either the migration confirmed + a designated test account, or Jono running it himself. `tsc --noEmit`: zero new errors (same 52 pre-existing baseline as all session). `python3 -m py_compile`: clean. Committed and pushed (`2a534ee`).

**Known gaps, not covered by this pass:** no way to cancel a pending deletion within the 30 days except contacting support directly (no self-service "undo," no email confirmation link — would need real email-sending infrastructure this app doesn't have); teacher-owned artifacts beyond the literal "personal data" scope (resources they uploaded, `admin_teacher_strategies` they created, their own `teacher_checkins` self-check-in history) are untouched by either the request or purge flow — deliberately not decided on, flagging rather than guessing.

### S2. Privacy Policy / Terms of Service — ✅ both real and substantive; accuracy fixes drafted, not yet applied
**Correction to the initial audit pass:** Jono's message described "Terms of Service missing entirely." Re-verified live directly in response — `classofhappiness.com/terms.html` returns `200`, titled "Terms of Service — Class of Happiness," and is a genuinely complete 13-section document (who can use the service, accounts, subscriptions/Stripe, acceptable use, liability, safeguarding, governing law, contact). Not missing, not thin. Flagging this discrepancy rather than silently recording either version as fact — possibly Jono meant something else (e.g. a distinct App Store Connect EULA field, which is a separate, genuinely-optional Apple setting independent of having a website page) or simply hadn't seen the current live site. Both `privacy-policy.html` (~13,000 characters) and `terms.html` (~8,100 characters) are real, well-structured, non-boilerplate documents — fetched and read in full, not sampled.

**Real accuracy gaps found on close reading (not fabricated for effect — every one below is a specific line checked against actual code):**
1. **Google Sign-In is never disclosed anywhere in the Privacy Policy** — it's real and implemented (`frontend/app/auth/login.tsx:14-17`, real Google Cloud project `691097467706`), but neither the "What Data We Collect" nor "Data Sharing & Third Parties" sections mention it.
2. **Push notification recipients are described incompletely.** The policy says alerts go "to teachers only, where enabled." The actual code (`server.py:5629-5660`) deliberately and correctly separates *school*-context check-ins (→ teacher) from *home*-context check-ins (→ linked parents/family) — a real, well-built feature the policy doesn't mention at all. Not wrong, just incomplete.
3. **Retention/deletion claims aren't backed by automation** (same root issue as S1): *"Student check-in data is retained for up to 12 months, after which it is automatically deleted... Upon account deletion, personal data is permanently removed within 30 days."* No retention/cleanup job exists in `server.py` (grep-confirmed), and there's no "account deletion" mechanism to trigger the second claim at all.
4. **Broken in-app link:** `about.tsx:142` sends users to `classofhappiness.com/privacy-policy` (no `.html`) — confirmed live, that exact URL **404s**. Real page is `/privacy-policy.html`.

**Drafted, not yet uploaded — for Jono's review:** wrote targeted accuracy edits (not a from-scratch rewrite; both documents were already good) directly into local copies of the real live pages, addressing gaps 1-3 above:
- Added a "Signing In with Google" disclosure + Google as a listed third-party processor, with a link to Google's own privacy policy.
- Corrected the push-notification bullet to describe both the teacher (school check-ins) and parent/family (home check-ins) recipient paths.
- Softened retention/deletion wording from unconditional automated claims to accurate ones: *"We aim to retain student check-in data for no longer than 12 months"* and *"If you request deletion of your account or data... we will process this manually within 30 days of your request"* — true today regardless of whether S1 gets built, and upgradeable to an automated claim later if it does.
- `terms.html`'s own data/privacy summary section already just says "you have rights... to delete your data" without an automation claim — checked, no change needed there once the Privacy Policy fix lands, since it's already consistent.
- Verified the edited Privacy Policy file parses as valid HTML (`html.parser`, zero errors).
- Saved as `/Users/jono/Desktop/privacy-policy-UPLOAD-2026-08-19.html`, same manual-cPanel-upload pattern as prior website fixes. **Not yet uploaded — needs Jono's review of the actual wording first**, then his own upload.
- `about.tsx`'s broken link (gap 4) — ✅ DONE 2026-08-19. Changed `classofhappiness.com/privacy-policy` to the real working `/privacy-policy.html`. `tsc --noEmit` clean (zero errors, not even pre-existing ones — only touched a text string). Committed and pushed (`bd8458f`). Requires a new app build to reach a real device/TestFlight; not visually confirmed.

### S3. Google OAuth Testing vs. Production status — confirmed by Jono directly; not independently verifiable from this environment
Confirmed what's actually configured in code: a real Google Cloud project (`691097467706`), three real client IDs (iOS/Android/web) wired via `expo-auth-session` in `frontend/app/auth/login.tsx:14-17`. Whether the OAuth consent screen is in **Testing** or **Production** is a Google Cloud Console setting (APIs & Services → OAuth consent screen → Publishing status) — not exposed through the Supabase service key or any API reachable without `gcloud` authentication, which isn't set up in this environment (`gcloud` isn't even installed here), so this was never independently verified by inspection — **Jono has since confirmed directly that it's still in Testing mode.**

**Real consequence while it stays in Testing:** Google caps it at 100 explicitly-added test users and shows an "unverified app" warning to everyone else — would silently block Google Sign-In for real users at any real scale, including TestFlight testers who aren't on the test-user list. **Action needed from Jono:** submit for verification in the console (may require the Privacy Policy fix in S2 and a few other prerequisites Google's own flow will specify) before general users can use Google Sign-In. Worth doing before or alongside TestFlight, not after, since testers hitting the "unverified app" wall would be a bad first impression.

### S5. Screenshots / store assets — not yet created (per Jono)
Reported directly by Jono, not independently checked (no access to a design/asset pipeline from this environment). Needed for both App Store Connect and Google Play Console listings before either can go live — screenshots per required device size, an app icon meeting each store's spec, and any promotional graphics Play Console requires. Flagging as a real, concrete pre-submission task, not investigated further here.

### S6. COPPA / children's-app classification — needs Jono's real decision, not a guess
This is a genuine legal-classification question with real compliance consequences depending on the answer, and per Jono's explicit instruction it's flagged here for his decision rather than asserted either way.

**What's actually true about the app's structure, confirmed via code (relevant context, not a recommendation):** students never create independent accounts or provide their own contact details — profiles are created and managed entirely by a teacher or parent, confirmed in `server.py` and matching the Privacy Policy's own description. This kind of "child use is supervised, the account-holder is always an adult" structure is architecturally consistent with how many EdTech apps position themselves outside the strictest "primarily child-directed" classification — but that positioning is a real legal call with real consequences (COPPA operator obligations in the US if classified as child-directed or with actual knowledge of collecting under-13 data; Google Play's "Designed for Families" program has its own strict ad/data rules if declared; Apple's Kids Category has its own separate requirements) — not something to infer from code structure alone.

**Needs Jono's decision, ideally with real legal input given the compliance stakes:** does Class of Happiness get declared as a children's/family app (Google Play target-audience declaration, Apple Kids Category) or positioned as an education tool for adults (teachers/parents/schools) with supervised student use — the classification affects both stores' submission questionnaires directly, and picking the answer that doesn't match reality carries real regulatory risk either direction.

### S4. Data collection inventory — real, pulled from the live 31-table schema, not a guess
Compiled for Apple's "nutrition label" and Google's "Data Safety" form:

| Category | What's actually collected | Source |
|---|---|---|
| Contact Info | Email, name | `users`, `family_members` |
| Identifiers | `user_id`, Expo push token, Stripe customer ID, session token | `users`, `user_sessions` |
| Photos | Creature-drawing photo uploads (4 stages per submission), custom avatar images | `creature_submissions.stage1-4_url`, `avatar_custom` fields |
| User Content | Check-in comments, creature names/descriptions, alert/support messages, wellbeing-tracker notes | `feeling_logs`, `creature_submissions`, `student_alerts`, `school_wellbeing_tracker` |
| Emotional/wellbeing data | Zone check-ins (blue/green/yellow/red) + free-text comments — the app's core data, and genuinely sensitive-adjacent (a minor's emotional state) — likely needs its own line item, not lumped into generic "usage data," on both platforms' disclosure forms | `feeling_logs`, `family_zone_logs` |
| Usage Data | Timestamps, language preference, subscription/trial status | `users`, most tables |
| Financial | Stripe customer ID + subscription status only — confirmed no raw card data anywhere (Stripe handles that directly, matching the policy's own claim) | `users` |
| Confirmed NOT collected | No location/GPS, no contacts. `students.device_id` column exists in the schema but is verified never read or written anywhere in backend or frontend — dead/unused, so the policy's "no device identifiers from students" claim is actually accurate in practice | grep-confirmed across `server.py` and the full frontend |

**Needs Jono's decision:** use this table directly to fill in Apple's App Privacy questionnaire and Google's Data Safety form — recommend treating the emotional/wellbeing check-in data as its own disclosed category rather than folding it into generic usage data, given how central and sensitive it is to what this app actually does.

---

# FEATURES BUILT (outside the original review scope)

### F1. Kids' voice recording playback on colour/helper check-in buttons — ✅ DONE 2026-08-19, live-verified
**Files:** `backend/server.py` (`GET /voice-clips`), `frontend/src/utils/voiceClips.ts` (new), `frontend/src/components/VoiceToggleButton.tsx` (new), `frontend/app/student/zone.tsx`, `frontend/app/student/strategies.tsx`, `frontend/app/settings.tsx`

Real narrated audio (Matilda in English, Mateus in Portuguese) now plays automatically when a student taps a colour on the check-in zone screen or selects a helper strategy. English and Portuguese only — the other 4 app languages silently skip playback since no clips exist for them yet, by design (same "missing key → skip" path used for unfinished Portuguese clips).

**Storage decision:** no new DB table. `GET /voice-clips?language=X` lists the `voice-recordings` Supabase Storage bucket directly and returns `{clip_key: url}` for whatever's actually uploaded — replacing or adding a clip is the only step ever needed; nothing else can drift out of sync with it (the kind of dual-source-of-truth drift this review repeatedly found elsewhere, e.g. A7, A9).

**Bucket reorganization:** the bucket Jono had already created (`voice-recordings`, not `voice-clips` as first proposed — caught and corrected before building) held 50 real `.m4a` files flat at the root, human-named (`Blue_emotions.m4a`, `Ajudar_um_amigo.m4a`, etc.), not organized by language or matching the 28 canonical `clip_key` IDs (4 colours + 24 helpers, from `DEFAULT_HELPERS`). Listed the full inventory live, mapped every file to its canonical key by content (2 ambiguous matches — `Drink_water.m4a`→`blue_2`, `Continua_assim.m4a`→`green_1` — confirmed with Jono before moving anything), and moved all 35 real clips into `{language}/{clip_key}.m4a` via the Storage move API. Fixed two filename hazards in the process: a literal `&` in `Squeeze_&_release.m4a` and a trailing space in `Emocoes_Vermelhas .m4a` — both gone once renamed to plain `clip_key` names. Left 15 non-matching clips (greeting/praise/closing lines — `Well_done`, `See_you_tomorrow`, `Como_te_sentes_hoje`, etc.) untouched at the bucket root; Jono confirmed these are for a future phase (check-in start/completion screens), not this build.

**Coverage after reorganization:** English 27/28 (missing only the `green` colour clip). Portuguese 8/28 (4 colours + 4 of 6 green-zone helpers; all blue/yellow/red helper clips still pending from Mateus) — matches what Jono described going in.

**Playback + mute:** `voiceClips.ts` mirrors the existing (but previously unused/unwired) `sounds.ts` pattern. One persisted setting (`AsyncStorage`, device-local — not synced to the account, since check-in devices are often shared classroom tablets) with two entry points: a Settings toggle and a quick per-screen mute button on both check-in screens, rather than two states that could drift apart. Helper audio fires only when a card is *selected*, not deselected. Both of the app's two different strategy-ID schemes (`blue_1` from the real backend data, `b1` from the frontend's hardcoded emergency fallback) are normalized to the same clip key at lookup time, so playback doesn't silently break depending on which source a strategy card came from.

**Verified:** `python3 -m py_compile` and `tsc --noEmit` clean on every touched/new file (zero errors, not even pre-existing ones). All 35 reorganized Storage files confirmed publicly fetchable (200, correct `audio/x-m4a` content-type) via direct HTTP checks before and after deploy. Live-tested `GET /voice-clips` against production for both languages post-deploy: English returns exactly 27 keys, Portuguese exactly 8, an unsupported language (`es`) returns `{}` cleanly. **Not audio-tested in a running app** — no simulator/device available in this environment, so playback itself (does it actually sound right, timing, volume) hasn't been confirmed — only that the URLs, manifest, and code are correct. Committed and pushed (`c5ba4cc`).

**Follow-up round (2026-08-19) — 4 real-device UI bugs found and fixed:**
1. **Settings Voice switch invisible** — not a rendering bug: `settingLeft`/`settingText` had no width constraint, so the Voice row's long description text overflowed uncapped and pushed the `Switch` off the visible card edge entirely. Short values elsewhere (e.g. "English") never triggered it, which is why no other row looked broken before. Fixed by adding `flex: 1` to both — bounds the text to the row width, restores normal wrapping, applies to every row in the file at once.
2. **Description text overflowing card boundary** — same root cause and same fix as #1.
3. **Colour screen mute icon overlapping the greeting card** — it was `position: 'absolute'` floating over the top of the screen, directly on top of the greeting card's corner (both anchored to almost the same spot). Fixed by moving it into the greeting row as a normal flex child instead of an absolute overlay, so it can't overlap anything regardless of name length or screen size.
4. **Helper screens: icon needed to be inside the coloured zone heading, not top-right** — moved into the existing `zoneHeader` box (the bordered, colour-tinted "outline" showing the dot + zone name) as a flex sibling pushed to its right edge. One shared component driven by `zone`, so this covers all four colour pages in a single change.

All four verified via `tsc --noEmit` (zero new errors in either touched file, same 2 pre-existing unrelated errors as all session). Not visually re-confirmed on a device — no simulator available in this environment. Also flagged, not fixed: the same description-overflow symptom exists on the Set Password screen and in admin emails — separate files/StyleSheets, pre-existing, unrelated to tonight's feature — logged as a new P2 backlog item, not touched here per Jono's instruction.

**Coverage completed (2026-08-19, later same night):** 21 more files uploaded — 20 real Portuguese helper clips (filling every remaining blue/yellow/red slot plus green_5/green_6) and the one missing English colour clip (`Green_emotions.m4a`). Inventory-checked and mapped against the 28 canonical keys before touching anything, per the same process as the first batch — all 20 Portuguese mappings were unambiguous 1:1 translations (two, `Beber_agua`→`blue_2` and `Pedir_ajudar`→`red_5`, flagged as "plausible not certain" pending Jono's confirmation; `blue_2` confirmed correct — "Drink Water" is the current real name, "Warm Drink" in `DEFAULT_HELPERS` is stale text needing its own separate update, not yet done). Two more filename hazards fixed in the move: a literal space in `Contar ate 10.m4a` and a trailing space in `Contar_ao_contrario .m4a`. Reorganized into `{language}/{clip_key}.m4a` the same way as before. **Both English and Portuguese are now 28/28 — fully complete.** Live-verified immediately after the move (no deploy needed, pure Storage change): `GET /voice-clips?language=en` and `?language=pt` both return exactly 28 keys.

`red_5`/`Pedir_ajudar` — ✅ confirmed correct by Jono on a real device ("Pedir Ajuda"/"Ask for Help", matches). `blue_2`/`Beber_agua`/`Drink_water` also confirmed correct earlier. Both ambiguous matches from this batch are now resolved.

**"Warm Drink" → "Drink Water" — ✅ DONE 2026-08-19**, see the dedicated writeup below (this section's own scope was just the filename↔clip_key pairing; the actual displayed text across `DEFAULT_HELPERS` and every other real name-resolution list was fixed separately, commit `21b0f27`).

**Real problem found by real-device listening, 2026-08-19-20 — Portuguese content/clip_key mismatch, distinct from the filename-pairing work above — ✅ DONE, fully resolved:** Jono reported the actual *spoken content* on several Portuguese helper clips didn't match their clip_key/label — not a bad recording, a mispairing. By ear: Blue all 6 wrong, Green 1&2 correct but 3-6 wrong, Yellow 1 correct but 2-6 wrong, Red all 6 correct. Investigated whether this was introduced by this session's own reorg work — ruled out: the Storage move API only renames, cannot alter file bytes, and re-checking every PT translation used for the mapping confirmed the semantic pairing was linguistically correct. Checked original upload timestamps (preserved through the move) as a possible ordering signal — dead end, all 20 files were uploaded in a ~2-second bulk window, no usable sequence in that window. **Conclusion: the mismatch predated this session's reorg — the files were already misaligned with their actual spoken content before being uploaded/renamed.** No file-metadata signal could resolve which content actually belonged where, so this needed Jono to listen to each wrong slot and report what it actually said.

**Jono's by-ear data revealed the real pattern — each zone's slots 2-6 independently shuffled among themselves** (slot 1 always correct in every zone), consistent with a per-zone recording/export-order mix-up rather than one global mechanical bug:
- **Blue** — two cycles: `(blue_2 → blue_5 → blue_3 → blue_2)` and `(blue_4 ↔ blue_6)`.
- **Green** — two swaps: `(green_3 ↔ green_5)` and `(green_4 ↔ green_6)`. `green_1` flagged separately (see below), `green_2` genuinely correct.
- **Yellow** — one connected 5-cycle: `(yellow_2 → yellow_5 → yellow_6 → yellow_4 → yellow_3 → yellow_2)`.
- **Red** — confirmed 100% correct, zero changes.

**Applied:** 25 Storage move operations (each affected clip staged through a temporary filename first, then moved to its final correct slot — necessary because several of these are closed cycles, not simple pairwise swaps, so a direct move would have overwritten content before it was relocated). Verified zero leftover temp files afterward and all 28 canonical PT keys still present. Live-verified: `GET /voice-clips?language=pt` still returns exactly 28 keys post-move, URLs resolve to the newly-correct paths.

**`green_1` — flagged, not touched:** audio says "Continua Assim" (keep going *like that*), expected "Continua" (keep going). Not part of the shuffle — no other slot's content matches it better, and nothing else is missing a home. This is a phrasing difference, not a mispairing; a rename can't fix it since there's no file to swap it with. Left as-is; a re-record is Jono's call if he wants it tightened.

**Bonus finding, same investigation — `red_1`'s displayed text didn't match its (correct) audio:** audio says "Ficar Parado" (Stay Still); the Portuguese helper name shown in the app said "Parar" (Stop). Jono confirmed the audio is right and the text should change to match, not the reverse. Fixed in both real Portuguese sources: `HELPERS_PT` (`server.py`, the live `GET /helpers?lang=pt` data — also fixed its dead, shadowed duplicate definition for consistency, since it's harmless and avoids future confusion) and `STRATEGY_NAME_MAP_PT` (PDF/report name resolution). Confirmed via grep this doesn't touch Spanish's own separate `"Parar"` entries (different language, unrelated, correctly left alone) and that no frontend/portal file hardcodes Portuguese strategy names — fully contained to two backend dicts. Deployed and live-verified: `GET /helpers?feeling_colour=red&lang=pt` now returns `"name": "Ficar Parado"` for `r1`; `lang=es` still correctly returns `"Parar"`. Committed and pushed (`2dc1eb2`).

**The Portuguese content-quality issue across the entire 28-clip helper set is now fully closed.**

**Not built yet, explicitly out of scope for this pass:** the 15 future-phase greeting/praise clips (F1 follow-up, whenever those screens get their own audio treatment — inventoried 2026-08-19: `How_are_you_feeling_today`/`See_you_tomorrow`/`Thank_you_for_checking_in`/`Well_done`/`Great_job`/`Check_in_with_my_feelings`/`You_did_it` all have EN+PT pairs recorded; `I_did_it` has no Portuguese counterpart yet); backend translation-dict entries for the new `t('voice')`/`t('voice_narration')`/`t('voice_narration_desc')` keys (currently rely on the English fallback string, same as many other keys in this app — not a functional gap, just unpolished for non-English Settings screens).

### F2. Custom strategies silently skip voice playback — needs a "why is this quiet" indicator, not a fix
Custom strategies (teacher/parent-added, per-student) will never have a canonical voice clip — `playVoiceClip` correctly no-ops for them (same code path as unsupported languages/unfinished clips), so nothing crashes, but a kid tapping one and hearing nothing has no way to know that's expected rather than broken. **Decision (Jono):** don't try to make it sound like a mute/error state — add a small visual marker on custom-strategy cards themselves (e.g. a "Made for you" badge, or a distinct border/colour) that explains the silence as a positive, on-brand touch rather than looking like a bug. Plan to be shown before building — see below. Separately logged as a real future feature, not building now: letting students/families record their own voice for a custom strategy, likely needing teacher/parent approval before it plays given child-safety considerations.

---

# POST-LAUNCH
*(Explicitly deferred by Jono — record only, no work now.)*

### P1. Multi-currency / country-adjusted pricing
Launch is **EUR-only**. AUD figures currently exist in `SUBSCRIPTION_PLANS` (`price_aud` fields, `server.py:44-79`) as leftover/parallel values — leave as-is for now, don't extend or fix them as part of L2/L3. Deferred work for later: real multi-currency support, purchasing-power-adjusted pricing by country, and a public-vs-private-school pricing distinction. For now, affordability cases are handled manually via NGO grants and ad hoc discounts, not through product-level pricing tiers.

### P2. UX/polish backlog — from real-device testing, 2026-08-20
Record only, no investigation or design done yet. All raised by Jono during real-device testing tonight, alongside A18/A19 above.

- **Creature submission placement** — ✅ DONE 2026-08-19. Removed the standalone "Submit Creature" shortcut from `parent/dashboard.tsx` and `teacher/dashboard.tsx`; added a persistent "Submit a Creature" button inside the shared `CreatureManagement` component instead (used by both `/parent/creature-code` and `/teacher/creature-code`), so one change covered both surfaces. `tsc --noEmit` clean on all 3 touched files (zero errors, not even pre-existing ones). Committed and pushed (`655ca44`). Not visually confirmed on a device.
- **Classroom code — copy-to-clipboard.** Add a copy icon next to the classroom join code so teachers/parents don't have to manually retype it.
- **Missing loading indicator, Family dashboard.** No emoji/spinner loading state shown while the family dashboard's data is fetching — screen likely appears blank or frozen during load.
- **School contact details not editable.** Neither superadmin nor school_admin currently has a way to edit a school's contact details (address/phone/email or similar) — needs a real UI path, not currently exposed anywhere in app or portal.
- **Analytics: school-specific stats + colour-over-time graph.** Current analytics are aggregate; want the ability to drill into a single school's stats specifically, plus a graph showing zone-colour trends over time (not just point-in-time counts).
- **Future idea: "team sharing" PDFs.** Rough concept, not scoped — some way to share PDF reports with a wider team/group rather than one-to-one. Needs real scoping before it's actionable.
- **Settings-row text overflow, pre-existing, wider than tonight's fix.** Fixing `settings.tsx`'s Voice toggle (2026-08-19, see F1 follow-up work) surfaced that its `settingLeft`/`settingText` styles had no width constraint, letting long description text overflow the card and push right-side elements (like a `Switch`) off-screen entirely — fixed in that one file. Jono separately confirmed the same description-overflow symptom exists on the **Set Password screen** and in **admin emails**, in files with their own separate `StyleSheet` definitions, not touched by tonight's fix. Not investigated — needs its own look at whichever screens/emails are affected before fixing.
- **Settings shows no "which school am I in" confirmation, and "Status" label is misleading for school-covered teachers — found 2026-08-20 during account-deletion live testing.** Two related gaps in `settings.tsx`: (1) once a teacher successfully joins a school, the "Join Your School" card just disappears (`settings.tsx:645`, gated on `!school_name`) — nothing replaces it with a positive "You're enrolled in: {school_name}" confirmation, so there's no UI anywhere showing which school a teacher belongs to. (2) The "Status" row (`settings.tsx:365-369`) only reflects personal `subscription_status` (`trial`/`active`/else "Inactive") — it has no concept of school-plan coverage (`school_admin_id` presence, used server-side e.g. `server.py:2583`'s `is_school_covered`), so a teacher fully covered by their school's plan still sees "Inactive." Same gap exists in the global `hasActiveSubscription` flag (`AppContext.tsx:640-642`) — also purely personal-subscription-based, no school-coverage awareness, so any other UI gated on that flag could misfire for a school-covered teacher too. Confirmed via live DB check that the underlying data is correct (`jono+deletetest@gmail.com` has `school_admin_id`/`school_name` both correctly set to Sunshine International) — this is purely a display/gating-flag gap, not a data bug, and doesn't affect account-deletion's reassignment logic (reads `school_admin_id` straight from the DB). Explicitly deferred — Jono was mid account-deletion test, didn't want it fixed tonight.
- **Portal "Users at Your School" always shows nothing for school_admin** — ✅ DONE 2026-08-21. Found 2026-08-20 during account-deletion live testing (confirmed pre-existing, unrelated to that test). `renderSchoolUsers()` (`portal100.html:1506`) called `/admin/users`, hardcoded superadmin-only (`server.py:8874`) — every school_admin call silently 403'd. Fixed by adding a real school-scoped endpoint, `GET /school-admin/users` (`server.py`, right after `/school-admin/analytics`), filtered server-side to `school_admin_id == caller's own user_id`; portal now calls that instead, with the now-redundant client-side filter removed. `py_compile`/`node --check` clean, committed and pushed (`3fdcb93`). **Live-verified** against `schooladmindemo`: returns the 4 real linked users (Sarah Mitchell, James Okonkwo, Maria Santos, plus the `jono+deletetest@gmail.com` test account — still listed since it's only soft-deleted, 30-day grace period, by design). Portal side uploaded to cPanel by Jono (`portal100-UPLOAD-2026-08-21.html`).
- **Portal "Class Performance Comparison" shows stale demo classes and hides real reassigned ones** — ✅ DONE 2026-08-21. Found 2026-08-20 during account-deletion live testing (confirmed pre-existing, unrelated to that test). `/school-admin/analytics` built its classroom lookup from linked `teacher_ids` only, never the school_admin's own `user_id` — so a classroom reassigned to them (e.g. via account-deletion's teacher-reassignment logic) was invisible, while empty pre-seeded demo classrooms (`demo_teacher_001/002/003`) still rendered. Fixed: classroom/student lookup now also includes the school_admin's own `user_id` (`classroom_owner_ids = teacher_ids + [user_id]`) — `teacher_ids` itself deliberately left untouched everywhere else in the function (total_teachers/seats_used/teacher_checkin_rate must stay teacher-only). Also fixed the early-return that skipped straight to "no data" whenever `teacher_ids` was empty, even if the school_admin had their own classrooms — now only returns empty if there's truly nothing (`not teacher_ids and not classrooms`). Added a real `teacher_name` fallback (the school_admin's own name) for classrooms they own directly, instead of generic "Teacher". `py_compile` clean, committed and pushed (`3fdcb93`). **Live-verified** against `schooladmindemo`: `classroom_breakdown` now includes `{"classroom_name": "Geography Class", "teacher_name": "Demo School Admin", "student_count": 1, "checkin_count": 0, ...}` alongside the 3 demo classes — the exact reassigned classroom (Geography Class / student Tom) Jono wanted to confirm was visible. **Update 2026-08-21**: this backend fix was correct, but Jono still saw zero bars for every classroom after uploading — traced to a SEPARATE, pre-existing portal bug (see next entry), now also fixed.
- **Portal "Class Performance Comparison" bar chart always showed zero bars for every classroom, real or demo, regardless of actual check-in data — found 2026-08-21.** After the classroom-inclusion backend fix above (confirmed correct via direct API test — `zone_distribution` had real non-zero numbers for demo classes), Jono still saw flat/empty bars for every class in the actual portal. Root cause: `portal100.html:1854-1857`'s Chart.js dataset read `c.zone_counts?.green` (and blue/yellow/red) — but the backend field is named `zone_distribution`, not `zone_counts` (confirmed in `server.py`'s `classroom_breakdown` construction). Since `zone_counts` never existed on the response, every `?.x||0` silently fell back to `0` — this has been broken for every school, not just Sunshine, and not something introduced by tonight's or 2026-08-20's backend work; purely a pre-existing frontend field-name mismatch, not a caching issue (a hard refresh would never have fixed it). Fixed: renamed all 4 lookups from `zone_counts` to `zone_distribution`. `node --check` clean, diff confirmed isolated to just this rename. Also confirmed separately: this chart never displayed a numeric "N students" figure anywhere — only coloured zone-count bars — so there's no separate "student count missing" bug, that's just what the (until-now broken) bars were supposed to represent. Needs upload: `portal100-UPLOAD-2026-08-21b.html`.
- **`/school/bulk-invite` ignored superadmin-supplied target school_id — same bug class as `/school/generate-invite-code`** — ✅ DONE 2026-08-21. Found while investigating why zero parents show for Sunshine in the new "Users at Your School" list: system-wide, only 1 parent account has ever had `school_admin_id` set (linked to Kairos, not Sunshine) — traced to `/school/bulk-invite` (the portal's superadmin Schools panel "📧 Bulk Invite" button) always using the CALLER's own `user_id`/`school_name`/`subscription_expires_at`, ignoring any `school_id` the portal sent, identical to the bug fixed in `generate-invite-code` (`a4f1a28`, 2026-08-20). Fixed the same way: superadmin can now pass `school_admin_id` in the body to target a specific real school (404s cleanly on a bad id); default (no target, or school_admin's own self-service call) unchanged. Unlike `/school/join`, this endpoint has no role restriction on the target account (can link a parent or teacher), so it's the only real path to link a parent to a school right now — `/school/join` was deliberately locked to `role=="teacher"` on 2026-08-20 (see A19/parent-teacher-linking fix above). `py_compile` clean, committed and pushed (`63bef66`). **Live-verified**: school_admin self-service still links to their own school (unaffected); superadmin with an explicit `school_admin_id` correctly links to that school, not superadmin's own account; superadmin with no target still defaults to superadmin's own id (old behavior preserved); bogus target 404s. All 3 throwaway test accounts cleaned up after verification. Note: the portal's Bulk Invite UI itself still doesn't pass a working target (same deferred "Schools & Codes" UI work as `generate-invite-code` — `school_id` it sends refers to the near-empty `school_profiles` table, not a real school_admin id) — this fix is backend-only infrastructure, consistent with what was deferred on 2026-08-20.

### Analytics Parity Audit — 2026-08-21, full cross-surface audit (school_admin/superadmin/teacher/parent × app/portal, PDF exports, expand/collapse)
Full audit run via 5 parallel research passes plus direct follow-ups, triggered by Jono wanting to pick the "comprehensive analytics" thread back up. Full writeup published as an artifact (not reproduced in full here — this entry tracks status/action taken on each finding).

- **`/admin/stats` leaked platform-wide data to school_admin, labeled "Your school's data"** — ✅ DONE 2026-08-21, launch-blocking, fixed same night it was found. Live-confirmed before/after: zone_counts went from summing to 1000 (whole platform, 90d) to 840 (exactly matching the portal's already-correct `/school-admin/analytics`). Also fixed in the same pass: classroom-ownership gap (school_admin's own directly-owned classrooms, e.g. reassigned via account-deletion, were invisible to this endpoint too — same fix already applied to `/school-admin/analytics`), and the app's own Users tab (`UsersManager` in `admin/dashboard.tsx`) calling the superadmin-only `/admin/users` directly instead of the new scoped `/school-admin/users`. One block (`feeling_logs` rows with a null `student_id`) deliberately left unscoped — that table has no teacher/user identity column at all, so it structurally can't be scoped without a schema change; disclosed in a code comment. Committed `84d46f2`, deployed, **live re-verified fresh** twice: `/admin/stats?days=90` and `/school-admin/analytics?period=90` return identical numbers (840/25/4) on the same account/window.
- **`GET /analytics/student/{student_id}` had no auth check at all** (`server.py:3217`) — ✅ DONE 2026-08-21, launch-blocking, fixed same night. Now requires login and mirrors the exact ownership pattern already used correctly on the nearby student-PDF endpoint (owns directly, owns their classroom, is a genuinely linked parent via `parent_links`, or a `family_members` link; superadmin always allowed). Committed `0dd8df0`, deployed. **Live-verified, 4 scenarios**: no token → 401; `schooladmindemo` requesting Tom (owned via their own reassigned classroom) → 200; `schooladmindemo` requesting a demo_teacher_001 student they don't own → 403; superadmin requesting the same non-owned student → 200.
- **`GET /reports/pdf/family/{family_member_id}/month/{year}/{month}` had no ownership check** (`server.py:4008`) — ✅ DONE 2026-08-21, launch-blocking, fixed same night. Added the missing `.eq("user_id", user["user_id"])` to the lookup; kept the failure as a 404 ("Family member not found") for both "doesn't exist" and "isn't yours," so the response never confirms whether a given id exists. Committed `0dd8df0`, deployed. **Live-verified, 3 scenarios**: no token → 401; `jono@gmail.com` requesting their own family member (Madalena, April 2026, real data) → 200, real PDF returned (116KB, valid PDF 1.4); same account requesting a family member owned by a different user → 404.
- **`GET /reports/pdf/classroom-overview` has no role check, only ownership-of-URL-user_id** — confirmed real but low severity (no cross-account leak, just doesn't enforce its own "teacher's classrooms" docstring). Not fixed, not blocking — logged for a future pass.
- **Portal's school_admin "🚨 N students need support" banner was undismissable and unclickable** — ✅ DONE 2026-08-21. `saAlertBanner` (`portal100.html:1613`) had zero interactivity — no `onclick` anywhere, unlike the identically-shaped `saColorTip` dismiss button right above it. Investigated the real/stale question too: school_admin has no separate "Alerts" nav tab at all (`buildSchoolAdminPortal`'s tabs are Dashboard/Resources/Strategies/Users/Wellbeing Tracker/Our Team/Services/Creatures/My Wellbeing) — "Recent Alerts" is just a card within the same Dashboard/overview page as the banner itself, so "click through" can only mean scroll-to, not a tab switch. Fixed: banner now `scrollIntoView`s to the `saAlerts` card on click. Separately confirmed **why the "24" number appears**, live via direct Supabase query: it's `students_needing_support` (distinct students with any 'red' log in the active period) from the already-correctly-scoped `/school-admin/analytics` — genuinely real data, not a bug, but it's 100% demo-seed artifact, not a live signal: all 24 of Sunshine's real (demo-seeded) students have at least one red-zone log somewhere in their history (170 total red logs, seeded 2026-07-30), so ANY period ≥30 days shows "24 of 24" — literally the entire school. The default 7-day "Week" view correctly shows 0 (nothing in the last 7 days); the banner only appears once you switch to Month/Term/Year. Not something to "fix" — just not a real live alert, a fully-saturated demo fixture. Needs upload: `portal100-UPLOAD-2026-08-21c.html`.
- **Superadmin portal's "Schools Breakdown" shows 0 check-ins this week for both schools** — confirmed genuinely correct, not a bug (checked same night). Sunshine's real seeded data is dated 2026-07-30 (outside any 7-day window); Kairos has two `school_admin` rows, neither with any linked teachers or students at all. Not the same bug class as Class Performance Comparison (that was a real field-name mismatch hiding real data — this is real data correctly reflecting a stale/empty dataset).
- **Superadmin portal's "Schools Around the World" map** — confirmed to be the same known, already-flagged `school_profiles`-table emptiness issue (`renderSAWorldMap(wwEl, saAllSchools)`, fed by `loadSASchools()`, comment explicitly says "real lat/lng from school profiles"), not a new bug. Deferred consolidation work, unchanged status.
- **No family-wide/"export all" PDF export** — confirmed real gap, only one family PDF endpoint exists (`/reports/pdf/family/{family_member_id}`, scoped to a single member). Post-launch, not building tonight.
- **Parent portal's "Family Emotions" donut has no time-window selector**, silently defaults to the backend's `days=7` fallback (both `/zone-logs/student/{id}` and `/family/zone-logs/{id}` called with no `days` param), and the scope label next to the heading only ever shows which child is selected, never the time window. Post-launch, not building tonight.
- **`jono+teacher@gmail.com` demo account was `role: "parent"`, not `teacher`, despite the login shortcut's name/PIN implying otherwise** — ✅ DONE 2026-08-21, flipped to `role: "teacher"` in the DB directly (was already linked to Kairos Montessori via `school_admin_id`/`school_name`, so no other change needed). Root-caused as a stale/mislabeled test account, not a portal bug — `buildPortal()`'s role dispatch (`portal100.html:410-423`) is a plain, unambiguous switch with no fallthrough; `buildTeacherPortal()` is real and full-featured. Zero real users were ever affected.
- Everything else from the audit (school_admin classroom-level view missing from app, superadmin's dead "Subscriptions & Revenue" section, orphaned `/admin/dashboard-summary`, teacher/parent app-vs-portal minor gaps, the PDF export matrix's remaining gaps — school-overview app-only, classroom-overview portal-only, superadmin has no PDF UI anywhere, teacher-wellbeing PDF picker app-only, expand/collapse never persisting) — confirmed real, all missing-feature/parity gaps rather than broken-or-insecure-data issues, correctly triaged as post-launch. Not reproduced in full here; see the published audit artifact for complete detail.

### School_admin wellbeing depth — 2026-08-21, three real gaps closed
Triggered by Jono explicitly reframing school_admin as "who they represent in real life" (principal/SEND coordinator/psychologist) and asking what that person actually needs to see across student/teacher/classroom/school levels. Full audit was reported inline (not reproduced here); status of the three real gaps found:

- **"Download School Report" was a print-dialog, not a real PDF** — ✅ DONE 2026-08-21. Root cause: the portal's only reachable button here (labeled "🖨️ Print" on the Dashboard's Emotion Check-ins card) called `window.print()` — never hit any backend endpoint. A second, separate half-built function (`exportOverviewPDF()`) existed but was never wired to any button — confirmed dead, removed entirely. Real fix: new `downloadSchoolOverviewPDF()` (portal), mirroring the already-working `downloadAuthPDF()` pattern used by classroom-overview/teacher-wellbeing, hitting `/reports/pdf/school-overview` (already existed, already worked — confirmed in the PDF audit). **Live-verified**: 200, real PDF, 113943 bytes.
- **Per-student click-through for school_admin — confirmed genuinely absent, not just unlinked** — ✅ DONE 2026-08-21. `openStudentDetail()` already existed (teacher/superadmin-only before this) but school_admin had no student roster to click from at all — `/school-admin/analytics` is deliberately aggregate-only, no names/IDs, by design. Built: new `GET /school-admin/students` (school-scoped roster with classroom names), a new portal "Students" tab reusing `openStudentDetail()`, and a shared `_is_authorized_for_student()` helper adding school_admin as a real authorized role. **Found and fixed in the same pass**: `/feeling-logs/{id}` and its two aliases (`/zone-logs/{id}`, `/zone-logs/student/{id}`) had **zero authentication at all** — same severity class as the two unauthenticated endpoints fixed earlier tonight — plus `/reports/available-months/{id}` had no auth either. All three now use the same shared helper. **Live-verified**: real roster returned (25 students incl. Tom via the reassigned Geography Class), analytics/feeling-logs/available-months all 200 for an own-school student, 404 (not a leak) for a bogus id.
- **Teacher-consent mechanism for individual wellbeing visibility — built from scratch, migration pending** — ✅ backend + app + portal DONE 2026-08-21, **not live until migration runs**. Previously there was no path at all, not even opt-in, for school_admin to see a specific teacher's wellbeing. Default-off, explicit opt-in, revocable any time — mirrors the existing parent↔teacher `home_sharing_enabled` pattern. New `users.teacher_wellbeing_shared_with_admin` column (migration below), `GET/PUT /teacher/wellbeing-sharing-status` + `/teacher/toggle-wellbeing-sharing`, `_school_admin_can_view_teacher_wellbeing()` gating both the existing teacher-wellbeing PDF and a new `GET /school-admin/teacher-wellbeing/{id}` JSON view. App: real toggle in Settings ("Share My Wellbeing", teacher-only, gated on already being linked to a school). Portal: shared teachers get a "💚 Shared" badge in "Users at Your School", clickable to an inline 30-day zone-count summary. **Live-verified the defensive fallback specifically** (migration not run yet): `GET` returns `{"shared":false}` cleanly, `PUT` returns a clear 400 "not available yet" instead of a 500 — nothing broken in the meantime.

  **Migration required** (not run by me — no DDL access from this environment):
  ```sql
  ALTER TABLE users ADD COLUMN teacher_wellbeing_shared_with_admin boolean DEFAULT false;
  ```

### Creature enrichment — 2026-08-21, backend + UI done, one piece migration-pending
Full gap analysis was reported inline before building (not reproduced here). All 5 requested items built:

- **Stages carousel, expiry badge, evolution count** — ✅ DONE, live-verified. `/creatures/global` now returns `stage2/3/4_url`, `times_fully_evolved` (per-creature count from `creature_unlocks` where `stages_unlocked==4`, grouped — same data/pattern already used in `/creatures/analytics`'s aggregate view, just per-creature now), and `featured_until` (only populated when actively featured, via a real join to `featured_creatures`). Portal: `renderApprovedCreaturesInto()` — a real image carousel (‹ › arrows, stage counter), evolution count, and a featured-expiry badge when applicable. Live-verified against the real "Waterman" creature: all 4 stage URLs present, `featured_until: 2026-08-31...` (correctly currently featured), `times_fully_evolved: 0` (real, no unlocks yet).
- **Superadmin view, same card, system-wide** — ✅ DONE. `/creatures/global` already had no school filter (confirmed, not a new change) — added an "✅ Approved Creatures" card to superadmin's Creatures tab, reusing the exact same `renderApprovedCreaturesInto()` function, not a separate/lesser view.
- **Classroom + which-student picker on submission** — ✅ backend + app DONE, **classroom capture not live until migration runs**. Confirmed live, via real data, that `creature_submissions.student_id` has always actually been the SUBMITTING ACCOUNT's own id (a real "approved" row's `student_id` resolves to a `users` row with `role: teacher`, not any `students` row at all) — so there was never a reliable way to know which child or classroom made a creature. Built: a real "Who made this? (optional)" picker on the submission screen (`submit-creature.tsx`) — teachers see their own students (`/students`), parents see their linked children (`/family/members`, only resolves a real classroom for school-linked ones). `/creatures/submit` accepts optional `real_student_id`/`classroom_id` with a defensive fallback (same insert-with-fallback pattern already proven working on the teacher-consent toggle above) so nothing breaks before the migration runs. Old submissions are NOT backfilled, by design.

  **Migration required** (not run by me):
  ```sql
  ALTER TABLE creature_submissions ADD COLUMN real_student_id text;
  ALTER TABLE creature_submissions ADD COLUMN classroom_id text;
  ```

### Creature superadmin scope-visibility bug — 2026-08-21 — ✅ DONE & LIVE-VERIFIED
Found live while testing the newly-built superadmin scope-editing dropdown: `/creatures/global`'s query unconditionally filtered `.eq("is_globally_available", True)`, and changing a creature's scope away from "global" (via the dropdown) sets `is_globally_available: False` — so the creature immediately vanished from the very card the dropdown lives on. Fixed: superadmin callers now see all approved creatures regardless of scope/availability; every other role keeps the original global-only filter. Confirmed live via `/creatures/global`: superadmin session saw a school-scoped creature that a non-superadmin session correctly could not.

### Student-facing creature eligibility + privacy fix — 2026-08-21 — ✅ trimmed scope DONE & LIVE-VERIFIED
Real gap: approved community creatures (`visibility_scope`: classroom/school/global) never actually surfaced anywhere a student could see them — `world-creatures.tsx` called a nonexistent `api.get` (no such export existed in `api.ts`) wrapped in a silent `catch`, so it always rendered the empty state regardless of how many creatures were approved. This directly explains the original "approved creatures don't show up anywhere students can see" report — it wasn't a missing feature, it was a genuinely broken screen. Separately, `/creatures/unlock` and `/creatures/my-unlocks` were real, working endpoints with **zero frontend call sites** (never wired to any UI), and `student/creatures.tsx` ("My Creatures" collection screen) was fully orphaned dead code — wrong endpoint URL, undefined `authToken`/`onBack` refs, zero call sites anywhere in the app.

Built (trimmed scope, per Jono's explicit call — scope-preference toggle and expiry-aware history snapshots deferred, see below):
- New `GET /creatures/eligible?student_id=` — real eligibility computation (classroom/school/global match against each creature's `visibility_scope`, resolving the student's classroom→owning teacher's `school_name`), replacing the broken `api.get('/creatures/global')` call in `world-creatures.tsx`.
- **Privacy fix** (explicit ClassDojo precedent, non-negotiable per Jono): `/creatures/eligible` never includes peer creator identity (`student_name`/`school_name`/`country`) — the admin-only `/creatures/global` (school_admin/superadmin moderation tooling) keeps identity, since that's a separate, appropriately-gated audience.
- `unlock_creature_stage` now optionally enforces eligibility server-side when the caller sends `real_student_id` in the body — a student can no longer unlock a creature outside their own eligibility (403), while old callers with no `real_student_id` are unaffected.
- Rewired `world-creatures.tsx` (real progress dots + unlock button per card, "My Collection" link) and rewrote `student/creatures.tsx` from scratch against the real `/creatures/my-unlocks` shape, now reachable from World Creatures.
- **Live-verified end-to-end** against real production data (Kairos Montessori's "Waterman" [global] / "Waterman 2" [school-scoped]): a Kairos student's `/creatures/eligible` correctly returned both; a different-school student correctly returned only the global one; the different-school student's unlock attempt on the school-scoped creature correctly 403'd ("This creature isn't available to this student"); the Kairos student's unlock attempt correctly proceeded (200, check-in-threshold message). No creator identity present in any `/creatures/eligible` response.

**Deferred to next session, explicitly logged per Jono's call** (not built tonight):
- Student scope-preference toggle (family/school/global/any) in "My Creatures" — confirmed genuinely absent from both `students` and `users` tables, needs a new column.
- Expiry-aware obtained-history — `creature_unlocks` only stores a final `completed_at`, no per-stage timestamps and no snapshot of the creature's state (name/scope/images) at the moment of obtaining, so a later scope change or removal would silently rewrite history. Needs new columns/a snapshot mechanism.
- Also flagged, not fixed: `TRANSLATED_HELPERS`' PT `red_6` entry (dead-code strategy dict, unreachable) shows "Conta até 20" instead of Self Hug — cosmetic, zero live impact.

**Two more real gaps found and fixed while honestly assessing "is this actually operational end-to-end" (Jono's explicit ask, not deferred — core to whether the feature works at all):**
- `unlock_creature_stage`'s check-in count queried `feeling_logs.student_id` by the caller's **account** id, but `feeling_logs.student_id` is always a real `students.id` (confirmed live, zero overlap between the two id spaces) — this endpoint had zero UI callers before tonight, so the bug was invisible; wiring it into a real button would have made it always say "do 5 more check-ins" forever, no matter how many a student actually did. ✅ Fixed & live-verified: uses `real_student_id` when the caller sends one (Mateus's real 3-of-5 recent check-ins correctly returned "2 more needed", was previously always "5 more" regardless of real activity).
- `creature_unlocks` was keyed only by the submitting **account's** `student_id` — meaning a teacher's whole classroom, or a multi-child parent's kids, shared ONE progress bucket per creature. Not an edge case — the common case for teacher accounts. ✅ Fixed: new `real_student_id`-keyed lookup/insert path (`_creature_unlocks_for()` helper), same defensive try/except-fallback pattern as tonight's other migration-dependent work, applied consistently across `/creatures/unlock`, `/creatures/eligible`, and a newly-added optional `student_id` param on `/creatures/my-unlocks` (also now strips peer creator identity, matching the `/creatures/eligible` privacy fix). **Live-verified the pre-migration fallback** (all three endpoints return clean 200s, no 500s) — full per-student persistence verification pending the migration below.

  **Migration required** (not run by me):
  ```sql
  ALTER TABLE creature_unlocks ADD COLUMN real_student_id text;
  ```
  **✅ Migration ran 2026-08-21.** Live-verifying per-student persistence surfaced a SECOND real bug: the table's pre-existing unique constraint `creature_unlocks_student_id_creature_id_key` is on `(student_id, creature_id)` — `student_id` is always the account id, so this constraint predates `real_student_id` and still only allows ONE row per account per creature, regardless of which real student it's for. Confirmed live: two students (Emma/Liam) under one teacher account, same creature — Emma's insert succeeded, Liam's insert 500'd (`23505 duplicate key value violates unique constraint`, full traceback captured via `railway logs`). Fixed the immediate danger — an unhandled 500 that would have silently corrupted the wrong student's row if retried naively — by returning an honest 409 instead of attempting a workaround. The **actual fix** needs one more migration (not run by me):
  ```sql
  ALTER TABLE creature_unlocks DROP CONSTRAINT creature_unlocks_student_id_creature_id_key;
  CREATE UNIQUE INDEX creature_unlocks_unique_progress ON creature_unlocks (COALESCE(real_student_id, student_id), creature_id);
  ```
  This keeps old (pre-migration) rows unique per account as before, while new real_student_id-keyed rows become unique per actual student — which is what makes two students on one account able to hold independent progress on the same creature at all.

  **✅ Migration ran 2026-08-21, live-verified end-to-end.** First re-test still 409'd; `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='creature_unlocks'::regclass` confirmed the old constraint was genuinely gone, so the 409 wasn't the old constraint — re-tested immediately after and it passed clean (transient propagation, not a real ongoing issue). Full two-student isolation test against real production data (Emma/dstu001 + Liam/dstu002, same teacher account, same creature): both got independent rows, Emma progressed stage 1→2 while Liam correctly stayed at stage 1 throughout, confirmed via `/creatures/unlock`, `/creatures/eligible` (per-student `my_stages_unlocked`), and `/creatures/my-unlocks` (no peer identity in the response). Test rows and session cleaned up afterward — creature feature is genuinely done.

### Creature scope-preference toggle + expiry-aware history — 2026-08-21 — ✅ built, migration-pending
Both explicitly deferred earlier tonight ("skip for tonight, log as fast-follow"), then explicitly reversed and approved for tonight ("this is core to whether the feature is genuinely operational, not a refinement"). Built with the same defensive-fallback pattern as everything else migration-dependent tonight — nothing breaks before the migrations run (live-verified: `/creatures/eligible`, `GET .../creature-scope-pref` both 200 cleanly, `PUT .../creature-scope-pref` returns a clean 400 "not available yet" instead of a 500, `/creatures/my-unlocks` unaffected).

- **Scope-preference**: `students.creature_scope_pref` (new column), `GET`/`PUT /students/{id}/creature-scope-pref`. `/creatures/eligible` now returns `{creatures, scope_pref, has_classroom}` and narrows results to the stored preference — the preference can only narrow real eligibility, never widen it. Note: there's no separate "family" visibility_scope value in the data model (only classroom/school/global exist) — the picker reuses "classroom" as the closest real match, labelled "My Family" when the student has no classroom (a home-only family member) vs "My Classroom" when they do, rather than inventing a new scope value tonight.
- **Expiry-aware history**: new `creature_unlocks` columns (`creature_name_snapshot`, `emotion_colour_snapshot`, `stage_image_snapshot`, `was_featured`, `featured_until_snapshot`), captured once at the exact moment a student fully evolves a creature (stage 4). `student/creatures.tsx` prefers the snapshot over the live join once one exists, shows a "⭐ Limited Edition (expired)" badge when applicable. **Known residual gap, not fixed tonight**: `creature_unlocks.creature_id` has `ON DELETE CASCADE` to `creature_submissions` — actual deletion of a creature (not just a scope/feature-status change) still wipes its unlock history entirely, snapshot or not. No delete-creature UI exists anywhere yet, so practical risk is low, but this is a real gap in "history survives" if that ever gets built.

  **Migrations required** (not run by me):
  ```sql
  ALTER TABLE students ADD COLUMN creature_scope_pref text DEFAULT 'any';
  ALTER TABLE creature_unlocks ADD COLUMN creature_name_snapshot text;
  ALTER TABLE creature_unlocks ADD COLUMN emotion_colour_snapshot text;
  ALTER TABLE creature_unlocks ADD COLUMN stage_image_snapshot text;
  ALTER TABLE creature_unlocks ADD COLUMN was_featured boolean DEFAULT false;
  ALTER TABLE creature_unlocks ADD COLUMN featured_until_snapshot timestamptz;
  ```
  **✅ Both migrations ran 2026-08-21, live-verified end-to-end against real production data.**
  - Scope-preference (Mateus, real Kairos student): default `any` showed both Waterman [global] + Waterman 2 [school]; set to `global` → only Waterman; set to `school` → only Waterman 2; restored to `any` afterward.
  - Expiry snapshot (Liam/dstu002, 4 real unlock calls to reach stage 4 on the currently-featured Waterman): DB row confirmed `creature_name_snapshot: "Waterman"`, `emotion_colour_snapshot: "blue"`, `stage_image_snapshot` = real stage4 URL, `was_featured: true`, `featured_until_snapshot: 2026-08-31...` — all correct, and `/creatures/my-unlocks` returns them with no peer identity leaked. Test rows, sessions, and Mateus's preference all cleaned up/restored afterward.

### Creature unlock check-in colour-filtering bug — 2026-08-21 — ✅ DONE & LIVE-VERIFIED
Found via a direct investigation question, not a routine sweep: Jono asked what happens once a student fully evolves one creature, which surfaced that `unlock_creature_stage`'s check-in count had **no colour filter at all** — it counted every recent check-in regardless of colour toward whichever creature was being unlocked, so a blue check-in could progress a red creature. Confirmed the default per-colour creature system already does this correctly (`add_points`, `server.py` ~line 3158, routes each check-in's own `feeling_colour` to the matching creature via `FEELING_COLOUR_MAP`) and mirrored the same real pattern: `feeling_logs` is now filtered by `feeling_colour == creature.emotion_colour` before counting. Also closed a latent related gap: the creature record is now fetched unconditionally (previously only inside the `real_student_id` branch), so an invalid `submission_id` now correctly 404s regardless of caller.

**Live-verified** against a real student (Emma/dstu001, real 30-day check-in mix: blue=5, red=10, yellow=10, green=10 — 35 total): unlocking the real global "Waterman" (blue) creature correctly capped at stage 1 (needs 5 more blue-only check-ins for stage 2, despite 35 total check-ins available), while a temporary test red creature independently progressed to stage 2 using only its own 10 red check-ins — neither leaked into the other. Pre-fix, the same 35 total check-ins would have wrongly qualified *both* creatures for stage 4 regardless of colour. Test creature, unlock rows, and session all deleted afterward — production restored to its exact prior state.

### Collapse/expand UX pass — 2026-08-21 — ✅ DONE (portal only needed changes)
Parent portal Family Strategies (colour groups collapsed by default, tap to expand) and Recent Check-ins (grouped by date, today expanded, other dates collapsed) built via a new shared `toggleCollapse(id)` helper, reusing the existing `toggleSASchoolDetails` pattern. Same date-grouped collapse applied to the shared student-detail modal (`renderSDLogs`) used by teacher/school_admin/superadmin alike, covering all three admin surfaces in one fix. Verified: app already had this correctly built in 5 places (parent family-strategies.tsx, teacher/parent check-in history sections, portal teacher check-ins) — nothing to add there. Deliberately not touched: superadmin/school_admin strategy managers (flat single-zone CRUD lists, not stacked multi-colour lists — the scrolling problem doesn't apply), school_admin's dashboard check-in card (aggregate/chart-only by design, no individual list exists). Portal JS syntax-checked post-edit (`node --check`), clean.

### ES/FR/DE/IT voice script finalization — 2026-08-21 — ✅ script fixed and verified; recording is a separate real-world task
Confirmed (via the earlier translation fork's cross-check) that ES/FR/DE/IT shared the exact same "Warm Drink" pre-rename drift PT had — fixed the same way: `id: b5`'s card name updated to each language's already-correct "Drink Water" phrasing (verified consistent with each language's own `strat_blue_2` key, which was already correctly renamed) across both live `HELPERS_ES/FR/DE/IT` blocks in `server.py`. Descriptions left as-is (same "not perfect for now" precedent as PT/EN). Full 24-concept table below, verified against real source (`HELPERS_PT/ES/FR/DE/IT` + `DEFAULT_HELPERS` in `server.py`), not translated from scratch. Actual audio recording is explicitly a real-world task for Jono and his kids — not blocked by tonight's build, since the language-availability badges in Settings already correctly show ES/FR/DE/IT as unavailable until real audio exists.

### Portuguese translation drift — 2026-08-21 — ✅ DONE & VALIDATED
Jono flagged 7 specific card-text mismatches plus a Settings i18n gap, all pulled from what he was actually seeing live on a real device, cross-checked against real source (not translated from scratch):
- Fixed 7 strategy-card name mismatches (Favourite Song→"Música Preferida", Tell Someone→"Contar a Alguém", Drink Water→"Beber Água", Keep Going!→"Continua Assim!", Try Something New→"Experimentar Algo Novo", Body Shake→"Abanar o Corpo", Self Hug→"Abraçar-te a Ti Mesmo") — applied to both live copies of `HELPERS_PT` in `server.py` (confirmed duplicated, in-sync) and the matching `strat_*` keys in `pt.json`, including a second, separately-live key family used by teacher-side strategy assignment (`strat_favourite_song`, `strat_tell_someone`).
- Settings' Security/Voice sections were already using `t()` with English fallbacks but the PT keys were simply never added (also confirmed absent from ES/FR/DE/IT); Danger Zone had **zero** i18n at all, fully hardcoded English. Added all missing PT keys, wired Danger Zone through `t()` for the first time.
- **Confirmed the exact same "Warm Drink" pre-rename staleness exists in ES/FR/DE/IT too** (`id: b5` still reads as a literal warm-drink phrase in all four, never updated when EN renamed this concept to "Drink Water") — same bug class as the PT one just fixed. Flagged, not fixed tonight — a real follow-up item whenever Jono's ready to do the other 4 languages properly.
- Validated: `py_compile` clean, `json.tool` clean on `pt.json`, `tsc` clean on `settings.tsx`.

### Queued for a future session — strategy/check-in collapse-by-default UX pass
Not started. Jono's ask: parent portal's Family Strategies colour groups should start collapsed (tap to expand); check-ins should default-expand only for today's date, collapsed for all other dates; same collapse/expand pattern applied to the equivalent teacher/school_admin/superadmin views across both app and portal, reusing existing collapse UI already in the codebase (e.g. `SchoolsManager`'s expand/collapse pattern in `admin/dashboard.tsx`, or the school contact-details expand/collapse just built in the portal) rather than building new. Explicitly lower priority than the builds — pick up after.

---

## SUGGESTED EXECUTION ORDER

1. **L1** — ✅ DONE 2026-08-18 — hardcoded bypass removed, verified against real login. Not yet deployed (no push/rebuild done).
2. **L2 + L3 together** — ✅ DONE & VERIFIED 2026-08-18. Deployed, tested end-to-end against Stripe test mode (checkout → webhook → subscription_status/plan), and a real pre-existing webhook bug (`.get()` on Stripe objects) was found and fixed in the same pass — see both sections above for full detail. Remaining: Jono to create the 3 school Stripe products/Payment Links (manual sales flow, not blocking), and upload the corrected website copy to cPanel.
3. **L4** — ✅ DONE 2026-08-18 — school identity backfill + dual-match audit, verified live.
4. **A1** — ✅ DONE 2026-08-18 — dead duplicate routes merged/removed, verified, not yet deployed.
5. **A4** — ✅ VERIFIED RESOLVED 2026-08-18 — re-checked against the live portal.html, no code change needed.
6. **A2 item 1** — ✅ DONE 2026-08-18 — legacy admin.html retired (closed A5's admin.html instance, resolved A6's cross-surface conflict). A2 items 2-3 (Wellbeing Tracker/Team/Services app parity, Settings tab decisions) still open — Jono's product call.
7. **A3** — ✅ DONE 2026-08-18 — backend/portal confirmed complete and live-tested, no build needed. Mobile-parity question left open, not urgent, Jono's product call whenever.
8. **A9** — ✅ DONE 2026-08-18 — teacher fix (add missing type filter) and parent fix (build missing fetch) both applied and live-tested against production with real teacher/parent accounts. Not yet deployed; on-screen render not visually confirmed (no simulator available this session).
9. **A7** — ✅ DONE 2026-08-19 — frontend TSX/TS sweep complete (shared constants file + all genuine zone-colour declarations across `frontend/app`+`frontend/src`, see A7 section). Backend Python dict + portal.html remain out of scope for this item, tracked separately.
10. **A5** — ✅ DONE 2026-08-19 — portal.html audit complete, same unescaped-innerHTML pattern confirmed in all 4 requested areas (Users, Schools, Alerts, Wellbeing Tracker) plus an attribute-context escaping gap, fixed with a shared `esc()` helper. Not yet deployed — dated upload copy left on Desktop for Jono to push to cPanel.
11. **A8** — PDF gating — family PDF stays ungated by decision (see L3); still open for school-overview/classroom-overview routes.
12. **A6** — ✅ DONE 2026-08-20 — turned out to be a live privilege-escalation path, not just dead-code cleanup: removed the self-serve `admin` role entirely (4 grant paths, ~20 endpoints treating it as superadmin-equal), then found and removed two more severe, previously-unaudited self-serve endpoints (`/auth/promote-superadmin`, `/auth/promote-school-admin`) plus the app UI exposing them to every user. Both rounds live-verified against production.
13. **A11** — portal fragility — standing risk, plan separately, no fixed timeline.
14. **A12** — doc correction only.
15. **A13** — cleanup, anytime.
16. **P1** — do not schedule; revisit after launch.
17. **A14** — ✅ DONE 2026-08-19 — parent My Wellbeing crash fixed, live-tested and confirmed by Jono on a real device. Found during real-device testing of A9, not part of the original review.
18. **A15** — ✅ DONE 2026-08-19 — "most used strategies" raw-UUID display fixed, live-tested against real production check-in data. Same discovery session as A14.
19. **A16** — ✅ DONE & LIVE-VERIFIED 2026-08-20 — creature submission code now validated before the full flow, not after. Confirmed live against production with both a real generated code and a garbage one.
20. **A17** — ✅ DONE 2026-08-20 — admin PIN race condition fixed (reads session token fresh instead of racy state), all 4 outcome paths verified against the real backend contract, including re-confirming L1's security fix still holds alongside it.
21. **A19** — ✅ DONE 2026-08-20 — root-caused (A17's own side-finding was the exact cause, not a regression), real PIN check restored on top of the existing session+role gate, deployed and verified against 9 live-production scenarios. Real (non-demo) school_admin accounts intentionally left role-only (Path 1) — a per-school PIN is a separate future feature.
22. **A18** — ✅ DONE 2026-08-19 — root-caused to a query-param mismatch on the parent "My Wellbeing" screen (not the family dashboard as first suspected), fixed by sending the already-computed `days` param instead of unread `start`/`end`, live-verified all 4 range pills against production.

### A20 — iPad tablet polish — 2026-08-21 — audited, deliberately NOT built tonight
Jono asked whether the app supports iPad, both at the config level and the real UI level. Findings, verified against real source:

- **Config-level iPad support already exists**: `app.json` has `"supportsTablet": true` (ios) — the app is already installable/runnable on iPad today, this was never actually iPhone-only at the build level.
- **Real UI risk on iPad: Medium** — "stretched phone app," not broken/crashing. Nothing structurally fails, but nothing was designed for the larger screen either:
  - **`Dimensions.get('window')` read once at module load, not reactive** — used in 15 files (`student/zone.tsx:12`, `parent/dashboard.tsx:80`, `teacher/dashboard.tsx:17`, `teacher/student-detail.tsx:34`, `parent/widget.tsx:13`, `kiosk/index.tsx:12`, `src/utils/globalStyles.ts:7`, `src/components/CelebrationOverlay.tsx:7`, others) to derive single-column, full-bleed element sizes (e.g. `teacher/dashboard.tsx:652` sizes a chart as `width - 80`). On iPad this doesn't break, it just balloons proportionally with no redesign to use the extra space. Only 3 of 43 screens use the reactive `useWindowDimensions` hook instead (`parent/my-wellbeing.tsx`, `parent/family-member-stats/[id].tsx`, `teacher/checkin.tsx`).
  - **Zero tablet-aware code anywhere** — confirmed via grep, no `Platform.isPad`/`isTablet` hits, no screen branches its layout for a bigger display.
  - **Modals have no `maxWidth` cap** — bottom-sheet style throughout (e.g. `teacher/student-detail.tsx:1427-1432`'s `modalContent`), stretches edge-to-edge (~1024pt) on iPad instead of centering at a sensible width.
  - **Creature grids fixed at `numColumns={2}`, no conditional** — `student/world-creatures.tsx:163`, `student/creatures.tsx:150` (both from tonight's creature-feature work) — on iPad this means 2 oversized cards with wasted whitespace where 4-5 columns would suit the screen.
  - **Safe-area handling is solid** (`useSafeAreaInsets`/`SafeAreaView` in 41 of 43 screens) — not an iPad-specific gap, ruled out as a concern.
  - **`"orientation": "portrait"` is set globally** in `app.json` — locks both iPhone and iPad to portrait-only. Worth an explicit product decision on whether iPad should support landscape, separate from the layout-polish work above.

**Explicitly deferred, not built tonight** — genuinely a separate session per Jono's call. For tonight's App Store submission: iPad screenshots left blank, shipping iPhone-only visuals; iPad users can still install and use the app (it isn't blocked), it just won't look tablet-native until this is scoped and built.

### A21 — default + community creature systems unified, one-active-per-colour rule — 2026-08-21 — ✅ DONE & LIVE-VERIFIED
Real bug found by live testing (Jono's daughter): fully evolving the default "Aqua Buddy" then tapping "Start" on a community creature appeared to work, but check-ins still fed Aqua Buddy, and going back into World Creatures showed no trace the creature had ever been started. Root cause, confirmed by reading `add_points` in full: `target_creature = FEELING_COLOUR_MAP.get(feeling_colour)` was hardcoded to the 4 default creatures — there was no code path anywhere for a community creature to ever become what check-ins feed into.

**The rule this settles** (explicitly reversing the earlier "can a student progress multiple creatures at once" finding from earlier tonight — the real answer is no): one active creature per colour; a student may only choose a new one once their current active creature for that colour is fully evolved; progress on the non-active one is blocked server-side, not just hidden in the UI.

Built:
- New `students.active_creatures` (JSONB, one slot per colour — holds either a default creature id or a community creature id).
- `add_points` now branches per colour: default ids keep the original points-based path unchanged; community ids route through a new shared `_progress_community_creature()` helper (extracted from `unlock_creature_stage`, removing ~80 lines of duplicated logic) using the real check-in-count mechanic.
- New `POST /creatures/start/{id}` — a genuine selection action (not progress), gated on eligibility + the current active creature for that colour being fully evolved first.
- `unlock_creature_stage` now also enforces "must be your active creature" server-side — closes a bypass where the endpoint could be called directly to progress a non-active creature.
- New `GET /students/{id}/my-creatures` — the real permanent "My Creatures" collection (Pokedex-style): every creature ever started/evolved, default or community, grouped by colour, real total-collected count. A creature leaving "active" status is never removed — progress rows are never deleted, so its final state (or the expiry-snapshot from earlier tonight) just stays as permanent history.
- New `GET /students/{id}/active-creatures` for frontend gating.
- Frontend: new `CommunityCreatureDisplay` (photo-based) alongside the existing emoji/animation-based `CreatureDisplay`; `rewards.tsx` branches between them. `world-creatures.tsx`'s interaction model changed from "tap to manually progress" to "tap to set as active" (progress now automatic via check-ins, matching how defaults always worked). `creatures.tsx` rewritten as the one real "My Creatures" screen — 4 collapsible colour sections (collapsed by default, matching the app's existing pattern), real total-collected count, a direct "🔍 Find a New Creature" shortcut (per Jono's follow-up: browsing/starting shouldn't require going through check-in first). Consolidates what used to be **three** separate, inconsistent "My Creatures" surfaces (a defaults-only modal on the reward screen, this screen's earlier community-only version, and nothing unified) into one.
- Two smaller real bugs caught and fixed in the same pass: the Bronze Shield badge could visually overlap the bouncing creature (creatureSection's greedy `flex: 1` bounded to a fixed `minHeight`), and the Continue button's background colour silently broke (`undefined`) for community creatures, which have no `color` field — now falls back to the real `EMOTION_COLOURS` palette.

**Live-verified end-to-end against real production data** (Mateus, a real Kairos student who already had Aqua Buddy [blue] and Blaze Heart [red] fully evolved from real usage) — full 7-step verification, all correct: starting Waterman persisted and was immediately readable back; My Creatures showed Aqua Buddy (frozen, `is_active: false`) alongside the new Waterman (`is_active: true`) with nothing disappearing; a real check-in correctly routed to Waterman while Aqua Buddy's points stayed frozen; attempting to start a second blue creature while Waterman was incomplete was correctly rejected (400); attempting to directly progress that second creature via `/creatures/unlock` was correctly rejected too (403, confirms the bypass-closing gate); `/creatures/unlock` still worked normally on the genuinely-active creature. All test state reverted to Mateus's original values afterward.

  **Migration required** (ran 2026-08-21, no DDL access from this environment):
  ```sql
  ALTER TABLE students ADD COLUMN active_creatures jsonb
    DEFAULT '{"blue":"aqua_buddy","green":"leaf_friend","yellow":"spark_pal","red":"blaze_heart"}'::jsonb;
  ```

### A22 — Nunito never actually wired into the mobile app — 2026-08-22 — found during i18n scoping, logged as a real future task, not urgent
Found while scoping Hindi/Chinese/Arabic font support: "Nunito" is the real, correctly-loaded brand font on the web portal (`portal100.html:8`, real Google Fonts CDN link) — but on mobile, no `.ttf`/`.otf` file is bundled anywhere in the repo, no `expo-font`/`useFonts` call exists, and `fontFamily: 'Nunito'` appears in exactly one screen (`app/auth/forgot-password.tsx:194`) with nothing registered behind it. The mobile app has been running entirely on the OS system font (San Francisco/iOS, Roboto/Android) this whole time. Nothing is broken — text renders fine — but this is a real, unaddressed brand-consistency gap, arguably bigger than any single new-language addition. Not built tonight; logged as its own future task, separate from the Hindi/Chinese/Arabic i18n work.

### A23 — Full i18n scoping pass: Hindi/Chinese/Arabic support — 2026-08-22 — investigation complete, real numbers below
Jono asked for a real scoping pass before building anything, across translation scope, font support, RTL readiness (Arabic-specific), and effort ranking. Full findings, verified against real source:

**Translation scope — bigger than "24 helpers + 4 zones," and en.json is NOT the reference file.** Union of all keys across the 6 existing language files: **742 unique keys**. `pt.json` is the real near-complete master (718/742, 96.8%) — `en.json` has only 200 keys and is missing 542 of the full set. Real per-language gaps against the pt.json master, confirmed by direct key-diff:
  | Language | Keys present | Missing vs 718-key master |
  |---|---|---|
  | es.json | 687 | 32 |
  | fr.json | 327 | **414** |
  | de.json | 327 | **414** |
  | it.json | 327 | **414** |

  FR/DE/IT (all shipped last night) are missing the identical 414 keys each — a real, substantial gap, not evenly small like ES. Root cause of why this wasn't caught earlier: 60% of the app's 1,006 `t()` calls use the `t('key') || 'hardcoded English fallback'` pattern, so a missing key silently shows baked-in English instead of an obviously-broken placeholder.

**Confirmed real bug, not a false alarm**: the creature-system screens built in the previous session (`rewards.tsx`, `world-creatures.tsx`, partially `creatures.tsx`) use hardcoded English strings for "Submit a Creature", "World Creatures", "Set as Active", etc. — zero `t()` wrapper, even though the translation keys (`submit_creature`, `world_creatures`) already exist in most language files. These are the exact screens genuinely untranslated in every shipped language right now.

**Font support**: Nunito has no Devanagari/CJK/Arabic glyphs (confirmed, Latin/Cyrillic/Greek-extended only). OS-level automatic font substitution (CoreText on iOS, system fallback on Android) means unsupported scripts render correctly today regardless — no tofu/blank boxes, just an OS-default look rather than a branded one. Recommended real fallback fonts: *Baloo Devanagari 2* (Hindi), *Noto Sans SC* (Chinese — the honest practical choice, not a stylistic compromise; genuinely playful full-coverage CJK fonts don't really exist for body text), *Baloo Bhaijaan 2* with *Cairo* as a smaller-size-legibility fallback (Arabic).

**RTL readiness (Arabic) — zero infrastructure exists, large real effort**: `I18nManager` has 0 references anywhere in 68 app files. ~187 hardcoded LTR-assuming style declarations (58 `marginLeft`, 44 `marginRight`, 23 `borderLeftWidth`, ~46 `left`/`right` position props, 13 hardcoded `textAlign`). ~54 directional icons (back arrows/chevrons) that RN never auto-mirrors. Concentration worst exactly where the real content lives: parent screens 100% of files affected, teacher screens 91%. `flexDirection: 'row'` (used 384 times) would auto-mirror correctly with zero `row-reverse` landmines found — but only for the ~56% of files with no competing hardcoded left/right.

**Effort ranking**: confirmed Jono's assumption that Hindi ≈ Chinese (both translation-volume-bound, real font to bundle, zero structural RTL work — medium effort). Arabic is a materially larger, separate-category effort: same translation lift plus the full RTL pass above, starting from zero existing infrastructure, concentrated in the highest-traffic screens.

**Hindi/Chinese voice-recording tables**: could NOT be produced the way ES/FR/DE/IT were — those languages had real, pre-existing human-translated JSON files to read from; `hi.json`/`zh.json`/`ar.json` don't exist anywhere in the repo (confirmed directly). Jono is sourcing real Hindi/Chinese speakers separately; app-side technical groundwork (placeholder JSON structure, hardcoded-string cleanup, fallback fonts) proceeding in parallel — see below.

### A23 follow-up 1 — FR/DE/IT gap-fill — 2026-08-22 — ✅ DONE, machine-drafted/unverified, native review still needed
Follow-up finding: checked es.json specifically and confirmed it's in much better shape (32 missing vs the master) than fr/de/it (414 missing each, identical set). Jono chose speed over waiting for native speakers, given this affects builds already sitting in App Store/Play Store review — drafted all 414 keys myself (cross-referencing pt.json + es.json as real source material, both Romance languages giving solid grounding for translating into FR specifically, plus DE/IT), clearly flagged as machine-drafted/unverified, same disclaimer pattern as the encouragement phrases from two nights ago.

**Explicitly flagged for priority native review** (safety/help-seeking language, most consequential to get exactly right in a children's wellbeing app): `wellbeing_alert`, `share_with_wellbeing`, `request_support`, `checkin_saved_shared`, `alert_sent`/`alert_sent_desc`, `share_wellbeing`, `keep_private`/`private_message_note`, `zone_alert`, `mark_resolved`, `disclaimer_1`/`disclaimer_2`/`disclaimer_3` (the clinical/professional-referral disclaimer), `red_zone_name`/`red_zone_desc`, `strat_safe_space_name`/`_desc`, `strat_no_teaching_name`/`_desc`, `strat_reconnect_name`/`_desc`, `strat_repair_name`/`_desc`, `ask_for_cover`/`_desc` (teacher crisis-coverage request).

**Real, non-obvious build-timing finding** (confirmed against source, not assumed, per Jono's explicit ask): this project has **no OTA update capability** — `expo-updates` isn't installed, no `channel` config in `eas.json`. The frontend JSON files are compiled into the JS bundle at `eas build` time, so editing `fr.json`/`de.json`/`it.json` alone would NOT reach iOS build #20 / Android versionCode 3, already sitting in review. **But** `AppContext.tsx`'s `fetchTranslations` (`:424-441`) merges `{...backendData, ...localTrans}` — local (bundled) keys win where they exist, but for the 414 keys genuinely *absent* from the already-bundled local files, the live backend's values survive the merge. So the fix was applied in **both** places: the frontend JSON files (correctness for future builds) **and** the backend's `TRANSLATIONS` dict in `server.py` (`fr`/`de`/`it` sub-dicts, live on Railway, deployed immediately) — the second one is what actually closes the gap for the builds already in review, without needing a new build. Verified via AST-parsing the live `TRANSLATIONS` dict post-edit: fr 485 keys, de/it 476 keys each (up from 81/67/67).

### A23 follow-up 2 — creature-screen hardcoded strings — 2026-08-22 — ✅ DONE
Confirmed real, not a false alarm: `rewards.tsx`'s "Submit a Creature"/"World Creatures" buttons and `world-creatures.tsx`'s "Set as Active"/title/subtitle/"Fully evolved!"/active-hint text were all hardcoded English, zero `t()` wrapper — even though `submit_creature`/`world_creatures` already had real translations in most shipped languages. `creatures.tsx`'s header was correctly wired; its action buttons and status text weren't. Fixed all of it: reused the real existing `submit_creature`/`world_creatures`/`my_creatures` keys where they already existed (immediate real-language improvement, no fabrication), and added a small set of genuinely new keys for concepts that didn't exist before yesterday's creature-system build (`set_as_active`, `find_next_creature_subtitle`, `find_new_creature`, `fully_evolved`, `active_badge`, `creatures_collected`, `limited_edition`/`_expired`, `finish_creature_first`, `active_checkin_hint`, `stage`) — added to `en.json` only, with graceful `t('key') || 'English fallback'` per the app's existing convention, since drafting NEW translated content for these wasn't in scope of this specific fix. Also closed a small pre-existing gap: `en.json` itself was missing `my_creatures` (only had `my_creatures_title`), causing the fallback to silently trigger even in English.

### A23 follow-up 3 — Hindi/Chinese technical groundwork — 2026-08-22 — ✅ structure ready, deliberately not user-facing yet
Created `hi.json`/`zh.json` with the exact same 718-key structure as `pt.json` (the real master), values = English placeholder text (`en.json`'s value where one exists; an explicit `"[NO EN SOURCE YET — key: X]"` marker for the 518 keys that have no EN source at all — these need their EN meaning established, likely from screen context, before Hindi/Chinese translation can even start). Registered in `translations/index.ts` with a clear comment. **Deliberately NOT added to `settings.tsx`'s `LANGUAGES` picker** — exposing these now would show English text under a "हिन्दी"/"中文" label, worse than not offering the language at all. Wire into `LANGUAGES` once Jono's native speakers deliver real content.

**Not yet done, explicitly out of scope for this pass**: fallback font bundling (Baloo Devanagari 2, Noto Sans SC) and the Nunito-on-mobile fix (A22) — next.

**⚠️ Superseded below** — Jono reversed this call shortly after and asked for a fresh build including the creature-screen fix (see next entry). Leaving this note for the historical record of the decision sequence.

### A23 follow-up 4 — build decision reversed, ES gap closed, iOS #21 / Android v4 cut — 2026-08-22
Jono decided to cut a new build now rather than wait for Hindi/Chinese/Arabic content, specifically to include the creature-screen `t()` fix. Before cutting, did one more full parity pass across all 6 shipping languages (explicitly requested "clear yes/no, no known gaps" check) and found ES was still missing 32 keys (same Settings security/voice/danger_zone cluster gap as PT/FR/DE/IT had before last night's fixes, just never applied to es.json) — closed it with real drafted Spanish. Also closed `wellbeing_private`, the one remaining gap in FR/DE/IT/EN, using an already-verified value found in the backend `TRANSLATIONS` dict rather than drafting fresh.

**Final parity, confirmed by direct key-diff**: FR/DE/IT show **zero real gaps** (excluding the 9 brand-new creature-screen-only concepts that don't exist as translated concepts for any language yet — `set_as_active`, `find_new_creature`, etc., intentionally English-fallback everywhere). ES/PT share a 23/24-key residual gap that PT (the actual master file) has itself — pre-existing, not introduced this session, out of scope.

Hindi/Chinese groundwork (`hi.json`/`zh.json`) included in this build — confirmed safe since they're not wired into `settings.tsx`'s `LANGUAGES` picker, so unreachable by any real user regardless of being in the compiled bundle. Font bundling (Baloo Devanagari 2, Noto Sans SC) and Nunito-on-mobile (A22) deliberately left for later — moot until those languages are actually selectable.

### 2026-08-22 — end of session, final status
- **iOS**: build #21 (commit `13d711fb`, confirmed via `git merge-base --is-ancestor` to genuinely include the creature-screen `t()` fix — not just requested, verified) uploaded and processed in App Store Connect. Build #19's pending Beta App Review was withdrawn by Jono directly in the ACC UI ("Remove from Review" on the build's detail page — confirmed this is a real, working option, no version bump/rebuild needed). #21 submitted for external review successfully.
- **Android**: versionCode 4 built (same commit, same fixes). Jono manually uploaded to Play Console himself and updated the Android Linktree link to versionCode 4's artifact. Play Console identity verification status unknown from this end — no tool access to check, last known state was still pending as of this morning.
- **App Store Connect listing/description**: Jono asked whether the app description needs updating for tonight's changes. Could not check — no local copy of App Store metadata exists anywhere in this repo (`dist/metadata.json` is a build asset manifest, not listing content), and no App Store Connect access from this environment. Real judgment call left to Jono: tonight's work was a bug fix (creature system) and translation work, not new user-facing functionality, so a description rewrite is likely unnecessary — but only Jono can confirm against the actual live text.
- Both platforms now genuinely in review with the correct, verified builds. Stopping point for this session per Jono's explicit call.

### A24 — World Creatures / My Creatures real-usage fixes — 2026-08-22 — built, NOT deployed, awaiting Jono's own Expo Go verification before build #22
Live-tested on Expo Go by Jono (reflects current code directly). Found the real architecture gap: `student/select.tsx`'s "My Creatures" button already existed on each student's card, in the right place — it was just wired to the old, disconnected defaults-only `CreatureCollection` modal instead of the real unified screen. Small, surgical fix: repointed it to `/student/creatures` (setting `currentStudent` first, matching the existing card-tap pattern), and removed the now-dead modal state/import.

Also found a real regression from last night's Bronze Shield fix: `rewards.tsx` had no `ScrollView` anywhere — removing `creatureSection`'s `flex:1` (to fix the overlap) meant content could exceed screen height with the Continue button pushed off-screen, unreachable. Fixed by wrapping the scrollable content in a `ScrollView`, action buttons pinned outside/below it so the primary CTA is always reachable.

`world-creatures.tsx` fully rebuilt:
- Real bug fix: the screen never called `navigation.setOptions({headerShown:false})` — the default stack header (showing the raw route name, Jono's "studentworld-creatures") was rendering above the custom one. Now uses `TranslatedHeader` (the app's established pattern, already used elsewhere) — fixes the heading bug and adds the missing COH logo + home button in one move.
- New: each card auto-cycles through its stage1-4 images as a preview (1.4s interval), clearly labelled "👀 Preview" and never touching the real, separate progress-dot row below it — a student can't mistake the cycling animation for having actually obtained the fully-evolved creature. Preview stops once the creature is genuinely fully evolved (shows its real final stage).
- New: 2x2 grid per screen (`PAGE_SIZE = 4`), horizontal paging via a `pagingEnabled` `FlatList` of pages when a colour has more than 4 creatures — replaces the old single vertically-scrolling grid.
- New backend fields on `/creatures/eligible`: `featured_until` (real expiry, only set when a creature is currently featured — same join pattern as `/creatures/global`), and `country` — **global-scope creatures only**, gated behind the exact same real 5-contributor privacy threshold already used on the superadmin dashboard (`portal100.html`'s `MIN_THRESHOLD = 5`), computed across the full global population so the anonymity set is real, not just this one student's eligible subset.

**Explicit hold, per Jono's instruction**: build #21 stays in review untouched. Everything above is committed but genuinely not yet verified visually — Jono is testing each item live on Expo Go himself before deciding to cut build #22. Known open risk not yet resolved: card sizing for the 2x2 grid is computed from screen width only; whether 2 rows genuinely fit without vertical clipping on a short device hasn't been confirmed on a real screen.

### A24 follow-up — real double-heading root cause found, more Expo Go findings fixed — 2026-08-22
The double-heading bug persisted after the first fix attempt because the real cause was never in the component at all: `app/_layout.tsx`'s central `<Stack>` explicitly lists every student screen with its own `headerShown: false` override (`student/rewards`, `student/creatures`, `student/submit-creature` all have real entries there) — but `student/world-creatures` was never added to that list, so it silently fell back to the global default (`screenOptions`, headers shown, with a route-derived title). Fixed at the actual source: added the missing `<Stack.Screen name="student/world-creatures" options={{headerShown:false, title:'World Creatures'}} />` entry, matching its siblings exactly.

Also fixed: the reward page's redundant "Submit a Creature" + "World Creatures" two-button row (redundant now that My Creatures already surfaces both) collapsed to one "Find World Creatures" button; added a dismiss ✕ to the Bronze Shield badge (top-right, matching the app's established dismiss pattern) so a student can clear it and just see their creature; brought back a gentle idle bounce animation on World Creatures cards, echoing the old collection modal's more playful feel per Jono's explicit ask not to lose that polish in the redesign.

**Findings for items 5 & 6 — investigation only, no build, per Jono's explicit hold:**

- **Item 5 (country-anonymity threshold)**: confirmed Jono's instinct is correct — dropping the threshold to 1 contributor would eliminate the real k-anonymity protection the 5-contributor threshold exists for; a single-contributor country tag would let anyone who can otherwise narrow down "which student/school is in that country" directly identify the creator. A collapsible "creatures per country" leaderboard/graph respecting a real minimum threshold is genuinely buildable — it's the same aggregate-and-filter pattern the superadmin dashboard's existing `MIN_THRESHOLD = 5` world map already does (`portal100.html`), just needs a student-facing UI on the same real, already-proven computation. Not built — awaiting Jono's decision on threshold value and whether to proceed.
- **Item 6 (scope levels + submission-flow country/scope capture)**: confirmed only 3 real scope levels exist end to end — `classroom`, `school`, `global`. No "family" scope value exists anywhere in the codebase, not even as dead code (grepped specifically) — the "My Family" label students see is purely a UI relabeling of "classroom" for students without one, not a real 4th scope. Jono's belief was correct. Separately confirmed: **a teacher does not select a scope at submission time at all** — `submit-creature.tsx` has zero scope-related UI; `visibility_scope` is only ever set later, by an admin, at approval time (`/creatures/global-approve/{id}`, defaults to `"school"` if unspecified). **Country entry is also missing from the submission flow entirely** — `submit-creature.tsx` has zero country-related UI; the backend silently falls back to the submitting account's own stored profile `country` field (or `"PT"` if even that's missing) — never something the teacher actively enters or confirms at submission time.

### Urgent security fix — /creatures/approve and /creatures/reject had zero ownership check — 2026-08-22 — ✅ DONE & LIVE-VERIFIED
Found while investigating item 8. Confirmed real: any authenticated teacher/parent/school_admin could approve or reject ANY creature submission system-wide, not just ones at their own school — same severity class as earlier unauthenticated-endpoint findings, fixed immediately and separately from the rest of item 8's scoping. New shared `_can_approve_creature(user, submission)` check: superadmin/admin unrestricted (unchanged); teacher/school_admin scoped to their own resolved school; parent scoped to their own submissions only (`student_id == their account`) — the safest, most conservative reading given parents have no reliable school-affiliation field, pending a real product decision on whether parents should get broader same-school access (tracked as item 8(c) below).

**Real pre-existing data gap found while testing, not introduced by this fix**: `school_profiles` (the table the existing `/creatures/pending` teacher-scoping already depended on) is confirmed live to have exactly one unusable row (`school_admin_user_id: None`) — essentially empty in production. Using it as the *only* teacher-school resolution path would have silently locked out every real teacher, not just closed the security hole. Adjusted to fall back to the teacher's own `school_name` field (imperfect, but real and populated) when the "more correct" indirect lookup comes up empty.

**Live-verified against real production accounts and two temporary test submissions** (Kairos Montessori + Sunshine International School, both deleted afterward): a different-school teacher was correctly blocked (403) from a Kairos submission; the submitting parent correctly succeeded approving their own submission; a same-school teacher (Sunshine) correctly succeeded on their own school's submission; a different parent (not the submitter) was correctly blocked (403) rejecting the Kairos submission; superadmin correctly retained unrestricted approval authority. All 5 scenarios passed. Test data and sessions fully cleaned up afterward.

### Item 8 breakdown delivered, decisions made, (a)-(d) built + (e) label renamed — 2026-08-22 — ✅ DONE & LIVE-VERIFIED
Delivered the requested (a)-(e) breakdown as separate decision points (not one bundled feature). Jono's decisions: (a) build parent "my submissions" — smallest piece. (b) teacher "my submissions" = classroom-wide ("everything my classroom has submitted"), explicitly not just what the teacher personally logged. (c) fix the parent/school_admin overlap once (a) exists — parents should only ever see their own family's queue. (d) submitter-role tracking — cheap fix, go ahead. (e) rename the "Approved Creatures" card honestly to reflect what it actually shows, rather than rebuild it as school-scoped — a deliberately-built real school-scoped card is a separate future feature if wanted later.

**Built:**
- **`/creatures/pending`** (8c): parent branch split out from school_admin's — parents now see only their own family's pending submissions (`eq("student_id", user_id)`), not the full school queue. Teacher/school_admin/superadmin behaviour unchanged.
- **`/creatures/pending`** (8d): every row now annotated with `submitted_by_role` (`teacher`/`parent`/`unknown`) via a batch lookup against `users.role` keyed on `student_id` — no new column needed, since `student_id` already reliably holds the submitting account's id.
- **`/creatures/my-submissions`** (8a/8b): this endpoint already existed but was dead — the frontend's `creaturesApi.getMySubmissions()` had no caller anywhere. Rebuilt its logic and wired it in: parent gets their own submissions (any status); teacher gets everything their classroom(s) have submitted (`classroom_id` join against their own `classrooms` rows) plus a fallback for legacy rows with no `classroom_id` captured, deduplicated.
- **`CreatureManagement.tsx`** (shared parent/teacher component, used by `parent/creature-code.tsx` and `teacher/creature-code.tsx`): new "My Submissions" / "My Classroom's Submissions" card below the existing Pending Creatures moderation queue, with status badges (pending/approved/rejected) and rejection reason shown when rejected. Refreshes after an approve/reject action in the queue above it.
- **`portal100.html`** (8e): renamed the teacher/school_admin "✅ Approved Creatures" card to "🌍 Global Creature Feed", with a subtitle ("Every school's globally-available creatures — not just yours") stating plainly what it already shows. Label/copy only, per Jono's explicit instruction — no logic change, still calls the same `/creatures/global` endpoint. Not tracked in git (same as prior portal fixes) — written to `/Users/jono/Desktop/portal100-UPLOAD-2026-08-22.html` for manual cPanel upload.

**Real bug found live during verification, fixed immediately, unrelated to items a-d themselves**: `/creatures/pending`'s teacher branch never received the `school_profiles`-empty fallback that `_can_approve_creature` got during the Aug 22 security fix. Since `school_profiles` is confirmed empty in production, this endpoint's teacher branch always resolved `school` to `""` and silently returned zero pending submissions for every real teacher — the teacher-facing moderation queue has been showing nothing to review, full stop, for however long that gap existed. Fixed with the identical fallback pattern (`if not school: school = user.get("school_name")`).

**Live-verified against real accounts** (Kairos Montessori parent `user_750b8fb79312`, Sunshine International School teacher `demo_teacher_001`, second Kairos parent `user_534131df3ccb`, teacher's real classroom `demo_class_001`) using three temporary `creature_submissions` rows: parent A's `/creatures/pending` correctly showed only their own test submission, not the second parent's, with `submitted_by_role: "parent"` correctly annotated (8c + 8d). Parent A's `/creatures/my-submissions` correctly returned their own test row plus their real pre-existing "Waterman 2" submission (8a). The teacher's `/creatures/my-submissions` correctly surfaced a classroom-scoped test submission the teacher never personally made (submitted under the second parent's account, tied to `classroom_id: demo_class_001`) — confirms genuine classroom-wide visibility, not self-submitted-only (8b). After the fallback fix, the teacher's `/creatures/pending` correctly picked up the same submission too. All temporary submissions and sessions confirmed deleted afterward.

**Deferred, not folded into this build — logged as their own follow-ups per Jono's explicit instruction:**
- **Visual polish (from A24 follow-up)**: bringing the old `CreatureCollection` modal's real per-zone movement variety (sway/hop/shiver/bounce by colour, `CreatureCollection.tsx:40-72`), Bonus Items section, and side-by-side small-icon-list + large-focus layout into the unified `student/creatures.tsx` / `world-creatures.tsx` screens. Verified live: `creatures.tsx` currently has zero animation at all; `world-creatures.tsx` has one generic bounce (same for every colour), no bonus items, no side-by-side layout. Not urgent, wanted eventually per Jono.
- **Submission-flow scope + country capture (item 6 escalation)**: Jono decided this is a real gap worth fixing properly, not just a technicality — teachers/parents should be prompted to choose scope (Classroom/School/Global) and confirm/enter country at submission time, rather than scope silently defaulting at approval and country silently inheriting the submitter's profile. Gave an honest complexity read before building anything: this is its own pass, not an item-8-sized add, for four real reasons — (1) no scope-picker UI exists anywhere in the app yet; (2) scope options must be conditionally valid against the submitter's actual resolved classroom/school context, same logic as `world-creatures.tsx`'s `scope_pref` pills but not yet threaded into the submission flow; (3) a real data-format inconsistency needs resolving first — the only existing country-picker precedent (`COUNTRY_FLAGS` in `admin/dashboard.tsx`) stores full country names, while the creature system's `country` field is an ISO-2 code (falls back to `"PT"`, `server.py:7210`) — reusing that list as-is would silently break the already-shipped country-of-origin/threshold feature; (4) a genuine workflow decision, not just a form field — does the submitter's scope choice become final immediately, or a request the existing superadmin global-approve gate can still override? Not built. Awaiting Jono's go-ahead as its own scoped piece.

---

## A25 — the "last build before #22" round: items 1-8, 2026-08-22

Jono's framing: build everything needed so #22 is genuinely the last build for a while, not another #19→#21 situation. Explicit standing instruction reaffirmed mid-round: keep building/committing/deploying without waiting for sign-off at each step, just never cut a build without explicit permission.

### Item 1 — submission-time scope + country picker — ✅ DONE & LIVE-VERIFIED
Resolved the two things Jono asked to be resolved before building: (1) country format — new `frontend/src/constants/countries.ts`, ISO-2 codes (matches the creature system's existing `country` field convention, e.g. `"PT"` fallback at `server.py:7210`) with flags computed from the code rather than hand-listed (no transcription risk across ~195 entries) — deliberately NOT reusing `COUNTRY_FLAGS` in `admin/dashboard.tsx`, which stores full names for an unrelated feature (school profile setup). (2) workflow question — decided the submitter's scope choice becomes the submission's real initial `visibility_scope` (previously only ever set later by an admin, defaulting to `"school"`), but superadmin's existing global-approve gate keeps full override authority — same two-gate architecture, just no longer starting from a silent, uninformed default.

**Built:** `GET /creatures/submission-options` (new) — resolves valid scope options server-side from a real student's classroom/school via the same `_resolve_student_classroom_school`/`_is_authorized_for_student` helpers the eligibility feed already uses, so the picker is never guessing from partial client-side data. `POST /creatures/submit` now accepts `visibility_scope`/`country`, clamps the requested scope down to whatever the real resolved data actually supports (never trusts the client outright — re-resolves server-side even if the client sends something else). New scope+country picker step in `submit-creature.tsx` (between details and photos), with a proper `Modal` for the country search (an earlier draft used `position:absolute` inside a `ScrollView`, which wouldn't have covered the screen correctly once scrolled — caught and fixed before shipping). Portal: the "Awaiting Final Approval" queue cards now show `📋 Requested: {scope}`, so superadmin's override is an informed decision rather than a blind one (a real, small gap found while checking whether the two-gate design actually worked as intended — the approve buttons had no visibility into what was originally requested).

**Live-verified** against `demo_teacher_001` / real classroom-linked student `dstu001` (Sunshine International School): `/creatures/submission-options` correctly returns `has_classroom:false` with no student selected vs. `has_classroom:true,has_school:true` with `dstu001` selected. Three real submit calls confirmed the clamp logic exactly: global-scope request with a real student + explicit country → stored as requested (`global`/`JP`, no clamp); classroom-scope request with NO real student → correctly clamped down to `global`; classroom-scope request WITH a real classroom-linked student → correctly stored as `classroom`, no clamp. All test submissions, the test code, and the test session deleted afterward.

### Item 2 — visual polish restored — ✅ DONE (movement + detail view), ⚠️ side-by-side adapted, not ported 1:1
Ported the old `CreatureCollection.tsx` modal's real per-zone movement (sway/blue, hop/green, shiver/yellow, bounce/default — `CreatureCollection.tsx:40-72`) into a new shared `AnimatedCreatureVisual` + `useZoneMovement` hook, generalised to animate a community creature's photo, not just a default's emoji (the old modal never had to handle that case). `world-creatures.tsx`'s cards swapped their placeholder single generic bounce (an approximation built during the A24 fix pass, not the real old behaviour) for the genuine 4-pattern variety.

Bonus Items and the "grid-select → large focused view" interaction went into a new `CreatureDetailModal`, opened by tapping a card on the My Creatures screen (`student/creatures.tsx`) — this is where they actually still make sense, not `world-creatures.tsx` (a browse/discover grid whose card action is already "Set as Active", not "inspect"). Real constraint surfaced and handled honestly rather than faked: Bonus Items (moves/outfits/foods/homes) are exclusively a default-creature concept — the 4 hardcoded creatures have an emoji+accessory system; community creatures are 4 photos and categorically can't have moves/outfits/foods/homes. The section simply doesn't render for a community creature, with real data (`rewardsApi.getCollection`, already live, was just never wired into the unified screen) for defaults. `server.py`'s `get_my_creatures` enriched with `stage_emojis`/`stage_urls` per entry so the detail modal can show the full evolution row, not just the current stage.

**Live-verified**: `/students/dstu001/my-creatures` confirmed all 4 default entries now return their full 4-emoji `stage_emojis` array (e.g. blue → `['🐟','🐬','🦈','🐋']`). Community `stage_urls` verified by code review only (same `cs.get(...)` pattern as the already-tested `stage_img` field on the line above it) — no real community unlock existed on a test account to round-trip against without creating one, and the pattern is identical to already-verified code.

**Honest note on scope**: the "side-by-side" element (small icon list + one large focused view) was NOT ported as a literal permanent split-panel layout — that would have fought the deliberately-redesigned collapsible-sections grid from A21 (itself an earlier, explicit fix for 3 fragmented old surfaces). Adapted instead as tap-to-open detail, which is the same underlying interaction (pick from a grid, see one creature large) without re-fragmenting the screen. Flagging this explicitly in case Jono wants the literal docked layout back specifically.

### Item 7 — community creatures on profile-select tick indicators — ✅ DONE
Added mid-round, alongside item 2. `select.tsx`'s per-student tick-indicator row previously only ever reflected the 4 default creatures (fed by `/rewards/batch/collections`). Now also fetches each student's actively-selected community creature per colour (`/students/{id}/my-creatures`, filtered to `type==='community' && is_active`) and renders it alongside the defaults with the same tick/in-progress treatment — using the creature's real stage-image thumbnail (not a generic icon), so multiple active community creatures for the same family don't look identical.

### Item 8 (new, mid-round) — linked-student progress continuity — investigated, confirmed structurally free, no build needed
Jono's ask: confirm whether a teacher-created student profile and a parent-linked one are the same `students` row before building continuity. Traced both real linking paths: `/parent/link-child` (`server.py:3970`) and `/family/members/{id}/link-student` (`server.py:11231`) — neither ever creates a new `students` row when linking to an existing one; both just point `family_members.student_id`/`parent_links.student_id` at the teacher's original row. `AppContext.tsx:351` already resolves this correctly client-side (`m.student_id || m.id`), and `/students/{id}/active-creatures` (`server.py:7869`) does one unconditional `.eq("id", student_id)` lookup with no role branching at all — so whichever account calls it, same row, same data. Continuity is already real and structurally guaranteed by the data model; no architectural change needed. Not independently round-tripped live (the demo data has 4 different unrelated rows all named "Matilda", none a clean real linked-pair to test against) — confidence here comes from the code trace itself, which leaves no conditional path that could diverge, not from behavioural reproduction.

### Items 3, 4, 5, 6 — three of four already built in a prior (Aug 21) session; real findings, not re-built
Checked each against live source before building anything, per standing practice this session. Result: only item 3 had a genuine, real gap.

- **Item 4 (schools contact-details minimise/expand) — already fully built, both surfaces.** Portal: `toggleSASchoolDetails()` (`portal100.html:1377`) is a real, working expand/collapse, explicitly built Aug 21 to mirror the app's `SchoolsManager`. App: `admin/dashboard.tsx`'s `SchoolsManager` (`:158`, `expanded[school.name]` state) is the original the portal was matching. Nothing built.
- **Item 5 (collapse/expand UX polish) — already fully built.** The review plan's own two entries were the confusion source: line 702's "Queued for a future session" note was the *original* request, written before the fix; line 689's "✅ DONE" entry is the actual later resolution, and was never cleaned up to remove the stale queued note. Live-verified directly in `parent/family-strategies.tsx:84`: `collapsedZones` initialises as `new Set(['blue','green','yellow','red'])` — all four colour groups genuinely start collapsed. Nothing built; the stale "queued" entry above is superseded by this note.
- **Item 6 (language-availability icons in Settings) — already fully built.** `settings.tsx`'s `LANGUAGES` array already has a real `hasVoice` field (`en`/`pt`: true, `es`/`fr`/`de`/`it`: false, `:16-21`), and it's genuinely rendered — an "🔇 Audio coming soon" badge (`:574-579`) shows on exactly the languages Jono described, hidden on EN/PT. Nothing built.
- **Item 3 (superadmin portal PDF export) — partially exists; the real gap is lower-value than it looked, flagging rather than building on it.** A "Global Overview" stats PDF for superadmin already exists in BOTH surfaces — portal (`downloadSuperAdminOverviewPDF`, `:6041`) and app (`SuperAdminDashboard`'s `downloadAllSchoolsPDF`, `admin/dashboard.tsx:629`), both hitting the real `/reports/pdf/school-overview` endpoint, both built Aug 21. The genuine gap is a Schools-directory/Users-directory PDF, which doesn't exist anywhere. **Did not build it**, because of a bigger finding surfaced while checking: the Schools *management* tab (where item 4's expand/collapse and any directory PDF would pull from) is sourced from `/admin/school-profiles` → the `school_profiles` table, which is confirmed live to have exactly **1 real row** in production (`school_profiles` count queried directly). Real schools like Kairos Montessori and Sunshine International School exist and show up correctly in the Dashboard's *aggregate* stats (a separately-computed path), but have no `school_profiles` row at all, so the Schools management tab — add/edit, invite codes, contact details, and now a hypothetical PDF export — is nearly empty of real data despite the system clearly having many real schools. Building a nicer PDF export on top of that right now would just polish something structurally broken. This is a bigger, separate decision (why schools aren't landing in `school_profiles` — no onboarding flow populates it, or a backfill is needed) than an item-3-sized PDF button. Flagged for Jono to prioritize, not silently built around.

### Deploy status
All of items 1, 2, 7 committed, pushed, and confirmed deployed (Railway `Application startup complete` after each push). No new iOS/Android build cut — per Jono's standing instruction, #22 waits for his own Expo Go verification of this whole round.

### school_profiles root-cause fix — 2026-08-22 — ✅ DONE & LIVE-VERIFIED
Follow-up to the item 3 investigation above. Root cause was singular and precise: `POST /admin/create-school-admin` (`server.py:10378`) is the ONLY real path that creates a school_admin account, and it never created a matching `school_profiles` row — confirmed live, 0 of the 3 real school_admin accounts (Kairos Montessori ×2, Sunshine International School ×1, all created 2026-07-28) had one, and the 1 orphaned row that existed ("St Lucy's") had `school_admin_user_id: None`, unlinked to any real account.

**Built:** new `_ensure_school_profile(school_admin_user_id, school_name)` helper — idempotent, creates a linked `school_profiles` row with the same default shape as the portal's own "+ Add School" (`subscription_package: "starter"`, `status: "trial"`, 5 seats). Called from both branches of `create_school_admin` (new account and re-promotion of an existing user). Every school_admin account created from now on gets a real, linked profile automatically.

**Backfill run** for the 3 real existing accounts — all 3 now have a correctly-linked `school_profiles` row. Orphaned "St Lucy's" row deleted after confirming it was still the same unlinked row (`school_admin_user_id: None`) before deletion.

**Live-verified end to end**: (1) called the real deployed `/admin/create-school-admin` with a temporary test account — confirmed a correctly-linked profile was auto-created with the right defaults, then deleted the test account/profile/session. (2) Called `/admin/school-profiles` — the exact endpoint the portal's Schools tab uses — via a temporary superadmin session: returns exactly 3 rows, all `school_admin_user_id` linked (`True`), matching Kairos Montessori ×2 and Sunshine International School. Session deleted afterward.

**Map (piece 3) — confirmed self-heals for the schools list, does NOT need a code change, and does NOT yet show pins.** `renderSAWorldMap` (`portal100.html:1144`) already handles missing coordinates honestly — it filters to `withCoords` and shows "Add latitude/longitude to a school's profile to see it appear here" rather than looking broken. The Schools tab itself (list, names, contact fields, expand/collapse, invite codes) is now fully populated and correct. The map's pins specifically still need real lat/lng entered per school via the already-working Edit form — nobody has ever entered that data, and none was fabricated here. Not a bug; a genuine remaining manual step, called out explicitly rather than left implicit.

Not yet uploaded to cPanel — no portal.html changes were needed for this fix (backend + data only), so the currently-live portal file already reflects the correct behaviour once the data existed.

Item 3 (schools directory PDF) is now real, small, and worth building whenever wanted — the same rich `school_profiles` schema (address/contact/subscription/counts) is genuinely populated for real accounts for the first time.

---

## A26 — live-testing round 3, 2026-08-23

### Item 1 — parent scope options — ✅ DONE & LIVE-VERIFIED — real bug, confirmed before building
Jono's report: a parent submitting only ever saw "Everyone Everywhere" as an option. Investigated before building, as asked. Root cause, precise: `get_eligible_creatures`'s "classroom" scope match (`server.py:7582`) required `classroom_id` truthy on BOTH sides (`elif scope == "classroom" and classroom_id and c.get("classroom_id") == classroom_id`). For a home-only family member (no classroom at all — the common parent case), the browsing side's `classroom_id` is always `None`, so the whole condition short-circuits false unconditionally. A "classroom"-scoped submission from a home-only family was eligible to **nobody, not even the family that submitted it** — not a picker-display issue, a genuinely non-functional scope end to end. This is why `/creatures/submit`'s own clamping logic (built in item 1's first pass) silently downgraded "classroom" to "school"/"global" whenever there was no real classroom — it was correctly protecting against creating an invisible, orphaned creature, which just meant the option was never offered at all.

Separately, for a school-linked child (where "classroom"/"school" scope IS technically functional), the "Who made this?" step that resolves which real student to check was optional and easy to skip — skipping it meant `submission-options` never got a `real_student_id` to resolve against, so the real options silently never appeared even when they should have.

**Built:** `get_eligible_creatures` now also matches "classroom"-scope submissions with no `classroom_id` against the submitting account itself (`c.get("student_id") == user["user_id"]`) — family-private, deliberately narrow to the one account that submitted it (not every account linked to that child — a home-only child only ever belongs to one parent account in the current data model, so this is the correct boundary, not a shortcut). `/creatures/submit` no longer downgrades "classroom" — it's always valid now, either as a real classroom match or a family-private one. `submit-creature.tsx`'s picker always offers the narrowest option, relabeled "My Family" when there's no real classroom (same contextual relabeling pattern `world-creatures.tsx`'s scope-preference picker already used). "Who made this?" now auto-selects when there's exactly one child, closing the easy-to-skip gap for the common case.

**Live-verified**: submitted and approved a real test creature as `user_750b8fb79312` (Kairos Montessori parent) for a real home-only family member (Mateus, `classroom_id: null`) with `visibility_scope: "classroom"`. Called `/creatures/eligible?student_id=Mateus` as the same parent — the test creature correctly appeared, confirming the family-private match works end to end, not just in theory. Negative case (a different account never matching) verified by construction — a direct string equality check has no ambiguous path — rather than an additional live round-trip, given time budget. Test submission, code, and session all deleted afterward.

### Item 2 — parent dashboard "My Creatures" navigation — ✅ DONE
Same bug class as the one already fixed once on `student/select.tsx`'s card button, just missed in a second location: `parent/dashboard.tsx`'s per-family-member "My Creatures" button (on the family dashboard grid) still opened the old defaults-only `CreatureCollection` modal (`setCollectionMember`/`setShowCollection`), never migrated when the unified screen was built. Fixed with the same `setCurrentStudent` + `router.push('/student/creatures')` pattern already used by this same file's `handleMemberCheckin` (linked child → real student record; non-linked → family-member wrapper with the resolved id). Dead modal render block, its state, and the unused `CreatureCollection` import all removed. `tsc` clean on the new code (pre-existing unrelated errors elsewhere in this large file, unchanged from before).

### Item 3 — Bonus Items celebration merge — investigated, honest scope read delivered, NOT built (as asked)
Read the old `CreatureShowcase.tsx` (the actual "slideshow" component, triggered by tapping a creature card in the old `CreatureCollection` modal — not a separate achievement-unlock event, contrary to how it might be remembered) in full before answering. This is genuinely a bigger merge than "restore a component," for five concrete reasons documented for Jono, not built: (1) different triggering/interaction models — old is an 18-second auto-advancing 5-step celebration with timers, dots, and a forced close; new `CreatureDetailModal` is a static, read-at-your-own-pace panel; merging means resolving a real interaction-design conflict, not just concatenating files. (2) the old showcase is 100% default-creature-only (emoji + moves/outfits/foods/homes) with zero concept of a community creature's photo-based stages — a community creature would need an adapted, differently-structured celebration (e.g. stage-photo reveal), which is new creative work, not a port. (3) data-loading mismatch — the new modal loads Bonus Items async with a spinner; the old showcase assumed everything was already loaded synchronously before it could even open, so a clean merge needs either a pre-fetch delay before opening or a new per-step loading state the original design never had. (4) two different bounce-animation amplitude conventions (old showcase's are larger/more dramatic than the shared `AnimatedCreatureVisual` already wired into 3 screens this session) — a real, if minor, consistency decision. (5) the real functional data the new modal added (scope, expiry, evolution row) has no natural "step" in the old slideshow's structure — needs real placement decisions, not just insertion. Recommended as its own scoped follow-up once Jono decides how much of the celebratory pacing he wants to keep vs. adapt. Not built.

---

## A27 — live-testing round 4, 2026-08-23

**Confirmed working, no action needed**: profile-select community-creature ticks; World Creatures screen (logo/home, heading, preview cycling, expiry badge, grid sizing); reward-page scroll/Continue access, Bronze Shield dismiss, single heading; linked-student progress continuity confirmed live by Jono himself (a creature added via the teacher account correctly appeared on the linked parent account for the same real student) — closes out A26's item 8, which had only been verified by code trace until now.

### Item 4 — family dashboard "My Creatures" nav — investigated properly, confirmed genuinely fixed live
Jono asked for a definitive check, not an assumption, given the fix appeared to "switch" mid-session with no obvious cause. Checked the current deployed source directly: no `CreatureCollection` import remains in `parent/dashboard.tsx`, no `showCollection`/`collectionMember` state remains, exactly one "My Creatures" button in the file, and it unambiguously calls `router.push('/student/creatures')`. Single code path, no ambiguity, no stale duplicate anywhere. The "switch" Jono saw mid-testing lines up with when the fix actually landed and deployed in the prior round (A26) — not a caching fluke, just testing spanning the moment the real fix went live.

### Items 1, 2, 3(visual), 5, 6 — ✅ DONE
- **Reward page layout**: My Creatures + Find World Creatures now share one row, half-width each; Continue unchanged, full-width, below.
- **Bonus Items unlock hint**: restored the "⭐ X pts to unlock" text for locked items in `CreatureDetailModal` - present in the old modal, dropped when rebuilt, real regression not a new ask.
- **Coloured background (visual)**: `creatures.tsx`'s grid cards now use the old modal's per-colour tinted background (`color + '25'`) instead of flat grey - the detail modal already had this correctly (`color + '20'`, matched the old detail panel), so the gap was specifically the grid cards, not the modal.
- **Family dashboard small cards**: now fetch and show active community creature icons too (real thumbnail + colour border + complete badge), matching `student/select.tsx`'s bigger cards - same `/students/{id}/my-creatures` enrichment, added to the existing per-member collection-fetch loop.
- **Student card messaging**: new "🌟 New creatures to evolve!" line when a student has eligible community creatures with zero progress so far (`/creatures/eligible`, `my_stages_unlocked` falsy) - shown regardless of default-creature completion state, not just when all defaults are done.

### Item 7 — linked-account indicator on student-flow cards — ✅ DONE, real bug found and fixed along the way
Scope check before building: `is_linked` already existed on `/students` (`server.py:2565`), just never surfaced in the teacher-facing UI - genuinely small, comparable to other fixes this session, not a bigger-than-expected item. While wiring it up, found it was also *incomplete*: it only ever checked `parent_links`, never the other real linking path (`family_members.student_id`, set by `/family/members/{id}/link-student`) - `_is_authorized_for_student` already treats both as equally real, this endpoint didn't. Fixed both: the missing check, and the missing UI (`select.tsx`'s cards now show the same small `link`-icon badge, top-right, that the Family Dashboard's cards already had).

**Live-verified**: created a temporary `family_members` row linking a real parent account to `dstu003` (Sofia Rossi) with deliberately NO `parent_links` row - before the fix this student showed `is_linked: false`; after, correctly `true`. Confirms the fix catches exactly the case it was meant to, not just in theory. Test row deleted afterward.

### Item 8 — country-count teaser — ✅ DONE, threshold unchanged
Jono confirmed he wants to keep the 5-contributor threshold and just add momentum. `/creatures/eligible` now returns `countries_joined` (reuses `safe_countries`, already computed for the existing per-creature country reveal - zero extra query, and structurally can never leak a below-threshold country since it only ever counts countries that already cleared the same real threshold). `world-creatures.tsx` shows "🌍 {n} countries have joined - keep submitting to unlock the map!" whenever the count is above zero. **Live-verified the field is present and correctly computed** (currently `0` in production - genuinely accurate, not enough global-scope creatures from any single country yet to clear the threshold; the teaser correctly stays hidden at 0 rather than showing a hollow "0 countries" message).

### Item 3 (Bonus Items celebration) — real plan delivered, per Jono's explicit ask to see the plan before any build
Jono's brief: keep the celebratory feel/pacing, adapt it properly for both creature types rather than forcing the old fixed 5-step structure onto data it wasn't built for. Plan, not yet built:

- **Trigger stays the same real interaction**: tapping a creature card opens the celebration - matches how the old showcase actually worked (per-tap, not a separate achievement-unlock event), so no new trigger model to design.
- **Default creatures keep the exact existing 5-step structure** (Meet → Move → Outfit → Food → Home) - already good, already loved, not broken, no reason to touch it.
- **Community creatures get a new, different step sequence**, not a re-skin of the default one: (1) Meet {creature} - current stage photo. (2) Evolution journey - the real stage1→4 photo progression, framed as "look how far you've come" (this is the community equivalent of an item unlock - genuinely something to celebrate that the old showcase never had data for). (3) Scope reveal - "Visible to: My Classroom!" etc., turning the new functional data into a celebratory beat instead of bolting it on as a plain info line. (4) Limited-edition/expiry reveal - only included as a step at all when `was_featured` is true, so a normal creature doesn't get a dead/empty step. (5) Completion beat - fully-evolved celebration, or an honest "keep growing" prompt if not done yet.
- **Step count becomes dynamic**, driven by an array the caller builds per creature type, rather than a hardcoded 5 - the showcase component itself becomes generic (renders whatever steps it's given), fixing the real structural mismatch rather than working around it.
- **Solves the async-loading conflict**: step 1 (Meet the creature) opens immediately from data `creatures.tsx` already has in hand - no wait. Only later default-creature steps need Bonus Items data; if that fetch hasn't resolved by the time the auto-advance timer reaches that step, that step shows a brief loading state rather than the whole modal waiting to open (the old showcase never had to handle this, since its data was always preloaded by its parent before it could ever open).
- **Animation sizing unified**: the showcase reuses the same `useZoneMovement` hook already wired into 3 screens this session, just at a larger `size`, instead of keeping its own separate, larger hardcoded bounce values - one real animation system, not two.
- **Exit/pacing controls carry over unchanged**: skip (✕), dot pagination, Next/Exit, auto-advance - all proven, all staying. Total auto-play duration scales with real step count instead of a hardcoded 18 seconds for a fixed 5 steps.
- **Decided (2026-08-23)**: after the celebration finishes (or is skipped), hand off into the existing static `CreatureDetailModal` rather than just closing - a calm place to reference the creature afterward instead of the excitement just evaporating. Confirmed by Jono, not just the leaning noted above.

Logged as its own follow-up, same tier as the earlier visual-polish item. Not urgent - explicitly deferred by Jono, app is in a strong place. Not built.

---

## A28 — portal testing round, urgent safety fix, 2026-08-23

### ⚠️ ACTION NEEDED — run this migration
The urgent fix below (item 1) requires a new column. Supabase REST access has no DDL - exact SQL to run in the Supabase SQL editor:

```sql
ALTER TABLE creature_submissions ADD COLUMN superadmin_approved_at timestamptz DEFAULT NULL;
```

**Until this runs**, creature browsing/starting (`/creatures/eligible`, `/creatures/start`, "My Creatures") will correctly show/allow **nothing** for any community creature - deliberate, safe-by-default behaviour while the real gate is unavailable, not a regression. Confirmed live. `/creatures/pending`/`/creatures/my-submissions`/portal moderation queues are unaffected (different, unrelated gate) and continue working normally. Once the migration runs, existing already-teacher/parent-approved submissions will need superadmin's `global_approve_creature` action (already available in the portal/app) before they reappear - nothing is auto-approved by the migration itself.

### Item 1 — URGENT safety gap — ✅ FIXED, with a real self-correction along the way
Confirmed exactly as Jono suspected: superadmin's second approval gate was never actually enforced on any student-facing read path. `get_eligible_creatures`, `start_creature`, and `get_my_creatures` all only ever checked `status=='approved'` - the teacher/parent gate. `global_approve_creature` (superadmin's action) only ever set `visibility_scope`/`is_globally_available`, which affect WHICH scope a creature shows under, not WHETHER it's visible at all. A teacher/parent-only-approved creature was immediately visible/startable by every matching student, for every scope (classroom/school/family/global), fully bypassing superadmin.

**First fix attempt (same day) was wrong, caught immediately by live verification.** Used `global_uses` as the "has superadmin approved this" signal, reasoning it was NULL until `global_approve_creature`'s approve action set it to 0. Deployed, then live-tested end to end (not just trusted the code) - and the test failed: a creature approved by teacher only still showed as eligible. Investigated immediately: `global_uses` has a **DB-level default of 0**, confirmed by checking a truly-fresh, zero-approval submission (`global_uses: 0` from the instant of insert, before any approval at all). It's a pre-existing popularity counter (incremented when a student fully evolves a creature), never something that could have worked as an approval gate - the first fix was a silent no-op, and the real hole was still open despite looking fixed. Caught and corrected the same session, before moving on to anything else.

**Real fix**: a genuine new column, `superadmin_approved_at`, set only by `global_approve_creature`'s approve action (with a try/except fallback so that endpoint doesn't hard-fail before the migration lands), NULL on every existing row and every future row until superadmin explicitly acts. Every gated surface now selects it defensively:
- `get_eligible_creatures` (browse) - fails closed (nothing eligible) if the column doesn't exist yet.
- `start_creature` (begin pursuit) - same, plus fixed its separate missing family-private eligibility branch (parity with the fix built earlier today).
- `get_my_creatures` (My Creatures/rewards display) - joined select fails as one unit if the column is missing, lands in the existing except, fails closed.
- `add_points` (defensive fallback resolution) - falls back to the default per-colour creature rather than erroring.
- `get_awaiting_global_approval` (superadmin's own queue) - this one is NOT student-facing, so it fails *open* to its old filter if the migration hasn't landed, staying usable in the interim rather than going dark for superadmin too.

**Also found and fixed in the same investigation**: `get_awaiting_global_approval`'s own filter (`is_globally_available==False`) never actually cleared once superadmin approved a non-global-scope submission - confirmed live, 2 real already-approved submissions were stuck permanently showing as "awaiting" (fixed as part of the `superadmin_approved_at` migration, with the safe fallback above for the interim).

**Live-verified end to end, twice** - once (correctly) proving the first attempt was broken, and once proving the real fix fails safely: created a real test submission, teacher-approved it only (not superadmin), confirmed via the real deployed endpoints that it does NOT appear in `/creatures/eligible`, cannot be started via `/creatures/start`, and does not appear in `/students/{id}/my-creatures` - all correctly blocked, in the fail-closed state pending the migration. `/creatures/awaiting-global-approval` confirmed still functional via its fallback (returns the 2 real stuck submissions). All test data cleaned up afterward.

### Item 2 — scope visibility for superadmin's decision — ✅ DONE
The "Requested: {scope}" label added earlier today (A26) capitalized the raw stored value, which meant "Family (private)" and real "Classroom" scope both displayed identically as "Requested: Classroom" - the exact ambiguity item 2 was flagging. Fixed: now checks `classroom_id` presence, showing "Family (private)" distinctly from "Classroom" when there's no real classroom behind it.

### Item 3 — teacher's "My Classroom's Submissions" showing a family-scoped item — investigated, confirmed NOT a leak
Directly queried the real "Voltkit" submissions in production: there are two, unrelated, same-named creatures - one genuinely family-scoped (`jono@gmail.com`, `classroom_id: null`, correctly matches only that account), one the teacher's own separate test submission (`jono+teacher@gmail.com`, real classroom_id, correctly belongs in their own queue). Called the real `/creatures/my-submissions` endpoint as the teacher directly: returns exactly 2 rows, both genuinely the teacher's own, the family-scoped one correctly absent. No backend leak. The real, valid confusion source: the queue only ever showed the *submitting account's* name ("Joana" for both), never the real child, so two same-named creatures from different accounts were indistinguishable at a glance - directly fixed by item 4.

### Item 4 — show real student + submission date — ✅ DONE
`creature_submissions.student_name` has always been the submitting account's own display name, never the real child's (same fact already documented for `/creatures/submit`). New `_annotate_real_student_names` helper resolves the real student via `real_student_id`, applied to `/creatures/pending`, `/creatures/my-submissions`, and `/creatures/awaiting-global-approval`. Frontend (`CreatureManagement.tsx`, portal's two pending-queue renderers, portal's superadmin final-approval queue) now shows "For: {real student}" + a formatted submission date instead of just the submitting account's name.

### Items 5/6 — Schools tab quick-view expand — confirmed it already existed, found and fixed why Jono never saw it
Re-confirmed `toggleSASchoolDetails` (built Aug 21) is real, live, and working - checked the actual live portal file directly, not just the local copy. The real gap: the expand arrow and panel only ever rendered when the school profile had *any* optional contact field filled in (`details.length > 0`). Every real school today (all backfilled by this morning's `school_profiles` fix) has zero contact fields filled in - nothing prompts anyone to add them yet - so the arrow was invisibly absent for exactly the schools Jono was looking at. It wasn't missing; it had nothing to expand, and gave no indication that was the reason. Fixed: the arrow and panel now always render, with an honest "No contact details added yet - tap Edit to add" empty state instead of silently disappearing. School_admin's own view: N/A, not a comparable gap - it's a single-entity settings form (one school, their own), not a list, so there's no list-item-expand pattern to compare against.

### Item 7 — "Schools Breakdown" showing nothing at any period — ✅ FIXED, real unrelated bug found
Traced the full resolution chain live (teacher lookup → classrooms → students → feeling_logs) against Sunshine International School's real data end to end. Teacher/classroom/student resolution all worked correctly. The actual break: `feeling_logs.select("feeling_colour,zone")` - confirmed live by fetching a real row that `zone` **has never been a real column** on `feeling_logs` (the actual schema is `student_id, feeling_colour, helpers_selected, comment, location, timestamp, logged_by, family_member_id`). Every single call to this query failed with a Postgres "column does not exist" error, silently swallowed by a bare `except: pass` - so Schools Breakdown showed 0 check-ins for every school, at every time period, regardless of how much real data existed, because the query never once actually succeeded. Nothing to do with time periods or Jono's testing - just a broken query. Fixed by removing the nonexistent column. **Live-verified**: Kairos Montessori now correctly shows 125 real check-ins, Sunshine International School 840, at a 90-day window.

### Item 8 — Google Maps / easier lat-lng entry — flagged, not built (explicitly low priority)
Two real options if Jono wants to revisit this: (1) Google Maps JavaScript API's click-to-place widget - most polished, but needs a Google Cloud API key + billing enabled on an account. (2) Leaflet.js + OpenStreetMap tiles - free, no API key/billing needed, a simple embeddable "click the map, get lat/lng" widget, good enough for this use case. Since the portal is a plain hosted HTML page (not sandboxed like an Artifact), either is technically straightforward to add later - recommending option 2 as the easier, cost-free path if/when this becomes worth doing. Not built - manual entry stays as-is for now, per Jono's explicit low priority.

### Migration confirmed run, column verified correct, safety fix re-verified end to end — 2026-08-23
Jono ran the migration; Supabase reported the column already existed. Verified directly rather than assuming this was fine: checked the real column definition via PostgREST's OpenAPI schema (`format: timestamp with time zone` - correct type, matches the spec exactly) and queried every row's actual value (all 5 real rows: `superadmin_approved_at` genuinely `NULL` - no garbage, no stale data, no leftover value from anything else). Column is exactly as specified.

**Re-ran the full live verification with the real column in place** - a clean positive/negative round trip, not just the earlier fail-closed half: submitted a fresh test creature, teacher-approved it only. Confirmed blocked on all 3 surfaces (`/creatures/eligible`: absent; `/creatures/start`: 404; `/students/{id}/my-creatures`: absent). Superadmin then approved it via the real `/creatures/global-approve` endpoint. Confirmed it became eligible immediately afterward (`/creatures/eligible`: present). The one remaining block on `/creatures/start` afterward was a *different*, unrelated, correct business rule ("finish your current creature before starting a new one" - the test student already had an in-progress creature for that colour) - not the safety gate, which had already let it through by that point. Test data cleaned up. The gate is confirmed genuinely working end to end, not just deployed.

**Follow-up: Jono asked for `information_schema`-level type/default confirmation specifically.** Honest limitation: this environment only has REST (PostgREST) access via the service key, no raw SQL/RPC execution - no function for that exists anywhere in this codebase (checked). Genuine `information_schema.columns` isn't reachable this way. Did the strongest verification actually available instead, and it's arguably more direct than reading a declared default would be: inserted a raw row straight into `creature_submissions` (bypassing the app layer entirely, zero mention of `superadmin_approved_at` in the insert payload) and read back its value immediately - `None`, confirmed at the database level with no application code involved at all. Combined with the type confirmation (PostgREST's OpenAPI schema is itself derived from the real Postgres catalog, not something the API could misreport) and all 5 real production rows independently checked as `NULL`, this is multi-angle, direct empirical confirmation - the column behaves exactly as specified. Test row deleted immediately after.

### Spanish voice recordings (partial rollout) — 2026-08-23 — ✅ DONE & LIVE-VERIFIED
Jono uploaded 4 real Spanish colour-name recordings directly to Supabase Storage. Checked the real bucket structure before wiring anything up: `en/` and `pt/` both hold exactly the 28 `{key}.m4a` files (`blue`, `blue_1..6`, `green`...) `VOICE_CLIP_KEYS` expects, confirming the established convention precisely. Jono's 4 Spanish files existed but at the bucket root, named `es_emociones_{colour}.m4a` (Spanish word, not the short English key) - didn't match the convention at all, in path or naming.

The system is deliberately designed so the Storage bucket itself is the single source of truth (`get_voice_clips` lists whatever's really there and returns only keys with a real file) - so the right fix was moving the files to match, not writing special-case code around a wrong path. Moved all 4 directly via the Storage API (`es_emociones_azules.m4a` → `es/blue.m4a`, etc.) - service key has that access, faster and more reliable than asking Jono to do it by hand. Verified the move: `es/` now holds exactly the 4 correct filenames, zero leftovers at the root.

**Backend change was one line**: added `"es"` to `VOICE_CLIP_LANGUAGES`. Nothing else needed - `get_voice_clips` already only returns keys with a real file present, and the frontend (`voiceClips.ts`) already no-ops silently on any missing key (`if (!url) return;`) - the exact same mechanism already proven live for `pt`'s own partial rollout, so items 2 and 3 of Jono's ask (wire up the 4 real clips; gracefully skip the other 24) both fall out of the existing design with no new code.

**Live-verified**: called the real deployed `/voice-clips?language=es` - returns exactly `{blue, green, red, yellow}`, no helper keys. Each URL independently checked - all 4 resolve to real, correctly-sized `audio/x-m4a` files (byte sizes match the original uploads exactly, confirming the move didn't corrupt anything). Confirmed `en`/`pt` unaffected (still 28 keys each). Couldn't literally tap a colour button in a running app in this environment, so this is backend-level verification - the manifest and the audio files themselves are confirmed correct and reachable, which is what the app's already-proven playback code (`playVoiceClip`) consumes.

---

## A29 — two more real bugs found during Jono's own portal testing of the safety fix, 2026-08-23

### Item 1 — approved creature staying in both queues — investigated, confirmed real, same gap class I'd already fixed but missed two more surfaces
Traced it before assuming anything. `/creatures/awaiting-global-approval` (the pending queue) was already correctly gated on `superadmin_approved_at` from yesterday's fix - that part was working. The actual bug: `get_global_creatures` (`/creatures/global`, powers "Approved Creatures" both on superadmin's own card and school_admin/teacher's "Global Creature Feed") only ever checked `status=="approved"` - the teacher/parent gate - never `superadmin_approved_at`. For superadmin specifically it didn't even check `is_globally_available`. Same exact gap class as the urgent safety fix, just two endpoints I didn't touch in that first pass: this one, and `feature_creature` (a teacher/school_admin could spotlight-feature a creature superadmin had never approved).

**Confirmed live before fixing, real current exposure**: 6 real submissions (Waterman, Voltkit ×2, Waterman 2, Emberwing, Bloomwing) were showing in "Approved Creatures" while still correctly sitting in "Awaiting Final Approval" too - exactly Jono's report. And "Waterman" was **actively featured** (spotlighted, active through 2026-08-31) despite never being superadmin-approved at all.

**Fixed all three surfaces**: `get_global_creatures` now gates on `superadmin_approved_at` (fails closed if the column is ever unavailable, same direction as everywhere else). `feature_creature` (write side) now requires it before a creature can be featured at all. `get_featured_creatures` (read side) filters out any already-bad `featured_creatures` rows rather than requiring a manual data cleanup - existing rows aren't deleted, they just stop being surfaced.

**Live-verified**: confirmed all 6 previously-exposed creatures are now absent from `/creatures/global`. Confirmed "Waterman" no longer appears in `/creatures/featured`. Confirmed a real teacher account attempting to feature one of the still-unapproved creatures is now correctly blocked (400).

### Item 2 — superadmin hard delete — ✅ DONE & LIVE-VERIFIED
A soft-cancel endpoint (`cancel_creature`, sets `status: "cancelled"`) already existed in the backend but was never wired into any UI - same dead-endpoint pattern found elsewhere this session. Didn't fit either of Jono's stated use cases (removing inappropriate content, cleaning up test submissions) since the row would sit in the table forever either way. Built a real `DELETE /creatures/{id}`, superadmin-only: removes the submission row, any `creature_unlocks` referencing it (a student's real progress on a creature that would otherwise point at nothing), any `featured_creatures` rows referencing it, and best-effort Storage image cleanup (never blocks the actual delete if that part fails - no prior code in this file had ever parsed a public Storage URL back into a bucket path, so this is new, lower-confidence territory kept from being able to break the part that actually matters). Wired into the portal's "Awaiting Final Approval" queue and "Approved Creatures" card, both behind a real `confirm()` since it's irreversible.

**Live-verified**: non-superadmin correctly blocked (403). Superadmin delete succeeds, confirmed the creature is genuinely gone from `/creatures/global` afterward (not just hidden), and a second delete attempt correctly 404s. `creature_unlocks`/`featured_creatures` cleanup uses the same simple `.delete().eq(...)` pattern already proven elsewhere in this file - not separately exercised in this specific test since the disposable test creature had no real unlock/featured rows to clean up, but the same well-tested mechanism.

### Quick confirmation (not investigated further, per Jono's explicit "not urgent")
Superadmin's approval buttons showing only Classroom/School/Global (no Family) for a teacher-submitted creature (Bloomwing) - confirmed intentional, not a gap. A teacher's own submission always resolves a real classroom, so "Classroom" is already the narrowest real option; "Family" only has meaning for a home-only parent submission. One real, minor nuance flagged for the record but not fixed (per Jono's ask): the 3 approval buttons are the same fixed set regardless of submission type, so a genuinely family-scoped parent submission would show a "Classroom only" button that's technically correct once clicked (falls back to family-private matching) but slightly mislabeled for that case.

---

## A30 — portal delete gap, i18n button audit, Bonus Items celebration, 2026-08-23

### Delete gap on Approved Creatures — investigated, confirmed nothing missing in code
Jono reported no delete option in "Approved Creatures." Checked the current deployed source directly rather than assuming a gap: both delete buttons (pending queue AND Approved Creatures) already existed in code from the prior round (A29), correctly gated to superadmin. Compared the live site against the local file - the live portal is several rounds behind (missing the delete buttons, the real-student-name display, the corrected scope labels, all of A29's work). Not a code gap - the file just hadn't been uploaded. No new code needed; flagged the exact file to upload (`portal100-UPLOAD-2026-08-23.html`).

### Systematic i18n button-overflow audit — real, contained fix, delegated to a fork and verified
Investigated the reported Spanish "Buscar Criaturas del Mundo" overflow systematically, not just patched the one string, per Jono's explicit ask. Real instances found: **2**, both in `rewards.tsx`'s action-button row (the reported `find_world_creatures` button, and its sibling `my_creatures` button - same structural risk, not currently overflowing but same latent shape). Root cause: `TouchableOpacity` with `flex:1` and icon+label as separate sibling Text nodes in a row, no `flexShrink`/wrap protection, zero horizontal padding on one of the two. Checked `creatures.tsx`'s similar two-button row too - structurally different (combined emoji+text in one Text node, which RN wraps safely by default) and not a confirmed bug, hardened anyway as a precaution. Checked ~8 other flex:1/fixed-width candidates across `parent/dashboard.tsx`, `teacher/dashboard.tsx`, `teacher/bulk-checkin.tsx`, `world-creatures.tsx` - none were real instances (correct content-sized padding already, or untranslated/dynamic text). Fix applied is structural (`flexShrink`+`textAlign:'center'`+padding, letting text wrap to a second line) not a shortened string, so it holds for any future language including Hindi/Chinese/Arabic. Verified clean via `tsc` (zero new errors, all pre-existing baseline). No visual/running-app verification possible in this environment - flagged honestly.

### Bonus Items celebration — investigated real mechanic, scoped, and built per Jono's simplified ask
Investigated before scoping, as asked: confirmed via the real `CREATURES` catalog (`server.py:131`) that items unlock by evolution stage (`unlocks_at_stage: 1/2/3`, uniform across all 4 default creatures, 3 items per category), not points directly and not any specific helper/strategy - `POINTS_CONFIG` awards flat, generic points per event type regardless of which strategy was used, confirming "Ask for Help" has no special connection to any item. Confirmed real and functioning (already powers the existing Bonus Items grid), and confirmed already progressive (3 waves per category, not one final-evolution reward) - the actual gap was purely that nothing ever celebrated a new wave unlocking. Also checked, at Jono's explicit request, whether the OLD modal (`CreatureCollection.tsx`/`CreatureShowcase.tsx`) had any real per-item animation/sound to reuse - confirmed directly via grep that neither file has any item-specific audio at all (only generic UI-feedback sounds), so this was genuinely new work, not adaptation.

**Built**, per Jono's simplified/decided scope (category-level sounds, no tap-to-select/persist, existing `playEvolutionSound()` hook point as the trigger):
- `add_points` now returns `newly_unlocked` - which items crossed `unlocks_at_stage` at the exact stage transition that just happened (data already sat in the catalog, small addition).
- New `BonusItemCelebration` component - 4 real `Animated` sequences (not a static fade-in): burst for moves, float-on-from-above for outfits, squash-stretch bounce for foods, slow cozy fade+scale for homes - each paired with its category's sound.
- Triggered in `rewards.tsx` at the same real stage-transition event `playEvolutionSound()` already uses, deliberately not chained off `EvolutionAnimation`'s `onComplete` - found live that `EvolutionAnimation`'s own mount condition requires `rewardsData?.evolution_info`, which the default-creature `add_points` response never actually sets, so that modal likely never shows for default creatures today. A separate, pre-existing gap, flagged but out of scope here, not fixed.
- Reused for tap-to-replay in `CreatureDetailModal`'s Bonus Items grid - unlocked items only (locked stay non-interactive, unchanged), single item, no persistence, border highlights the tapped card while it plays.
- Community-creature omission re-confirmed already correct by re-reading the live code (`CreatureDetailModal.tsx:154`, gated to `entry.type === 'default'`) - no change needed.
- New public Storage bucket `sound-effects` created (deliberately separate from `voice-recordings`, which is spoken narration not UI effects, and deliberately self-hosted rather than extending the existing external-CDN pattern the app's other generic sounds use). Expects `moves.m4a` / `outfits.m4a` / `foods.m4a` / `homes.m4a`. Same graceful missing-file handling as everywhere else in `sounds.ts` - confirmed live the URL currently 400s (no file yet) and the existing `playSoundUrl` try/catch already handles that silently, so the celebration plays animation-only until real files land.

**Live-verified end to end**: pushed a real test student (`dstu001`) through 5 check-ins to cross the stage-1 threshold. Confirmed the response's `newly_unlocked` contained exactly the 4 expected items (Wave Spin/moves, Captain Hat/outfits, Sea Snack/foods, Coral Cave/homes), correctly tagged by category, at the exact check-in that crossed the threshold (not before). Test data (`student_rewards` row) fully deleted afterward, confirmed clean.

**Sound guidance given to Jono**: short (under ~1s), bright/cartoonish/kid-friendly, `.m4a` format matching the existing convention, normalized volume across all 4 so none is jarringly louder.

---

## A31 — pre-release closeout: sounds, EvolutionAnimation, country leaderboard, per-school PDF, N+1, closing scope, 2026-08-23

Full pass through Jono's pre-build checklist, item by item.

**1. Bonus Items sounds — confirmed live.** All 4 real files (`moves.m4a`, `outfits.m4a`, `foods.m4a`, `homes.m4a`) present in the `sound-effects` bucket, HTTP 200, correct `audio/x-m4a` content-type, real non-trivial file sizes (5.7KB-14.7KB, not silence placeholders). Delivery mechanics confirmed end-to-end; actual audible playback needs Jono's own Expo Go pass.

**2. EvolutionAnimation modal — fixed and live-verified.** Root cause confirmed: the mount gate checked `rewardsData?.evolution_info`, a field `add_points` never returns for any creature - the modal was unreachable for every student, always. Fixed the gate to check `current_creature?.stages` instead, which is the modal's actual real requirement (`EvolutionAnimation` indexes `creature.stages![fromStage]`) - default creatures have this, community creatures deliberately don't, so the same gate change also prevents a latent crash: a community-creature evolution would have hit that undefined-array index the moment `evolution_info` ever did get set. Live-verified via a real check-in call against `dstu001` - `current_creature.stages` present and populated for a default creature, confirming the new gate resolves true on the real path the frontend hits. `tsc` clean (0 new errors; repo-wide baseline confirmed at 37 pre-existing, same before/after via `git stash` diff).

**3. Country leaderboard/graph — built and live-verified.** New `GET /creatures/country-leaderboard`, same k-anonymity computation already proven for the `countries_joined` teaser and portal100.html's world map (`MIN_THRESHOLD=5`). Resolves the "bucket sub-5 into Rest of World" decision: countries at or above 5 global-scope contributors are listed individually and ranked; everything below is summed into one "Rest of World" row rather than dropped or shown individually, so the total always reconciles and no country is ever identifiable below threshold. Collapsible UI added to World Creatures (tap the existing 🌍 teaser to expand a ranked bar-list with flags, lazy-loaded on first open). Live-verified against real production data: currently returns exactly one "Rest of World" row (count 1) - correct, since only one country has any global-scope approved creature right now and it's below threshold.

**4. Superadmin PDF export — built and live-verified, real bug found and fixed along the way.** Added a per-school "Export PDF" button to the Registered Schools list, reusing `/reports/pdf/school-overview`'s existing `school_name` query param. That param turned out to be accepted but never actually applied on the superadmin/admin code path - `display_name` (the PDF title) used it, but `teacher_ids` (the actual data) stayed the all-schools list regardless, so a per-school export would have shown one school's name on top of every school's numbers. Never triggered before now because nothing in the UI ever passed `school_name`. Fixed as part of wiring the button. Live-verified with real data: Kairos Montessori (56 check-ins, 12 students, 3 teachers) + Sunshine International School (720 check-ins, 25 students, 5 teachers) sum to exactly the "All Schools" aggregate (776 check-ins, 37 students) - confirms the per-school filter is now genuinely scoping, not just relabeling.

One unrelated anomaly surfaced while pulling real school data for this test, flagged for Jono's awareness, not fixed (out of scope for this list): `school_profiles` has two separate rows both named "Kairos Montessori" (different IDs). Not investigated further.

**5. Scope-button label nuance — fixed.** The "Requested:" display text already correctly showed "Family (private)" vs "Classroom" (from an earlier round), but the actual approve button next to it still always read "✅ Classroom only" regardless. Now reads "✅ Family only (private)" when the submission has no `classroom_id` (parent-submitted), "✅ Classroom only" otherwise - matches the label already used elsewhere on the same card. Portal-only change, syntax-checked via the same `node --check` extraction method used throughout.

**6. Visual polish (per-colour movement, Bonus Items in detail view) — confirmed genuinely complete, no loose ends.** Per-colour movement (`useZoneMovement`, 4 real distinct patterns: sway/hop/shiver/bounce) wired into all 3 real screens. Bonus Items grid renders all 4 categories correctly, locked-item point hints intact, this session's tap-to-replay/celebration additions integrated cleanly with no conflicts. No TODOs or stubs found near either area.

**7. Regression check on 4 earlier features — all confirmed intact.** Reward page button layout, family dashboard icons, "New creatures to evolve!" messaging, and the linked-account icon were all re-read against their original implementation and found structurally unchanged by this session's work. The "New creatures to evolve!" messaging is now driven by more correctly-gated eligibility data (this session's superadmin-approval-gate fix) - strictly more correct, not a regression. One housekeeping note, not a regression: `parent/dashboard.tsx.backup` and `linked-child/[id].tsx.backup` still exist alongside the live files - safe to delete whenever convenient.

**8-12. Lower-priority items — scoped, mostly deferred as genuinely bigger than they sound:**
- **8. Superadmin portal vs app parity**: real gap, not quick - the RN app has zero creature-moderation UI anywhere; full parity means building an entire moderation screen. **Deferred.**
- **9. Registered Schools vs Schools Breakdown merge**: different purposes (CRUD school management vs read-only analytics), different data sources - a real merge means redesigning both, not a label change. **Deferred.**
- **10. UI/UX consistency audit**: real inconsistencies confirmed (21 files with ad-hoc back-button styling, 31 files with inconsistent heading capitalization) - genuine multi-screen work. **Deferred** as its own future pass.
- **11. Set Password + admin email overflow**: Set Password is an inline panel in `settings.tsx`, not a separate screen. Checked the likely admin-email display spots (`settings.tsx`, `admin/dashboard.tsx`) - both already use the safe wrapping pattern (`flex:1`, plain `Text`, no `numberOfLines` clipping), unlike the earlier Spanish button bug. **No confirmed bug found in code as currently written** - recommend Jono verify with a real long email before spending build time; may already be resolved or was never in this exact spot.
- **12. `/creatures/analytics` N+1 — genuinely quick, built.** Confirmed real: one `users` query per unique student with any fully-completed creature, unbounded (scales with total platform-wide unlocks, not the top-10 actually returned). Replaced with a single batched `.in_()` lookup, same result shape. Live-verified: endpoint still returns 200 with correct data after the change.

**Explicitly excluded from this build, confirmed staying excluded**: Hindi/Chinese/Arabic (no real content/native speakers yet), Arabic RTL rebuild (separate major project), app-side drag-reorder for Strategies+Resources (blocked on a library conflict).

All changes this round committed and pushed; Railway deploy confirmed live (new `/creatures/country-leaderboard` route returns 401 not 404 pre-auth, then 200 with a real session). Portal file (`portal100-UPLOAD-2026-08-23.html`) still needs Jono's manual cPanel upload - this round added the per-school PDF button and the scope-button label fix on top of everything already pending upload from the last round.

No new build cut - holding per Jono's standing instruction until he's personally verified this round on Expo Go himself.

---

## A32 — pre-release health check (not a feature round), 2026-08-23

Jono asked for an honest "does everything actually work together" check before cutting the build - full codebase, not just this week's touched files. Results below, item by item, exactly as asked.

**1. TypeScript check, whole codebase.** Not zero errors: 37 pre-existing across the whole frontend (confirmed via `git stash` diff - all 37 predate this week's work). Two were genuinely severe, not just strict-mode noise, and are now fixed:
- `teacher/checkin.tsx`: `pinUnlocked` was read inside a `useEffect` dependency array ~270 lines before its own `useState` declaration in the same component - a real temporal-dead-zone `ReferenceError` on every mount of the teacher check-in screen (JS engines throw on this, not just TypeScript). Fixed by moving the state block up before the effects that use it.
- `subscription/success.tsx`: used `t()` in 3 places without ever destructuring it from `useApp()` - a real `ReferenceError` on the payment success/error screens, which render right after a genuine Stripe payment completes. Fixed.

Also fixed, lower severity: 3 files imported `useWindowDimensions` from `'react'` instead of `'react-native'` (wrong source, but the hook was never actually called in any of them, so dormant rather than a live crash) - removed as dead imports; 2 stale type declarations (`loginWithEmail`'s interface signature, the admin tab union missing `'schools'`/`'users'`) widened to match already-correct runtime behaviour, zero behaviour change. **37 → 24 errors.** The remaining 24 were individually triaged, not just counted - confirmed benign (stale/incomplete type interfaces like `Resource` missing `topic`/`category`, or known TS false-positives like a spread-then-computed-key object literal in `parent/dashboard.tsx` that TS can't statically prove is safe but is exactly the intended toggle-one-field pattern). One exception, deliberately NOT fixed: `teacher/student-detail.tsx`'s "Pause/Resume sharing with parent" button references undefined `BACKEND_URL`/`loadData` AND calls `POST /parent-links/school-sharing`, which doesn't exist anywhere in the backend - this button is fully non-functional (fails silently, empty catch block), but fixing it for real means building a missing backend feature, not a bug fix - flagged for a real decision, not patched.

Backend: `py_compile` clean (no syntax errors). No linter was configured in this project, so installed `pyflakes` fresh to get real coverage - 117 findings, all either unused imports/locals (cosmetic, common in a large single-file backend), 2 harmless unnecessary f-string prefixes, or 42 "dictionary key repeated" warnings clustered inside a large legacy embedded Portuguese-translation dict (pre-existing, not touched by any of this week's work - later duplicate key silently wins, meaning some earlier-written translation strings are dead code, not a functional bug since a valid Portuguese string still shows either way). Flagged, not touched - real cleanup work, out of scope for a health check.

**2. Safety-critical paths - re-verified live, fresh, today, zero regression.** Confirmed via `git diff` that the only backend commit since the safety-gate work touched three isolated spots (per-school PDF filter, the new country-leaderboard endpoint, the analytics N+1 fix) - none inside the gate/scope/delete functions themselves. Then ran a full fresh end-to-end live test against production, not just a diff-based inference: created a real teacher-approved-only (not superadmin-approved) classroom-scoped test creature → confirmed NOT eligible to a real student in that classroom → confirmed a teacher gets 403 attempting to delete it → superadmin-approved it → confirmed NOW eligible to the same classroom's real student → confirmed NOT eligible to a different classroom's student (scope isolation holds) → confirmed superadmin CAN delete it → confirmed it's genuinely gone from `/creatures/global`. All 6 checks passed exactly as expected. Test data fully cleaned up.

**3. Console errors/warnings during core flows.** Delegated a fresh, skeptical pass (not just re-reading what's already known) over check-in, creature submission, and the reward page in the real app (not the portal). Findings: both flows are clean and well-guarded. One confirmed-benign type mismatch in `strategies.tsx` (a `location` field the frontend interface doesn't declare but the backend model genuinely accepts and persists - not silent data loss). One real but harmless finding: `submit-creature.tsx` has an unconditional `console.log` on every screen load (debug leftover, not an error). Creature approval has no screen in the React Native app at all to check - confirmed again this is portal-only (matches the earlier "item 8" parity finding). Bonus Items/EvolutionAnimation code from this week is correctly guarded throughout.

**4. Cold-start crash check, all 5 roles.** No iOS simulator or device is available in this environment, so this is a static-review complement to Jono's own Expo Go pass, not a substitute for it - stated plainly, not glossed over. Traced real routing (`app/index.tsx`) to confirm each role's actual landing screen, then checked hook-call order and first-render data safety on each: student → `student/select.tsx`, teacher → `teacher/dashboard.tsx`, parent → `parent/dashboard.tsx`, school_admin/superadmin → shared `admin/dashboard.tsx` (split by `isSuperAdmin`). Specifically hunted for the same temporal-dead-zone pattern just found and fixed in `teacher/checkin.tsx` (a `useEffect` dependency array referencing a not-yet-declared `useState`) - zero matches anywhere else. No conditional/looped hook calls found (the classic React crash pattern). Lists rendered before their first fetch resolves (`students`/`classrooms`/`presetAvatars`) default to `[]`, not `undefined`, so are safe to `.map()` on first render. All 5 roles: **PASS**, with the environmental caveat above.

**5. Honest overall assessment.** Confident: the safety-critical creature-approval/scope/delete work from this week is solid - re-verified fresh today, not just carried forward from memory. Confident: the two real crash bugs found this pass (`checkin.tsx`, `subscription/success.tsx`) are now fixed and were pre-existing, not introduced by this week's feature work - neither file was touched by anything built this week before this health check. Not fully confident, flagged honestly: (a) the "Pause/Resume sharing with parent" button is genuinely broken and needs a real decision, not a patch; (b) cold-start and console-error checks are as thorough as static review allows, but nothing replaces Jono's own Expo Go pass for anything timing/rendering/device-specific; (c) the 42 duplicate-key backend translation strings are a real, if low-impact, piece of accumulated debt worth a future cleanup pass; (d) two small pieces of confirmed-dead state (`teacher/dashboard.tsx`'s `pendingCreatures`, `parent/dashboard.tsx`'s `studentCreatures`) are fetched but never rendered anywhere - harmless, but worth knowing they exist in case someone later assumes they're wired up.

All fixes from this round committed and pushed; Railway deploy unaffected (backend was not touched this round, only frontend). Still holding on cutting the build until Jono has done his own verification pass.

---

## A33 — portal: Approved Creatures not updating live after approve, 2026-08-23

Real UX bug Jono found live-testing: approving a creature correctly removed it from "Awaiting Final Approval" instantly, but it never appeared in "Approved Creatures" until a hard page reload - the approval itself was correct (data was right after refresh), purely a missing local-UI-update bug. Root cause confirmed: `saGlobalApprove()` only ever updated `saCreatureQueue` and re-rendered the pending list - it never touched or re-fetched `saApprovedCreatures` at all, so only `loadSACreatures()` re-running from scratch (i.e. a reload) ever refreshed that side. Fixed: after a successful approve, re-fetch `/creatures/global` and re-render `saApprovedCreatures` immediately (the exact same call `loadSACreatures()` already makes on initial page load) - both lists now update live with one action, no manual reload needed. Syntax-checked via the same `node --check` extraction method used throughout. Not yet live - part of the same pending `portal100-UPLOAD-2026-08-23.html` upload as everything else this round.

---

## A34 — CRITICAL: subscription/checkout revenue path, 2026-08-23

Jono reported via screenshots: no way to actually proceed to checkout after choosing a plan on either the Parent/Family or Teacher/Educator tab. Investigated as priority 1. Turned out to be two separate, real, stacked bugs on the actual revenue path, both now fixed and live-verified.

**Bug 1 - Subscribe button permanently hidden after trial/subscription expiry.** The button was always genuinely present in code - `isActive` gated it, trusting raw `subscription_status` forever with zero expiry awareness. Confirmed live: nothing in the real trial-start flow (`POST /subscription/start-trial`) ever flips `subscription_status` away from `'trial'` - the only code that ever did that (`/trial/status`) is dead, never called anywhere in the app. So any real user whose free trial lapsed without converting - exactly the people most ready to pay - could never see a Subscribe button again. Confirmed this wasn't a fresh-signup issue (new accounts start at `subscription_status: "none"` and see the button fine) - both of Jono's own long-running test accounts were independently hitting this (`jono@gmail.com`: stuck `active`, expired since 2026-05-19; `jono+teacher@gmail.com`: stuck `trial`).

While tracing this, also found `/subscription/plans` returned a raw dict with no top-level `trial_days`, though `frontend/src/utils/api.ts`'s `getPlans()` type had always expected a wrapped `{plans, trial_days}` shape - so the displayed trial length silently always fell back to a hardcoded 30, disconnected from the real 14 (`TRIAL_DURATION_DAYS`) enforced everywhere else. Fixed both: backend now returns the wrapped shape with the real number; frontend checks real expiry (trial start + real trial_days, or `subscription_expires_at` for a lapsed paid plan) instead of trusting the status string forever.

**Bug 1b (found live-testing bug 1's fix) - the fix over-corrected: valid trials also lost the button.** `isActive` needs to stay true during a genuinely valid trial so the "trial active, X days left" banner keeps showing correctly - but that same flag was also gating the Subscribe button, meaning nobody could ever voluntarily convert early during a still-valid trial, only after it lapsed. Split into two: `isActive` (banner, unchanged behaviour) vs new `canSubscribe` (button - false only when the user has a real, currently-paying, unexpired plan; a trial, valid or expired, can always subscribe). Live-simulated against both real accounts' actual data to confirm: expired-active account -> banner hidden, button shown; valid-trial account -> banner shown ("10 days remaining"), button ALSO shown.

**Bug 2 (found closing the loop on "does clicking Subscribe actually work") - total checkout outage, both plans, unrelated to bug 1.** `POST /subscription/checkout` was 500ing for every real attempt. Root cause: `STRIPE_PRICE_TEACHER_MONTHLY` and `STRIPE_PRICE_PARENT_MONTHLY` (Railway env vars, not in the repo) both held **test-mode** Stripe price IDs while the live secret key was active - "a similar object exists in test mode, but a live mode key was used." This meant the self-serve checkout flow could never have completed a single real subscription, independent of and in addition to bug 1. Did a **read-only** listing of the real live-mode Stripe prices (no creation, no charge) and found two unlabeled live recurring-monthly prices whose amounts matched the app's displayed pricing to the cent - €7.99 and €4.99. Confirmed with Jono before touching production payment config, then updated both Railway env vars to the real live price IDs:
- `STRIPE_PRICE_TEACHER_MONTHLY` -> `price_1TmBJDGVgsNSHYytXsgnmnO8` (was a test-mode ID)
- `STRIPE_PRICE_PARENT_MONTHLY` -> `price_1TmBFSGVgsNSHYytpOIuHIMq` (was a test-mode ID)

Live-verified end to end after redeploy: both plans now return `200` with a real `cs_live_...` Stripe checkout session and a genuine `checkout.stripe.com` URL. Both test sessions immediately expired via the Stripe API afterward so no live payable links were left dangling. Honest limit stated to Jono: actually *completing* a payment requires a real human entering a real card on Stripe's own hosted page - not something I can or should simulate myself.

All frontend changes committed and pushed; Railway env var changes deployed and confirmed live. This was the actual, complete blocker on the entire subscription revenue path - both bugs needed fixing together for it to work end to end.

**Item 2 (School Package enquiry form) - built and live-verified.** The button previously opened `mailto:hello@classofhappiness.com`, which silently fails for anyone without an email app configured on their device - a real, if less visible, drop-off point right at the school-sales funnel. Replaced with an in-app Name/Email/Message form (bottom-sheet modal, matching the existing add/edit-strategy modal pattern already used elsewhere in the app) that submits directly via `fetch` to the same Formspree endpoint (form ID `mlgzvorz`) already proven on the live website's own contact form - same inbox either way, `_subject` set to "School Package Enquiry" to match the old mailto's subject line. Live-verified with a real submission matching the app's exact request shape (JSON body, `Content-Type`/`Accept: application/json`) - Formspree returned `{"ok":true}`; a real test enquiry landed in the real inbox, flagged to Jono as safe to disregard.

**Bug 3 (found live on Expo Go, AFTER build 22 was already cut and uploaded) - Subscribe crashed on native with "Cannot set location.href".** Jono hit this testing as `jono@gmail.com` (parent). Root cause: `handleSubscribe` used `typeof window !== 'undefined'` alone to decide web vs native - but Hermes/Metro can define a global `window` on native too, so this branch was wrongly taken on-device, then `window.location.href = url` threw since RN's `window.location` isn't a real navigable object. Every other web/native check in this codebase already correctly gates on `Platform.OS === 'web'` first (`auth/callback.tsx`, `AppContext.tsx` x2, confirmed via grep) - this was the one place using the bare, unreliable check alone. Fixed to match. This was the actual last step of the checkout flow - session creation was already confirmed working (bug 2 above), but the redirect to the real Stripe checkout page never happened on native until this fix.

**Important: build 22 (already uploaded to App Store Connect, and the Android artifact already produced) contains this bug.** The fix above is in the repo but not in any built binary yet - a new build is needed for this to actually reach a real device. Not rebuilt yet, pending Jono's go-ahead (and ideally a bit more Expo Go testing first, since this is exactly the kind of thing that workflow exists to catch before a build goes out).

---

## A35 — free-tier creature caps (collection + submission), 2026-08-24

Investigated before building, per Jono's ask: confirmed via direct source reading (grepped `submit_creature`, `get_eligible_creatures`, `start_creature` for any subscription/trial/tier reference - zero hits in any of them) that community creature access was completely unlimited for free accounts, no exceptions. This was the real, confirmed baseline the new caps were built against.

**Built, per Jono's confirmed spec:**
- **Collection cap**: free-tier accounts get 1 fully-evolved community creature per emotion colour, ever, pooled per real student (not per account - confirmed decision). New `_free_tier_colour_locks()` helper, built on the existing `_creature_unlocks_for()` real-student-aware lookup, returns which colours are already "spent." Enforced in `start_creature` (blocks moving to a genuinely different creature once a colour's capped - the existing "must fully evolve current before starting a new one" sequencing rule already in that endpoint made this a clean, natural insertion point) and surfaced as a new `locked` field on `/creatures/eligible` - locked creatures still show, faded not hidden, per spec.
- **Submission cap**: 2 total creature submissions, any status - a rejection still counts as a used try, per Jono's explicit decision. Same `free_tier_limit|` pattern already used for students/classrooms/children, applied to `creature_submissions`.
- **New `_is_genuinely_free_tier()` helper**: unlike every existing `free_tier_limit` check in this file (all of which treat any non-"none"/"free" status as "not free," including a genuinely expired trial or lapsed paid plan), this one checks real expiry - a deliberate, acknowledged divergence per Jono's explicit call ("build correctly now"), not an oversight. The older checks are flagged, not silently copied. Also respects the same school-coverage bypass every other free-tier check already uses.
- **Frontend**: locked cards in World Creatures show faded with a lock icon; tapping shows an informational-only upsell with no embedded Subscribe button (a kid could be the one tapping a creature card, not the parent/teacher who'd decide to subscribe - Jono's explicit requirement). Submission-cap errors reuse the existing "Not Now / See Plans" pattern from `profiles/create.tsx` etc., since submitting is a more deliberate account-owner action than browsing.

**Live-verified end to end against production, real data, full cleanup after:**
- Submission cap: real free-tier test account - 2 submissions succeeded, 3rd correctly blocked with the `free_tier_limit` message.
- Rejected-submissions-count check: rejected both of the free account's 2 submissions, confirmed a 3rd attempt was *still* blocked - proves rejections genuinely count toward the cap, not just pending/approved.
- **Expiry fix, the core ask**: a genuinely lapsed trial account (`trial_started_at` 20 days ago, past the real 14-day `TRIAL_DURATION_DAYS`) hit the exact same 2-submission cap as a real free account - confirms the expiry-aware check works correctly for a real lapsed account. A genuinely valid trial account (started 2 days ago) submitted 4/4 successfully with zero cap applied - confirms no false positives against a real, still-valid trial.
- Collection cap: created 2 real approved+superadmin-approved blue community creatures scoped to a real classroom; started and fully-evolved the first for a real test student (`dstu001`) under a temporarily free-tier account; the second was correctly blocked (`403 free_tier_limit`) from `start_creature`; `/creatures/eligible` correctly showed the first as `locked:false` (already theirs) and the second as `locked:true` (faded, per spec). Negative control: with the same account genuinely active again, the same second-creature start attempt succeeded with zero restriction - confirms the cap only applies to real free-tier accounts, not a blanket block.
- All test users, submissions, unlocks, and temporarily-modified shared fixtures (`demo_teacher_001`, `dstu001`) fully cleaned up and confirmed restored to their exact original state afterward.

`tsc` clean (24, same pre-existing baseline, zero new errors), backend compiles clean. Committed and pushed; not yet in any built binary (same as the native-checkout-redirect fix above - both are in the repo, neither is in build 22).

---

## A36 — promo code (HAPPYCLASS2026) investigation + 14-vs-30-day fix, 2026-08-25

Jono asked about a promo/bypass code he remembered but had no record of from this session - investigated real source first, no changes, before touching anything.

**Findings reported:** it's a hardcoded `PROMO_CODES` dict in `backend/server.py` (two codes actually exist - HAPPYCLASS2026 and CLASSOFHAPPINESS2026, both 30-day trials), zero Stripe involvement, redeemed via a real Settings-screen code-entry field hitting `POST /subscription/redeem-trial-code`. While tracing the real redemption path, found the actual bug: that endpoint writes its 30-day expiry into `subscription_expires_at`, but every trial-status expiry check in the file (`check_subscription_active`, and last night's new `_is_genuinely_free_tier`) only ever computed expiry from `trial_started_at` + the standard 14-day `TRIAL_DURATION_DAYS`, completely ignoring `subscription_expires_at` - so a promo-code account silently lost real access after 14 of its advertised 30 days, including hitting the brand-new creature caps 16 days early. Also found a second, apparently dead endpoint (`/auth/promo-code`) writing to an unused `promo_trial_ends_at` field that nothing in the frontend ever called.

**Fixed, both items Jono asked for:**
- New shared `_trial_is_valid(user)` helper: checks `subscription_expires_at` first when present (the real promo-granted value), falls back to the standard 14-day `trial_started_at` window otherwise - a regular trial via `/subscription/start-trial` never sets `subscription_expires_at`, so this doesn't change behaviour for non-promo trials. Both `check_subscription_active` and `_is_genuinely_free_tier` now call this one shared helper instead of each duplicating the same buggy logic.
- Removed the dead `/auth/promo-code` endpoint and its `PromoCodeRequest` model entirely - confirmed via grep it was genuinely unreachable from the frontend before deleting.

**Live-verified end to end against production, real redemption, real cleanup:**
- Created a fresh free-tier test account, redeemed the real `HAPPYCLASS2026` code through the actual endpoint a real user would hit - confirmed `subscription_expires_at` set to exactly 30 days out.
- Simulated day 20 (past the old broken 14-day cutoff, within the real 30-day window): `/subscription/status` correctly returned `is_active: true`; 3 creature submissions in a row succeeded with zero cap applied - both checks correctly honoured the real 30-day window.
- Negative control, simulated day 35 (past the real 30-day window): `/subscription/status` correctly flipped to `is_active: false`; a further submission correctly hit the `free_tier_limit` cap - confirms this is a genuine, bounded fix, not a permanent bypass.
- All test data (account, sessions, submissions, submission code) fully cleaned up and confirmed gone afterward.

Not touched, flagged only: two admin/portal user-listing endpoints (`/school-admin/users`, `/admin/users`) still include the now-permanently-stale `promo_trial_ends_at` column in their SELECT list - harmless (nothing writes to it anymore, so it'll just read null), left alone since it's outside what was asked. Also flagged, not fixed: the subscription screen's frontend trial-days display (`trialDays`, from `/subscription/plans`) is a single global 14-day figure, not personalized - a promo-code user's "days remaining" banner text would still be off past day 14, even though the real backend access is now correct. Cosmetic only, doesn't gate anything.

**Confirmed deferred by Jono**: this is a minor UI polish item for a future round, not urgent - real access is what matters and that's already correct. Logged here so it doesn't get lost: the fix would mean the subscription screen reading the user's own `subscription_expires_at` (when present, for a promo-granted trial) instead of the global `trial_days` figure to compute "days remaining," mirroring the same `subscription_expires_at`-first pattern `_trial_is_valid` now uses on the backend.

`tsc`/backend compile clean, committed and pushed, live on production (backend-only change, no frontend touched this round).

---

## A37 — STRIPE_TEST_MODE flipped on for Jono's end-to-end payment test, 2026-08-25

Jono asked to flip `STRIPE_TEST_MODE` on to safely test the full subscription flow with a real Stripe test card before cutting build 23. Flipped it (Railway env var), which immediately surfaced a real, previously-latent bug: `STRIPE_PRICE_IDS` always read the plain (live-mode) `STRIPE_PRICE_TEACHER_MONTHLY`/`STRIPE_PRICE_PARENT_MONTHLY` env vars regardless of `STRIPE_TEST_MODE`, unlike `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` which already correctly split test/live - so checkout creation failed outright in test mode (a live-mode price ID against a test-mode key, which Stripe correctly rejects).

Did a **read-only** listing against the real Stripe test-mode account (no creation, no charge) and found the fix didn't need new Stripe objects at all: the two price IDs that were sitting in the plain env vars *before* yesterday's live-price fix (`price_1U5oDSGVgsNSHYytSC7Wr7jm` teacher, `price_1U5oENGVgsNSHYytxQePUM0X` parent) were never wrong - they're the genuine test-mode prices, correctly priced (€7.99/€4.99) and nicknamed, just never wired to a live/test-aware env var pair. Added two new Railway env vars (`STRIPE_PRICE_TEACHER_MONTHLY_TEST`, `STRIPE_PRICE_PARENT_MONTHLY_TEST`) holding them, and gave `STRIPE_PRICE_IDS` the same test/live split the secret key already has.

Live-verified after redeploy: real checkout session creation for both plans now returns genuine `cs_test_...` sessions with valid `checkout.stripe.com` URLs. Both test sessions expired immediately after via the Stripe API, nothing left dangling.

**Current state: `STRIPE_TEST_MODE` is ON in production right now** - confirmed safe for Jono to go through a real subscription attempt with a Stripe test card (4242 4242 4242 4242). This is a global toggle, not per-session - real customers attempting to subscribe during this window would also hit the test key. Needs to be flipped back to `false` once Jono's done testing, before cutting build 23.

---

## A38 — post-payment UX fixes + cancel-to-free safety investigation, 2026-08-25

Three items from Jono's live payment testing.

**1. Navigation bug - fixed.** `subscription/success.tsx`'s "Continue" button was hardcoded to `/teacher/dashboard` regardless of who actually paid - a parent subscribing landed on a screen built for teachers. Now goes to `/settings`, where the subscription status this payment just changed is the actual thing worth seeing next.

**2. Settings subscription display - fixed, confirmed no new plumbing needed.** The status text used to just be the bare plan key (`subscription_plan`, a lowercase machine value like `"parent"`/`"teacher"` set by the Stripe webhook via `_get_plan_from_price`) with no indication it was even active. Now reads "Parent Subscription Active" plus a real renewal countdown. Confirmed by reading the webhook handler directly: `subscription_expires_at` already holds the real Stripe `current_period_end` (set on every `customer.subscription.created`/`.updated` event) - no new field, no new Stripe API call needed, purely a display fix using data already being stored correctly.

**3. Cancel-to-free investigation - the critical safety question, answered with both code proof and a live test.** Grepped every `UPDATE`/`DELETE` against `creature_submissions` and `creature_unlocks` in the whole file: every single one is either real progress tracking (`_progress_community_creature`), content moderation (approve/reject/superadmin hard-delete), or the account-deletion purge path - **nothing anywhere touches either table based on subscription status changing**. The Stripe `customer.subscription.deleted` webhook handler only ever updates `users.subscription_status`/`subscription_expires_at`, confirmed by reading it directly.

Then proved it live rather than trusting the code read alone: real test account, free tier, submitted 2 creatures (hit the cap, 3rd correctly blocked) → simulated subscribing (`subscription_status: active`) → submitted a 3rd successfully, uncapped → simulated cancelling back to free (`subscription_status: none`) → confirmed the real DB count stayed at 3, not reset → attempted a 4th submission → correctly blocked again, cap still maxed at the same level from before "cancelling," not reset to 0. Also confirmed there's currently no self-serve "cancel subscription, keep account" endpoint at all today - the only existing cancellation logic lives inside the full account-deletion flow, which cancels the real Stripe subscription as a side effect of deleting the whole account, not as a standalone downgrade action.

**Verdict for Jono: safe to build.** The free-tier caps are genuine, permanent, DB-level lifetime totals with zero relationship to `subscription_status` - a subscribe → exceed-free-limits → cancel → resubscribe cycle cannot reset or bypass them by design, confirmed both by reading every code path that touches these tables and by reproducing the exact cycle live. Not built yet - this was investigation only, as asked.

Items 1/2 committed, pushed, live. Item 3 was investigation-only, no code changes.

---

### FUTURE FEATURE (ready to build, not urgent) — cancel-to-free self-serve downgrade

Risk question is fully answered (see A38 above) - safe to build whenever there's time, no rush. Logging the real scope now so it's not lost:

- **What's missing today**: no self-serve "cancel subscription, keep account + progress" path exists at all. The only cancellation logic lives inside the full account-deletion flow (cancels the real Stripe subscription as a side effect of deleting the whole account) - a genuinely different, much bigger action than what this feature needs.
- **What it needs, concretely**: a new endpoint (e.g. `POST /subscription/cancel`) that cancels the real Stripe subscription (`stripe_lib.Subscription.cancel` or `.modify(cancel_at_period_end=True)` - worth deciding whether it should downgrade immediately or let the current paid period run out first, matching standard SaaS practice) and sets `subscription_status` back to `none`/`free` without touching the account, students, creatures, or any other data. Frontend: a real "Cancel Subscription" action in Settings, probably behind a confirmation step explaining what they keep (all progress/creatures) vs. what changes (the free-tier caps apply going forward).
- **Confirmed safe**: the free-tier caps (1 fully-evolved creature per colour, 2 total submissions) are genuine, permanent, DB-level lifetime totals with zero relationship to `subscription_status` - live-verified end to end that a subscribe → exceed-limits → cancel → resubscribe cycle cannot reset or bypass them.

---

## A39 — i18n audit + Italian voice clips (28/28 live), 2026-08-25

**i18n audit** (requested while Jono investigated a real "Portuguese pages don't fully translate" report - no code changes, investigation only):
- Confirmed root cause of the immediate PT complaint: 6 keys added to `en.json` over the last 2 days (Bonus Items labels, country-teaser text, "Back to Settings") were never backfilled into `pt.json`/`es.json`/`fr.json`/`de.json`/`it.json` - any page using them shows English mid-page in every non-English language. Small, not yet fixed - logged here, not urgent per Jono.
- Found a bigger, older, pre-existing gap while auditing: 104 keys the app's code actually calls via `t()` are missing from `pt.json`, and 98 of those are missing from `en.json` too - meaning those specific strings were never wired into the translation system in ANY language; whatever shows today is a hardcoded JSX fallback, not a real translation. Also found `en.json` itself is surprisingly small (223 keys) versus `pt.json`/`es.json` (732) and `fr.json`/`de.json`/`it.json` (755) - the non-English files carry a lot of older bulk content English never got. Not fixed - logged for a future dedicated i18n pass, scope is bigger than a quick patch.

**Italian voice clips - full rollout, 28/28 canonical clips, live-verified.** Real investigation first (see the back-and-forth on file paths/formats in the actual session - worth noting Jono caught and corrected two of his own early mistakes, including a genuinely wrong path and a false "already converted" claim, both caught by direct verification before any upload happened, not assumption). Confirmed the real, complete voice-clip picture via live source and Storage inspection:
- **Canonical 28** (`{lang}/{key}.m4a`, `VOICE_CLIP_KEYS`) - 4 colours + 24 helpers. This is the only fully-wired, production-ready system.
- **8 "batch" phrases** (Great job, How are you feeling today, etc.) - real files already exist at the Storage bucket ROOT for en/pt (16 files, flat naming, no language folder), but only 1 of the 8 (`Great_job.m4a`/`Muito_bem.m4a`) is referenced by any endpoint (`/voice-clips/reward`). The other 7 are uploaded and genuinely orphaned in every language, not just Italian.
- **4 "longer phrases"** ("Let's choose a helper," "Choose your colour," etc.) - no existing code path in ANY language. Confirmed via full grep of every voice-clip constant/endpoint in the file.

All 28 canonical Italian files uploaded to `voice-recordings/it/{key}.m4a` - real converted `.m4a` files, verified by binary content (not extension) before upload, since an earlier version of the same files were genuine `.opus` audio mislabeled/miscommunicated as already-converted. Each filename mapped against the real `strat_{colour}_{n}` ordering in `en.json` (not guessed from filename order) so every clip lands on the correct helper. `VOICE_CLIP_LANGUAGES` updated to include `"it"`. Live-verified: `GET /voice-clips?language=it` returns exactly the expected 28 keys, zero missing, zero extra; spot-checked 4 real URLs, all returned real audio bytes with `200`.

**Logged as separate future work, not Italian-specific - affects every language equally, per Jono's explicit call:**
1. **Encouragement-phrase wiring**: 7 of the 8 batch phrases (real files, every language) have no endpoint/frontend hookup at all. Needs real backend (a proper `{lang}/{key}.m4a` structure or an extended `REWARD_VOICE_CLIP_FILES`-style map covering all 8, not just 1) and frontend wiring to actually play anywhere.
2. **Longer app-narration phrases feature**: the 4 "let's pick a helper"-style clips need a real feature built from scratch - deciding which UI moments they'd play at, then the same backend+frontend wiring. Nothing to reuse here, this is new scope.

Not uploading Italian versions of either until the underlying feature exists in some language first - uploading now wouldn't play anywhere, per Jono's own reasoning.

---

## A40 — CRITICAL: access-control investigation and fix (password bypass, auto-account-creation, dashboard role-gating), 2026-08-26

Jono reported someone logged into the Android APK with no password and no role selected, but could see both dashboards. Treated as the single highest-priority item of the entire engagement, ahead of everything else including i18n. Full investigation against real source, then fixed and live-verified, in this order.

**Investigation findings, all confirmed against real source, nothing assumed:**
- `POST /auth/email-login` only ever checked a password `if user.get("portal_password")` - i.e. only for accounts that had explicitly opted into Settings' "Set Password" flow. Any account that never did (the default state) could be logged into by anyone who typed that email - no verification at all.
- Unrecognized emails silently created a brand-new real account with `role: "teacher"` hardcoded - not a demo/sandbox account, a genuine row in `users`.
- `app/index.tsx` showed Teacher and Parent buttons unconditionally to every logged-in user; neither `teacher/dashboard.tsx` nor `parent/dashboard.tsx` had any role check at all. Confirmed via direct grep - zero enforcement either direction.
- **A second, more severe bug found independently while investigating, not something Jono had flagged**: `POST /auth/reset-password-request` returned the real reset token directly in its API response, and `forgot-password.tsx` auto-filled it into the reset form. "Forgot password" required nothing but knowing the target's email - a full account-takeover primitive that worked even against accounts that already had a real password, since completing the flow unconditionally overwrites `portal_password`. Confirmed there is no email-sending infrastructure anywhere in this codebase (no SMTP/SendGrid/etc.) to fix this the "right" way today.
- Real blast-radius check before deciding the fix: **46 of 47 real accounts (98%) have no password set** - 25 teachers, 17 parents, 3 school_admins. An immediate hard block on no-password login would have locked out nearly the entire real user base with no self-service recovery path, especially right after disabling the broken reset flow.

**Fixed, in agreed order:**
1. **Reset-token leak - closed immediately, no downside.** `/auth/reset-password-request` no longer returns the token; same honest response whether or not the email exists (doesn't leak which emails are real accounts either). Token generation/storage untouched so a real email-delivery flow can be wired in later without a schema change. Frontend shows the honest "contact support for now" message instead of silently advancing to a token-entry step nobody has a token for, with a manual "I already have a reset code" path for Jono to hand a verified user a code out-of-band.
2. **Force-password-on-next-login - the agreed smallest safe fix for the password bypass**, not a hard block, given the 98% blast radius. New `has_password` field (from a new `_public_user()` sanitizer - also fixes a real, separate issue: `/auth/me`, `/auth/email-login`, and `/auth/google` were all returning the raw user row to the client, including `portal_password`, a real bcrypt hash). `app/_layout.tsx` redirects any authenticated session with `has_password === false` to a new hard-gated `set-password-required.tsx` screen (no back button, `gestureEnabled: false`) on every navigation until a real password is set - closes the gap for each real account the moment they're next active, without an immediate mass lockout.
3. **Auto-account-creation on unrecognized email - removed.** `/auth/email-login` now returns a real 404 "no account found" instead of silently creating one. `/auth/google` still creates real accounts on first sign-in, deliberately left alone - Google itself verifies the email is genuinely owned, that's real identity verification, not a bypass.
4. **Dashboard role-gating - built to Jono's exact intended design.** Parent-only accounts are now locked out of `teacher/dashboard.tsx`; teacher accounts can still reach both (many real teachers are also parents). Caught and fixed a real rules-of-hooks violation while building this - the first draft put the early-return before some of the component's own hooks, which would have thrown/behaved inconsistently; moved to after every hook is declared, the only safe place for a conditional return in a function component.
5. **Found and fixed a second bug live-verifying fix #3**: the "no account found" 404 came back as a 500 instead. Root cause: the inner try/except around user lookup had a broad `except Exception` that also caught every `HTTPException` raised inside it - not just the new one, but the *pre-existing* "Incorrect password" 401, "Admin PIN required" 403, and "scheduled for deletion" 403 checks too. All of them were silently being rewrapped into 500s, real status code and message never reaching the client, this whole time. Fixed with an `except HTTPException: raise` before the generic handler.

**Live-verified against production, real accounts, real cleanup:**
- Unrecognized email → real `404`, confirmed no phantom account created in the DB afterward.
- Existing real no-password account (`sarah.mitchell@sunshine-school.com`) → `200`, `has_password: false`, confirmed `portal_password`/`reset_token` no longer present in the response at all.
- PIN-protected account with wrong PIN → confirmed real `403` with its real message now (would have been a `500` before fix #5).
- Reset-token endpoint → confirmed real response contains no `token` field of any kind; confirmed the token is still generated/stored server-side (for future real email wiring) via direct DB read, then cleared the test token from the real account.

**Not independently live-verifiable**: dashboard role-gating is a client-side redirect with no API surface to test directly - confirmed via type-checking and careful manual trace (including catching the hooks-order bug above), but needs Jono's own device pass for final visual confirmation, same limitation as every other frontend-only change this engagement.

All committed, pushed, deployed, and live on production. This was treated as fully blocking - no other work continued until this was complete.

## A41 — Silent role auto-sync removed, strategies fallback bug fixed, 2026-08-26

Follow-up from A40: investigating whether a real email/password signup flow exists (it doesn't - see A40 fix #3, auto-creation was the only path) surfaced a related, lower-severity but real design flaw - `PUT /auth/role` was being called automatically and silently on component mount in two places, meaning role was never a stable, deliberate choice.

**Found (both confirmed via grep, no other call sites exist):**
- `parent/dashboard.tsx` silently called `authApiExtended.updateRole('parent')` inside its data-load try block on every mount.
- `teacher/resources.tsx` silently called `authApiExtended.updateRole('teacher')` in a mount-time `useEffect`.
- Now that role is a real access boundary (A40's dashboard role-gating), silently re-deriving it from whichever screen was last opened is no longer just sloppy - it could silently undo a deliberate role choice just by visiting the other screen.

**Fixed:**
- Both silent calls removed outright, along with the now-unused `authApiExtended` imports in each file.
- New explicit "Account Type" section added to `settings.tsx`: shows the account's current role, with a "Switch to Teacher/Parent account" button gated behind an `Alert.alert` confirmation dialog before calling `PUT /auth/role` and refreshing state via `checkAuth()`. This is now the only way role changes.
- New translation keys (`account_type`, `switch_account_type_*`, `switch_to_teacher`/`switch_to_parent`) added with real translations to en/pt/es/fr/de/it (hi/zh remain part of the already-logged, not-yet-fixed i18n gap).

**Live-verified against production** with a temporary test account (created and deleted via direct Supabase REST calls): confirmed `GET /auth/me` reflects `role: parent` initially, confirmed a deliberate `PUT /auth/role` call still correctly flips it to `teacher` and `GET /auth/me` reflects the change immediately afterward - i.e., the backend endpoint itself is untouched and still works correctly, only the frontend's silent auto-calls were removed.

`npx tsc --noEmit` run after all changes: 24 errors, identical to the established pre-existing baseline - no new errors introduced.

Also included in this same commit: the previously-queued `strategies.tsx` fallback-strategies bug fix (removed a stale, incomplete 4-of-6-item hardcoded English fallback list; added a real retry-once-then-honest-error-state with a working "Try Again" button instead).

Committed and pushed (`3c48fc69`), no backend changes so no Railway deploy needed. This closes out tonight's urgent-tier queue.

## A42 — Real email/password signup with role picker, 2026-08-26

Item #2 from the A40/A41 follow-up: after removing auto-account-creation from `/auth/email-login` (A40) and silent role auto-sync (A41), there was genuinely no email-based way left to create a new account - Google sign-in was the only real path. Scoped and built as agreed, "next-in-queue" not emergency-tier, once A40/A41 were fully wrapped and verified.

**Investigated real source first, per instruction, rather than guessing:**
- **Teacher-school linking**: existing mechanism is `POST /school/generate-invite-code` (school admin generates a `SCH-XXXX-XXXX` code) redeemed via `POST /school/join` by an already-authenticated teacher - this already lives in Settings (`schoolInviteCode` state, `handleJoinSchool`), entered *after* the account exists, not at creation time. Confirmed via grep this is the only teacher-school link path (besides a superadmin directly creating a `school_admin` account). New teacher accounts (including every existing Google-signup teacher) already start with no `school_admin_id`/`school_name` - that's the normal, expected starting state, not a gap.
- **Parent-child linking**: existing mechanism is a per-*student* 6-character `link_code` (`POST /students/link`), generated by the teacher for one specific child, entered by the parent from a modal inside `parent/dashboard.tsx` itself (`handleLinkChild`) - confirmed via grep, this is unrelated to any class/school-wide code, and the parent typically doesn't have it until after the teacher hands it over, often after the child is already enrolled.
- **Conclusion, confirmed against source**: neither linking mechanism belongs on the signup screen - both are already real, working, post-signup steps that a fresh Google-signup account goes through identically. Adding either field to the new signup screen would have built a second, inconsistent way to do something that already works. The new signup screen intentionally stays to email + password + role only, landing the new account in exactly the same "not yet linked to anything" starting state a fresh Google sign-in produces.

**Built:**
- `POST /auth/signup` (backend): validates email format, password ≥ 8 chars, name present, role is exactly `teacher` or `parent` (no default, no admin/superadmin self-serve path); 409s on an existing email pointing the user at Sign In instead; hashes and stores the password immediately (never deferred, same standard as the A40 force-password fix); creates a real session token; returns `_public_user()` (so `has_password: true` is present from the very first response - confirmed this means the A40 force-password-required redirect does not fire right after signup, since it's keyed off exactly that field).
- `frontend/app/auth/signup.tsx` (new screen): name, email, password, confirm password, and an explicit two-button Teacher/Parent picker (no default selected - must be deliberately chosen). On success, routes straight to `teacher/dashboard` or `parent/dashboard` per the chosen role, skipping the generic role-picker landing screen since the choice was just made explicitly.
- `AppContext.tsx`: new `signupWithEmail()` - deliberately re-throws on failure (unlike `loginWithEmail`/`loginWithGoogle`, which swallow errors into a generic `Alert.alert`) so the signup screen can show the real error inline (e.g. the 409 "account already exists" message points the user at Sign In specifically).
- `auth/login.tsx`: added a "New here? Create an account" link to the new screen; fixed two lines of copy that A40 made factually wrong ("New users are created automatically" / "Password is optional" - neither has been true since A40's auto-creation removal and force-password fix). Full i18n pass on this screen remains separately logged/deferred, per last night's audit.
- Google Sign-In untouched - this is a genuine addition, not a replacement.

**Live-verified against production** (temp accounts created via the real endpoint, cleaned up after):
- Teacher signup → `201`-equivalent success, `role: teacher`, `has_password: true`, no `portal_password` in the response.
- Parent signup → same, `role: parent`.
- Duplicate email → real `409` pointing at Sign In.
- Password under 8 characters → real `400`.
- Invalid/missing role (e.g. `"admin"`) → real `400`, can't self-serve into a privileged role.
- Logging in immediately afterward with the real password just set at signup → `200`, confirms the password is genuinely usable right away, not just stored.
- Wrong password against the new account → real `401` "Incorrect password" - confirms the account isn't accidentally passwordless despite going through a different code path than `/auth/set-password`.
- `has_password: true` from the very first response confirms the A40 force-password redirect cannot awkwardly re-trigger immediately after signup.

`npx tsc --noEmit`: 24 errors, identical pre-existing baseline, none in any file touched this round.

Committed and pushed (`06ca3297`), deployed to Railway, confirmed Online before live-verifying against the real deployed endpoint (not local).

## A43 — i18n gap sweep and fix: 222+69+26+83 keys, submit-creature.tsx, login/signup, and 4 more hardcoded screens, 2026-08-26

Worked through last night's i18n audit findings in the order Jono set, verifying scope against real source at each step rather than trusting the compacted summary of last night's audit numbers.

**1. Six recently-added keys backfilled into pt/es/fr/de/it** — already done in an earlier round tonight (`back_to_settings`, `countries_joined_teaser`, `new_move_unlocked`, `new_outfit_unlocked`, `new_food_unlocked`, `new_home_unlocked`); confirmed via key-diff, no action needed.

**2. The pt/es key gap** — re-derived from real source instead of trusting the "96-key" recollection: extracted every literal `t('key')` call across `parent/dashboard.tsx`, `student/zone.tsx`, `parent/widget.tsx`, `teacher/resources.tsx`, `parent/checkin.tsx`, `parent/my-wellbeing.tsx`, `settings.tsx`, `parent/family-member-stats/[id].tsx`, `teacher/students.tsx`, diffed against `en.json`'s real key set (comments stripped) — found 222 genuinely undefined keys, meaning every one of these screens was silently falling back to hardcoded English regardless of language. Also found ~14 real key-reuse bugs along the way (same key, incompatible meanings in different screens — e.g. `stats` meaning both "Stats" and "Status", `teacher_resources` meaning both the page title and a "Reviews" header) and split each into its own key rather than picking one meaning and silently breaking the other. **Mistake caught and fixed before it shipped**: first backfill pass blindly overwrote pt.json/es.json with fresh translations for all 222 keys — a pre/post diff showed ~128 of those keys already had real, previously-authored pt/es translations from earlier work that would have been silently clobbered. Reverted and redid it as add-only (never overwrite an existing value); the real residual gap was 94 keys for pt/es, 86 for fr/de/it — 94 lines up almost exactly with Jono's own "~96-key" recollection once the real pre-existing coverage is accounted for.

**3. `submit-creature.tsx`** — confirmed via grep it was genuinely 100% hardcoded (zero `t()` calls in the whole file, including a destructured-but-unused `t`). Wired all 6 step-screens (tutorial, code, details, scope/country, photos, review) and every `Alert.alert`. 69 new keys, real translations in pt/es/fr/de/it; reused the 5 existing `creature_tip_*` keys and `creature_tutorial_title` instead of duplicating them.

**4. `strategies.tsx` fallback bug** — already fixed earlier tonight (retry-once + honest error state replacing the stale 4-of-6-item hardcoded English list). Confirmed still in place, no action needed.

**5. `auth/login.tsx`** — last night's most severe finding, the actual first screen every non-English user sees. Wired every remaining label/placeholder/button/error (previously only `pin`/`enter_pin` were real `t()` calls). Also wired `auth/signup.tsx` (built earlier tonight, same 100%-hardcoded state) in the same pass rather than leaving it as a second obvious gap. 26 new keys.

**6. Sweep for other hardcoded-JSX screens** — re-ran the heuristic (t()-call density vs. literal JSX text) across all of `app/`. Found no new, previously-undiscovered screens — the 4 worst offenders were exactly the 4 already flagged last night and not yet fixed: `kiosk/index.tsx`, `about.tsx`, `auth/forgot-password.tsx`, `subscription/index.tsx`. Fixed all four (83 new keys):
- **`subscription/index.tsx`** had a real live bug found while wiring it: it already called `t('trial_active')`/`t('trial_active_desc')` for three different status-banner states, with local English fallbacks. Both keys got real values earlier tonight for *other* screens' needs — which would have made this screen start showing a doubled checkmark and the wrong sentence entirely the moment those keys resolved to something non-empty. Split into `trial_active_banner`/`plan_active_suffix`/`access_active_banner` before it could ship broken.
- **`about.tsx`**: wired the UI chrome (headers, version/copyright) but *deliberately left* the long GDPR/COPPA disclaimer, privacy-policy, and terms-of-use paragraphs in English. That's dense compliance prose, not routine UI copy — translating it accurately is a real-stakes legal-language task that deserves a dedicated reviewed pass, not an ad hoc render in the middle of a UI i18n sweep. Flagged explicitly rather than silently machine-translating it or silently skipping the file.

**Totals**: 400 new translation keys added this round (222 + 69 + 26 + 83), real translations (not placeholders) for every one across pt/es/fr/de/it, verified programmatically after every merge that pt/es/fr/de/it have exact key-parity with `en.json` and that zero pre-existing translations were overwritten. `npx tsc --noEmit` re-run after every commit: 24 errors, unchanged pre-existing baseline throughout.

Committed in 4 stages (`d05bab80`, `59193258`, `7545599d`, `1d62abff`), each pushed immediately.

**Not done, deliberately deferred**: the long legal-prose sections of `about.tsx` (flagged above); hi/zh were not backfilled this round (they were already excluded from every prior i18n round this engagement, tracked as a separate, lower-priority gap).

**Live-verification**: attempted screenshot-based verification on the real running app (home, check-in strategies, wellbeing-support modal, creature-submission) in pt/es. Not possible in this environment - macOS 12.7.6's Playwright no longer supports Chromium here, no system Chrome installed, no iOS Simulator or Android tooling available either. Did not fabricate screenshots. Fell back to data-level verification instead: extracted every real `t()` key used on each flagged screen from source and confirmed the literal committed pt/es JSON values that would actually render. Home, creature-submission, and the wellbeing modal all came back clean (every key present, real distinct pt/es text, no fallback-to-English).

**Caught two real gaps this way that the sweep above missed** (fixed immediately, same session):
- `try_again` and `help_request_hint` were missing from ALL SIX language files including `en.json` - the "Try Again" button from this session's own `strategies.tsx` reliability fix, plus `creatures.tsx` and `subscription/success.tsx`, were silently on the inline JSX fallback in every language.
- `choose_helpers`, `loading_helpers`, `skip`, `tap_helpers_green`, `tap_helpers_other`, `write_sentence` were missing from `en.json` specifically, while pt/es/fr/de/it already had real values from earlier work - not a functional bug for those languages (their own file resolved the key correctly already), but a real completeness gap in the English source of truth. Backfilled using the existing JSX fallback text as canonical English.

Checked each key against every real call site first (no hidden key-reuse conflicts this time). Fixed and pushed (`53d2bb8a`). Full pt/es/fr/de/it parity with `en.json` reconfirmed; `npx tsc --noEmit` still 24 errors, unchanged baseline.

**Honest bottom line on live-verification**: the data-level check confirms the right text is committed and would render given a working `t()` lookup - it is not the same as a human or a real device confirming the actual rendered screen, font, RTL/wrapping, or that no other runtime path (e.g. a stale cached translation bundle) intervenes. Jono's own device pass is still the final word here, same limitation noted for every frontend-only change this engagement.

## A44 — Big batch: cancel-to-free, legal translation, pause/resume sharing, free-tier expiry fixes, drag-reorder investigation, Nunito font, 2026-08-26

Jono gave an 11-item batch before the next build, asked to work through in order with real investigation, live-testing where possible, and per-item reporting. Items 1, 2, 3, 4, 5, 9, 11 done this round (real bugs found and fixed along the way on several); 6, 7, 8, 10 deliberately paused per Jono's own explicit checkpoint request before continuing.

**1. Cancel-to-free subscription — built, live-verified end-to-end against a real Stripe test subscription.** `POST /subscription/cancel` schedules cancellation via `cancel_at_period_end=True` (not an immediate delete) — access continues until the paid period genuinely ends. `POST /subscription/resume` undoes it. No data-migration step needed on downgrade — confirmed `_is_genuinely_free_tier()`/`check_subscription_active()` already key off real expiry, and nothing in this codebase deletes creature/collection data on downgrade (only the separate, re-authenticated account-deletion flow does). Settings UI shows "Cancels on {date}" instead of the renewal countdown once cancelled, with a Resume action.
  - **Real bug found live-testing**: the `customer.subscription.updated`/`invoice.payment_succeeded` webhook handlers wrote `subscription_expires_at` as NULL whenever the event payload didn't carry `current_period_end` at the top level (Stripe's newer API versions moved it onto each subscription item instead) — not cancel-specific, this could fire on any renewal. Fixed with a fallback to the per-item field and by never overwriting a real value with nothing.
  - Full happy-path live-tested against a real Stripe test-mode subscription (customer/payment-method/subscription created via direct Stripe API calls, `STRIPE_TEST_MODE` flipped on briefly with Jono's explicit go-ahead each time, flipped back off immediately after): cancel correctly sets `cancel_at_period_end` on the real Stripe subscription (confirmed via direct Stripe API read), `GET /subscription/status` reflects it, resume correctly reverses it, `subscription_expires_at` survives both.

**2. `about.tsx`'s GDPR/COPPA legal prose — translated with real care, not a quick pass.** All 23 legal text blocks (disclaimer, privacy policy, terms of use) wired to real keys across pt/es/fr/de/it. GDPR localized to each jurisdiction's actual regulatory acronym (RGPD for pt/es/fr/it, DSGVO for de) rather than left as the English initialism; COPPA (a US statute, no EU equivalent) kept as-is rather than inventing a translated acronym; proper nouns, trademark symbols, the privacy-policy URL, and the contact email left untouched, matching standard legal-document translation practice. No terms required flagging as low-confidence — the content beyond the regulatory acronyms is descriptive factual prose (data practices, retention periods), not dense statutory language, translated directly without paraphrasing that could shift meaning. Honest caveat stated to Jono directly: a careful, good-faith translation using established terminology, not a substitute for professional legal review if this in-app text (rather than the canonical web privacy policy it already links to) is ever treated as independently binding.

**3. Real "Pause/Resume sharing with parent" — built, replacing a non-functional UI stopgap.** The existing button called `POST /parent-links/school-sharing`, which never existed on the backend (silent 404 swallowed by an empty catch) — pressing it did nothing, ever, and `school_sharing_enabled` was hardcoded `True` everywhere it was read. Built for real: `PUT /teacher/student/{id}/toggle-school-sharing`, gated to teacher/school_admin/superadmin with real per-student authorization (a linked parent cannot toggle their own visibility - this is the school's own consent control, mirroring `home_sharing_enabled` being the parent's). Pausing gates future reads only (`get_school_checkins`, `get_all_checkins_for_linked_child`, `get_school_strategies_for_linked_child`) — nothing is deleted, resuming restores full history instantly. Fixed a real multi-parent bug in the same code: the old home-sharing toggle only ever updated the *first* `parent_links` row for a student; the new endpoint updates every linked parent. Parent-side `linked-child/[id].tsx` now shows a real "sharing paused" notice using a `sharing_disabled` field the backend had returned since this was first scaffolded but nothing ever read.
  - **Two real bugs found live-testing against the real, not-yet-migrated table**: the toggle endpoint explicitly named the new column in a `select()`, which makes PostgREST error before the migration lands (unlike `select("*")`, which degrades gracefully) — 500'd instead of the intended 404. Fixed. The actual `UPDATE` can never succeed until the migration runs (expected, not fixable from application code) — but it was surfacing as an opaque 500; now returns a clean 503 with an honest, actionable message.
  - **Requires this migration** (no DDL access from code): `ALTER TABLE parent_links ADD COLUMN school_sharing_enabled boolean NOT NULL DEFAULT true;`
  - Also found, not fixed (flagged, not scope-crept): `teacher/student-detail.tsx` and `parent/linked-child/[id].tsx` have ~45 pre-existing missing `t()` keys unrelated to this change — a real i18n gap in two files outside last night's 9-file sweep. Logged for a future pass.

**4. Older `free_tier_limit` checks' expiry-blindness — fixed, same pattern as the Aug 24/25 creature-cap bugs.** Six ad hoc checks (`POST /students`, `POST /classrooms`, `POST /family/members`, `link_child`'s home-school-linking gate, `generate_pdf_report`'s student-PDF cap, `create_family_custom_strategy`) all used a blind `subscription_status in ("none","free",None)` pattern, ignoring real expiry — a lapsed trial/paid account dodged these caps forever. Two also used a bare `bool(school_admin_id)` instead of the real, already-fixed school-coverage helpers (meaning a teacher/parent linked to a *lapsed* school also dodged the cap forever). Swapped all six to `_is_genuinely_free_tier()`. Live-verified with a real regression test: a teacher with a lapsed "active" subscription now correctly gets capped at exactly 10 students (would have bypassed the cap entirely before the fix).

**5. Scope-button label nuance — already fixed, sitting un-uploaded.** Confirmed real and correct in the current local `portal100-UPLOAD-2026-08-23.html` from an earlier round; nothing to build, just needs Jono's manual cPanel upload alongside everything else already pending from that round.

**9. App-side drag-reorder for Strategies+Resources — investigated, the library conflict is fully resolved, not just resolvable.** `react-native-draggable-flatlist` v4.0.3 is already installed and compatible with the current `gesture-handler`/`reanimated` versions, and there's already a complete, working, production implementation in `admin/dashboard.tsx`. Backend support already exists too: `strategies`/`school_strategies` have `order_index`, already sorted server-side, already accept it in `PUT` bodies — ready to wire into `teacher/strategies.tsx` as-is. `resources` has `order_index` and `PUT` support but nothing sorts by it server-side yet (admin/dashboard.tsx works around this with a client-side sort — the same pattern would work for `teacher/resources.tsx`). One real gap: `custom_helpers` (a teacher's *per-student* custom strategies, a different table from the reusable strategy library) has no `order_index` column at all — would need a migration to reorder those specifically. Not built yet — investigation only, per how this item was scoped tonight.

**11. Nunito font wired into mobile — built.** Bundled the real Nunito font (Google Fonts' own repository, SIL Open Font License; only a variable-weight file ships upstream, confirmed against their repo listing — no separate static per-weight files exist to fetch). Loaded via `useFonts` in the root layout, gated behind the existing splash-screen hold. Applied as the app-wide default via `Text`/`TextInput` `defaultProps` rather than touching every screen's styles individually. Verified via a real `expo export` (not just a visual claim) — Metro bundled all 1914 modules cleanly, exported asset manifest confirms `assets/fonts/Nunito.ttf` (277 kB) is correctly included. Genuine on-device visual confirmation isn't possible in this environment (no simulator/browser available here) — recommend Jono's own Expo Go pass confirms the actual rendered look.

**Not started this round, paused at Jono's own explicit checkpoint request**: 6 (UI/UX consistency audit, ~30 files), 7 (superadmin portal-vs-app parity — the RN app has zero creature-moderation UI, real gap, not quick), 8 (Registered Schools vs Schools Breakdown merge — real design-call territory, different data sources), 10 (port world-creature-gallery + Subscriptions&Revenue from portal to app). Portal-touching work (7, 8, 10) explicitly approved by Jono to proceed on the local scratch file, upload handled manually by him same as always.

Committed in stages (`47cba32a`, `95aeef1e`, `5667f340`, `94ac1795`, `9d1727ec`, `cdb00551`, `475d21b2`), each pushed immediately after its own live-verification. `npx tsc --noEmit`: 23 errors throughout (one fewer than the prior 24-error baseline — not chased down, not a regression, noted for completeness).

**Item 3 follow-up, 2026-08-26**: Jono ran the `parent_links.school_sharing_enabled` migration. Re-verified against the real column (previously only the pre-migration graceful-degradation path had been confirmed): real teacher + student + **two** linked parents, a real seeded school check-in. Pause → `200`, both parents' `parent_links` rows genuinely updated (multi-parent fix confirmed against real data, not just code review), both parents correctly see empty check-ins + `sharing_disabled: true`, teacher-assigned strategies gated too. Resume → `200`, the exact same check-in reappears immediately for both parents — confirms nothing was ever deleted, only read-gated. Wrong-role toggle attempt still a clean `403`. Item 3 is now fully closed, no remaining gaps.

## A45 — Items 6 and 7: UI/UX consistency, superadmin creature-moderation screen, 2026-08-26

**6. UI/UX consistency audit — re-derived from real source (the original file lists were lost in context compaction), fixed, live-checked.**
- **Back buttons**: the app already has a canonical spec — `_layout.tsx`'s shared header and `TranslatedHeader` both use `arrow-back` at size 24, `#333`. ~19 screens duplicate their own inline back button instead of reusing `TranslatedHeader` (out of scope to force a full rewrite for a consistency pass); three had drifted from even that duplicated standard — `parent/widget.tsx`/`teacher/widget.tsx` (`size={22}`), `parent/strategies.tsx` (`size={20}`, indigo instead of gray, on both the icon and its "Back" text label). Fixed all three.
- **Capitalization**: built a heuristic scanner for section/field labels and button text, hand-filtered false positives (questions, stat captions under numbers, casual CTAs — not everything that isn't Title Case is wrong). Found and fixed 9 genuine inconsistencies — two introduced earlier in this same session's own work (`choose_your_plan`, `try_everything_free`), confirming the audit caught real issues, not just historical ones. Fixed `en.json` and matching JSX fallbacks only — deliberately did not touch pt/es/fr/de/it, since Title Case is English-specific and would be linguistically wrong applied to the other five. Also cleaned up two harmless `t(key) || t(key)` double-call bugs found along the way.
- Verified via a real `expo export` (clean bundle, 0 errors) — genuine on-device visual confirmation isn't possible in this environment.

**7. Superadmin creature-moderation screen — built, real parity with the portal, fully live-verified.** Investigated the portal's actual implementation first (`saCreatureQueue`/`saGlobalApprove`/`saGlobalReject`/`renderApprovedCreaturesInto`/`changeCreatureScope`/`featureCreature`/`saDeleteCreature` and the backend endpoints they call) before writing any app code, per Jono's explicit instruction. New "Creatures" tab in `admin/dashboard.tsx` (superadmin-only), following the file's existing componentized architecture exactly: Awaiting Final Approval queue (approve with Classroom/Family-private/School/Global scope choice, reject with reason, delete) and system-wide Approved Creatures list (change scope, feature, delete). Same classroom-vs-family-private relabeling rule as the A44 item 5 portal fix. Two deliberate mobile-native adaptations, not shortcuts: a real `TextInput` modal replacing the portal's browser `prompt()` for rejection reasons, and a real `Alert.alert` replacing its `confirm()` for delete.
  - **Live-verified end-to-end against production**, not just read against source: inserted a real test `creature_submissions` row, confirmed it surfaced correctly in the pending queue with the exact fields the new screen reads, approved it as family-private (`classroom_id` null — confirmed correct relabeling), confirmed it appeared correctly in the approved list, changed its scope to global, featured it, hard-deleted it, confirmed it was gone. Confirmed a non-superadmin gets a real `403` on the same endpoints.
  - `npx tsc --noEmit`: 23 errors, unchanged baseline. Real `expo export`: clean bundle. Genuine on-device visual confirmation not possible in this environment.

Committed and pushed (`0273703c`, `a94b3c2d`).

**Still open**: 8 (Registered Schools vs Schools Breakdown merge — real design-call territory, portal-side), 10 (port world-creature-gallery + Subscriptions&Revenue from portal to app).

## A46 — iPad/tablet Phase 1: reactive responsive utility, Dimensions.get migration, modal width caps, 2026-08-26

Scoped honestly first (21/44 screens with a real tablet issue, three categories), Jono confirmed portrait-only and authorized Phase 1 only (foundation + modal caps), explicitly held Phase 2 (grid reflow) for a separate go-ahead.

**Shared utility, `src/utils/globalStyles.ts` — rewired, not new.** Found this file already existed with almost the exact right shape (`isIPad`, `containerStyle`, `fontSize`, `spacing`, threshold 768) — but was dead code, imported nowhere in the app. Rewrote it in place as `useResponsive()`, a real `useWindowDimensions()`-based hook (reactive to iPad Split View/Slide Over resize and rotation, unlike the old `Dimensions.get('window')` read-once version), still unused by anything yet — it exists now as the foundation Phase 2 will build on. Exports `isTablet` (768pt threshold, platform-agnostic — deliberately dropped the old iOS-only gate since a large Android tablet deserves the same treatment), `fontSize()`/`spacing()` scalers, `containerStyle`, and `modalCapStyle`/`MODAL_MAX_WIDTH` (480) for the modal work below.

**11 `Dimensions.get('window')` call sites migrated.** Real mix, not uniform:
- **2 were dead code** (`student/zone.tsx`'s `width`, `parent/dashboard.tsx`'s `screenWidth` — declared, never read anywhere in either file) — removed rather than "migrated," since there was nothing to fix.
- **4 were genuine chart-width props** (`my-wellbeing.tsx`, `teacher/dashboard.tsx`, `parent/linked-child/[id].tsx`, `teacher/student-detail.tsx` — all feed a `BarChart`'s `width` prop) — moved to `useWindowDimensions()` called inside the component; these charts will now actually resize on Split View/rotation instead of freezing at whatever size the app launched at.
- **3 were module-level pixel grid constants** (`kiosk/index.tsx`'s `CARD_W`, `parent/widget.tsx`'s `CARD_W`, `teacher/widget.tsx`'s `CARD_SIZE` — all feeding a fixed 3- or 4-column `StyleSheet.create()` card width). These are architecturally close to the Phase-2 grid work but were in the Phase-1 list, so handled narrowly: moved the same divisor (3 or 4 columns — unchanged, that decision stays Phase 2's) inside the component via the hook and applied as an inline style override, so the pixel width is now reactive without touching column count.
- **1 was already effectively tablet-safe** (`CelebrationOverlay.tsx`'s card already had `maxWidth: 350`) — migrated anyway for reactivity consistency, moved the `width * 0.85` calc to an inline style since it can't live in a static `StyleSheet.create()` block driven by a hook.
- **1 became the rewritten shared utility** (`globalStyles.ts` itself).

**10 modals capped.** 9 needed real changes (added `width:'100%', maxWidth:480` to the bottom-sheet/centered card style, plus `alignItems:'center'` on the modal overlay — needed together, since capping width without centering the overlay would've just left it stuck flush-left on a tablet, and centering without `width:'100%'` would've broken the existing full-bleed phone layout by shrink-wrapping to content). One (`parent/dashboard.tsx`) was already correctly capped at `maxWidth: 400` with a centered overlay from earlier work — left untouched. Spot-checked the two files with 3 shared modal instances each (`teacher/resources.tsx`, `teacher/student-detail.tsx`) to confirm none is an image/gallery viewer that shouldn't be width-constrained — all are form/list content, correctly in scope.

**Live-verified**: `npx tsc --noEmit` — same 23 pre-existing errors as the confirmed baseline (diffed error-by-error against a clean `git stash` baseline run; every difference was just a line-number shift from removed dead code, zero new or fixed errors). `npx expo export --platform ios` — clean bundle, no Metro/bundling errors. Confirmed via source read that every touched modal's JSX usage site actually receives the new style (not just the `StyleSheet.create()` definition).

**What still needs Jono's own device/simulator pass** (no simulator or browser in this environment, stated up front): whether the modals actually *look* right on a real iPad — the 480pt cap and centering is mechanically correct but unverified visually; whether the reactive chart widths resize smoothly during an actual Split View drag rather than just computing the right number; the 3/4-column kiosk and widget grids at their current column count on a wider screen (Phase 2 will address whether the column count itself should change, but even unchanged, the reactive pixel width should be checked on a real device).

**Not started, per Jono's explicit hold**: Phase 2 (grid reflow for `student/creatures.tsx`, `teacher/checkin.tsx`, `parent/checkin.tsx`, `student/submit-creature.tsx`, `student/select.tsx`, `CreatureCollection.tsx`) and Phase 3 (polish).

## A47 — Items 8 and 10, 2026-08-26

**Item 8 — Registered Schools vs Schools Breakdown, investigated, not merged.** Direct re-inspection (not relayed) confirmed the earlier finding still holds: Registered Schools (`schoolManagementHTML()`, CRUD over `school_profiles`) and Schools Breakdown (dashboard card, live-computed from `feeling_logs` via `/admin/stats`) are genuinely different — different data source, different purpose, neither a subset of the other. Not merged. Two cheap polish items built instead: (1) renamed "Schools Breakdown" → "School Check-in Activity" in the portal to reduce the confusing name overlap; (2) real cross-linking — clicking a school row in the Breakdown chart now calls a new `saJumpToSchool(name)`, switches to the Schools tab, scrolls to and briefly highlights that school's actual Registered Schools card (matched by name against the already-loaded `saAllSchools`, since the breakdown payload carries no school id). Portal-only — confirmed the portal has zero i18n infrastructure anywhere (no `t()`, no translations object; the one `coh-portal-lang` reference is unrelated, just a PDF-report language param) and Jono confirmed it should stay English-only, matching its existing pattern as an internal superadmin tool. Syntax-checked (`node --check` on the extracted inline scripts).

**Item 10 — World Creature Gallery ported (real); Subscriptions & Revenue fixed at the root, not just ported.**
- **World Creature Gallery**: trivial, genuine port. New `SectionCard` in the app's existing Creatures tab (`admin/dashboard.tsx`), reusing the `approved` array `CreatureModeration` already fetches from `/creatures/global` for the Approved Creatures list — no second API call. Groups by `country`, same 5-creature minimum threshold as the portal. Live-verified against real production data: 6 approved global creatures exist today, all `country: "PT"`, clearing the threshold — confirms the gallery will show real content, not just an empty state.
- **Subscriptions & Revenue — real bug found, not just a missing aggregation.** Both the portal's card and the app's own pre-existing (but identically broken) `SectionCard` in `SuperAdminDashboard` have read `active_parents`/`annual_parents`/`active_teachers`/`annual_teachers`/`active_schools` from `/admin/stats` since the day each was built — none of the five fields were ever computed or returned. Both surfaces have shown nothing but dashes/undefined in production the entire time. Built real, expiry-aware aggregation in `/admin/stats` (superadmin/admin only, gated off for `school_admin` to avoid leaking platform-wide revenue data to a single school): `active_parents`/`active_teachers` count `users` with `subscription_status == "active"` and a non-expired `subscription_expires_at`; `active_schools` counts `school_profiles` with `status == "active"`.
- **`annual_parents`/`annual_teachers` deliberately dropped, not faked.** Confirmed live before building: `/subscription/checkout` explicitly rejects any plan other than `teacher_monthly`/`parent_monthly` — there is no self-serve annual plan for parents/teachers at all. Worse, `_get_plan_from_price()` collapses any price ID to `"parent"`/`"teacher"` regardless of interval, and no `subscription_interval` column exists anywhere — so even a manually-created Stripe subscription outside the app's checkout couldn't be distinguished as annual. Building these two fields would mean inventing data that doesn't exist. Jono's explicit decision: build the real active counts, drop the annual rows from both portal and app rather than show fake zeros or force a premature migration for a billing tier that isn't purchasable yet.
- **Stale annual pricing removed from every surface that had it**, per Jono's explicit request, not just hidden: portal footer and the app's `pricingBox` both advertised "€39.99/yr"/"€59.99/yr" with no purchase path — changed to "(monthly only)". Audited for any other mention: `app/subscription/index.tsx` (the real parent/teacher-facing subscription screen) was already clean — a prior Aug 18 fix already removed placeholder annual figures there, confirmed via its own code comment. `AppContext.tsx` has one leftover dead `annual: "Annual"` fallback-string key with zero references anywhere in the app — harmless, not fixed, flagged as minor housekeeping. Backend has no pricing display, nothing to fix. **Marketing site (classofhappiness.com) checked live** (fetched directly, no dedicated `/pricing` page or sitemap exists) — its `openModal(...)` pricing copy only advertises real monthly figures (€4.99/month parent, €7.99/month teacher, contact-based School Plan); no annual pricing found there. Also caught and fixed a separate pre-existing bug on the same touched line: the app's `pricingBox` displayed `$` (USD) while every other real pricing surface in the app uses `€` (EUR) — corrected to match.
- New UI text (World Creature Gallery labels, the fixed Subscriptions & Revenue card labels, pricing lines) wired into real `t()` keys — 12 new keys added to all 6 language files (en/pt/es/fr/de/it), genuine translations not placeholders, strictly additive (verified via diff — the only line touched in each pre-existing file was the prior last key gaining a trailing comma).
- **Live-verified end-to-end against production**, not just read against source: deployed, then via a real superadmin session, created a temporary paying parent + paying teacher + active `school_profiles` row, confirmed `/admin/stats` counts moved by exactly +1/+1/+1, deleted all three test rows, confirmed counts returned exactly to baseline (4/4/0 real production numbers) with a second fresh session. Confirmed `annual_parents`/`annual_teachers` are genuinely absent from the response, not present-but-zero. `npx tsc --noEmit`: same 23 pre-existing errors, none new. Real `expo export`: clean bundle.
- **What still needs Jono's own visual pass**: the portal changes (cross-linking, rename, fixed Subscriptions card) only take effect once he uploads the file via cPanel, same as always; genuine on-device confirmation that the new World Creature Gallery pills and the now-populated Subscriptions & Revenue card actually render correctly isn't possible in this environment.

Committed and pushed (backend + app + translations: `93e94840`; portal edited locally, upload pending Jono).
